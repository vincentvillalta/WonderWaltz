import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Turbopack is default in Next.js 16 — no explicit opt-in needed
  // Sentry will add its own next.config wrapper in Phase 8
};

export default nextConfig;
