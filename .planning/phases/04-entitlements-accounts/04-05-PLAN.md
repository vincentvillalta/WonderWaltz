---
phase: 04-entitlements-accounts
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - apps/api/src/auth/anonymous-trip-limit.guard.ts
  - apps/api/src/auth/anonymous-trip-limit.guard.spec.ts
  - apps/api/src/trips/trips.controller.ts
  - apps/api/src/plans/plans.controller.ts
  - packages/shared-openapi/openapi.v1.snapshot.json
autonomous: true
requirements:
  - AUTH-02
  - IAP-06

must_haves:
  truths:
    - "Anonymous user can create exactly one trip (second attempt returns 403 with upgrade prompt)"
    - "Anonymous user can view their free Day 1 teaser without signing up (existing plan entitlement projection works)"
    - "All protected endpoints have SupabaseAuthGuard applied"
    - "OpenAPI snapshot reflects all new endpoints (auth/anonymous, auth/upgrade, users/me, webhooks/revenuecat, purchases/restore, DELETE users/me)"
    - "IAP-06 App Store review notes draft exists as documentation artifact"
  artifacts:
    - path: "apps/api/src/auth/anonymous-trip-limit.guard.ts"
      provides: "1-trip limit for anonymous users"
      exports: ["AnonymousTripLimitGuard"]
    - path: "apps/api/src/auth/anonymous-trip-limit.guard.spec.ts"
      provides: "Trip limit guard tests"
  key_links:
    - from: "apps/api/src/auth/anonymous-trip-limit.guard.ts"
      to: "apps/api/src/auth/users.service.ts"
      via: "getTripsCount injection"
      pattern: "UsersService.*getTripsCount"
    - from: "apps/api/src/trips/trips.controller.ts"
      to: "apps/api/src/auth/auth.guard.ts"
      via: "@UseGuards(SupabaseAuthGuard)"
      pattern: "UseGuards.*SupabaseAuthGuard"
---

<objective>
Anonymous trip limit enforcement + auth guard wiring + OpenAPI snapshot update.

Purpose: AUTH-02 requires anonymous users to create exactly one trip — enforced server-side, not just client-side. All endpoints need auth guards applied. The OpenAPI snapshot must be updated to reflect all Phase 4 endpoints. IAP-06 (App Store review notes) is a documentation artifact for Phase 10 submission.

Output: AnonymousTripLimitGuard on POST /trips, auth guards applied across all controllers, updated OpenAPI snapshot, IAP-06 review notes draft.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-entitlements-accounts/04-CONTEXT.md
@.planning/phases/04-entitlements-accounts/04-01-SUMMARY.md
@.planning/phases/04-entitlements-accounts/04-02-SUMMARY.md
@.planning/phases/04-entitlements-accounts/04-03-SUMMARY.md
@.planning/phases/04-entitlements-accounts/04-04-SUMMARY.md

<interfaces>
From apps/api/src/auth/users.service.ts (created in 04-02):
```typescript
export class UsersService {
  getUserProfile(userId: string): Promise<UserMeDto | null>;
  getTripsCount(userId: string): Promise<number>;
}
```

From apps/api/src/auth/auth.guard.ts (created in 04-01):
```typescript
export class SupabaseAuthGuard implements CanActivate { ... }
```

From apps/api/src/auth/anonymous-purchase.guard.ts (created in 04-02):
```typescript
export class AnonymousPurchaseGuard implements CanActivate { ... }
```

OpenAPI snapshot generation (from Phase 2 patterns):
```bash
cd apps/api && NODE_ENV=test node dist/scripts/generate-openapi-snapshot.js
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnonymousTripLimitGuard + auth guard wiring on all controllers</name>
  <files>apps/api/src/auth/anonymous-trip-limit.guard.ts, apps/api/src/auth/anonymous-trip-limit.guard.spec.ts, apps/api/src/trips/trips.controller.ts, apps/api/src/plans/plans.controller.ts, apps/api/src/auth/auth.module.ts</files>
  <behavior>
    - Test 1: Guard passes when user is not anonymous (isAnonymous=false)
    - Test 2: Guard passes when anonymous user has 0 trips (first trip creation allowed)
    - Test 3: Guard throws 403 with upgrade_required when anonymous user already has 1+ trip
    - Test 4: Guard reads trip count from UsersService.getTripsCount
  </behavior>
  <action>
    1. Create `apps/api/src/auth/anonymous-trip-limit.guard.ts`:
       - NestJS CanActivate guard
       - Inject UsersService
       - If `request.user.isAnonymous` is false, pass (registered users have no limit)
       - If anonymous: call `usersService.getTripsCount(request.user.id)`
       - If count >= 1, throw 403 with `{ error: 'upgrade_required', message: 'Anonymous users can create one trip. Upgrade to create more.' }`
       - If count < 1, pass

    2. Apply `@UseGuards(SupabaseAuthGuard)` to trips.controller.ts:
       - All trip CRUD endpoints need auth
       - Apply `@UseGuards(SupabaseAuthGuard, AnonymousTripLimitGuard)` specifically on POST /trips (create) — per CONTEXT.md: "one trip per anonymous user" enforced at API layer
       - Ensure trip query methods filter by `request.user.id` (trips belong to the authenticated user)

    3. Apply `@UseGuards(SupabaseAuthGuard)` to plans.controller.ts:
       - Plan viewing and generation endpoints need auth
       - Existing entitlement projection logic (free tier sees Day 1 only) already exists from Phase 3

    4. Export UsersService from AuthModule so AnonymousTripLimitGuard can inject it

    5. Create spec file for the guard

    Note: The guard must run AFTER SupabaseAuthGuard (which sets request.user). Guard ordering in @UseGuards is left-to-right, so: `@UseGuards(SupabaseAuthGuard, AnonymousTripLimitGuard)`.
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run src/auth/anonymous-trip-limit.guard.spec.ts -x</automated>
  </verify>
  <done>Anonymous users limited to 1 trip server-side. All trip/plan controllers protected by auth guard. AUTH-02 enforcement complete.</done>
</task>

<task type="auto">
  <name>Task 2: OpenAPI snapshot update + IAP-06 review notes draft + full test suite</name>
  <files>packages/shared-openapi/openapi.v1.snapshot.json</files>
  <action>
    1. Rebuild and regenerate the OpenAPI snapshot to capture all new Phase 4 endpoints:
       - `cd apps/api && pnpm run build` (tsc compile needed for snapshot generation)
       - `cd apps/api && NODE_ENV=test node dist/scripts/generate-openapi-snapshot.js`
       - Copy output to `packages/shared-openapi/openapi.v1.snapshot.json`
       - New endpoints that should appear: POST /v1/auth/anonymous, POST /v1/auth/upgrade, GET /v1/users/me, DELETE /v1/users/me, POST /v1/webhooks/revenuecat, POST /v1/purchases/restore

    2. Run the full test suite to verify nothing is broken:
       - `cd apps/api && pnpm test` — all existing + new tests pass
       - `cd apps/api && pnpm run build` — clean build

    3. Create IAP-06 documentation artifact — App Store review notes draft:
       - Add a file at `docs/app-store/review-notes-draft.md` with content:
         ```
         # App Store Review Notes — WonderWaltz

         ## In-App Purchase Model
         WonderWaltz uses a single consumable in-app purchase: "Trip Unlock" at $9.99.

         Each $9.99 unlock applies to one trip planning session. Once applied, the credit is
         consumed and cannot be reused for a different trip. Users can purchase multiple unlocks
         for multiple trips.

         ## How It Works
         1. Users create a trip for free and receive a complimentary Day 1 preview
         2. To see the full multi-day plan, users purchase a Trip Unlock ($9.99)
         3. The unlock is tied to that specific trip and cannot be transferred
         4. Users can create new trips and purchase additional unlocks as needed

         ## Demo Account
         [To be configured before submission — sandbox test account credentials]

         ## Content
         WonderWaltz is an independent, unofficial planning app for Walt Disney World visitors.
         It is not affiliated with, endorsed by, or sponsored by The Walt Disney Company.
         ```
       - This is a Phase 10 submission asset (IAP-06) — the draft is created now, finalized before submission

    4. Verify the snapshot CI gate will pass by checking the diff is only additive (new endpoints, no breaking changes to existing shapes)
  </action>
  <verify>
    <automated>cd apps/api && pnpm test && pnpm run build</automated>
  </verify>
  <done>OpenAPI snapshot updated with all Phase 4 endpoints. Full test suite green. IAP-06 review notes draft created. Phase 4 integration verified.</done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm test` — full test suite passes
- `cd apps/api && pnpm run build` — clean TypeScript build
- OpenAPI snapshot contains all 6 new endpoints
- `docs/app-store/review-notes-draft.md` exists with consumable IAP explanation
</verification>

<success_criteria>
- Anonymous users blocked from creating >1 trip (server-side)
- All controllers protected by SupabaseAuthGuard
- OpenAPI snapshot reflects complete Phase 4 API surface
- Full test suite green
- IAP-06 review notes draft created
</success_criteria>

<output>
After completion, create `.planning/phases/04-entitlements-accounts/04-05-SUMMARY.md`
</output>
