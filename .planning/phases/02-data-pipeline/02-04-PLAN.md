---
phase: 02-data-pipeline
plan: "04"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/api/src/ingestion/ingestion.module.ts
  - apps/api/src/ingestion/queue-times.service.ts
  - apps/api/src/ingestion/queue-times.processor.ts
  - apps/api/src/ingestion/queue-times.service.spec.ts
  - apps/api/src/ingestion/queue-times.processor.spec.ts
  - apps/api/src/worker.module.ts
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Every 5 minutes, queue-times.com is polled for all 4 WDW parks (MK=6, EPCOT=5, HS=7, AK=8)"
    - "Each ride's wait time is written to Redis key wait:{ride_id} with 2-minute TTL"
    - "Each ride's data is inserted into wait_times_history with source='queue-times'"
    - "On fetch failure, existing Redis key TTL is extended by 10 minutes instead of deleted"
    - "is_stale=true when fetched_at is more than 5 minutes ago in Redis value"
  artifacts:
    - path: "apps/api/src/ingestion/queue-times.service.ts"
      provides: "HTTP calls to queue-times.com; Redis write + TTL logic; DB insert"
      exports: ["QueueTimesService"]
    - path: "apps/api/src/ingestion/queue-times.processor.ts"
      provides: "@Processor('wait-times') WorkerHost; upsertJobScheduler every 5min; dead-letter detection"
      exports: ["QueueTimesProcessor"]
    - path: "apps/api/src/ingestion/queue-times.service.spec.ts"
      provides: "DATA-01a/01c/01d tests: field mapping, Redis write, TTL extension on failure"
    - path: "apps/api/src/ingestion/queue-times.processor.spec.ts"
      provides: "DATA-06a test: dead-letter handler calls Sentry"
  key_links:
    - from: "apps/api/src/ingestion/queue-times.processor.ts"
      to: "apps/api/src/ingestion/queue-times.service.ts"
      via: "this.queueTimesService.pollPark(parkId)"
      pattern: "pollPark"
    - from: "apps/api/src/ingestion/queue-times.service.ts"
      to: "wait_times_history table"
      via: "db.insert(waitTimesHistory)"
      pattern: "insert.*waitTimesHistory"
    - from: "apps/api/src/ingestion/queue-times.service.ts"
      to: "Redis key wait:{ride_id}"
      via: "redis.set / redis.expire"
      pattern: "redis\\.(set|expire).*wait:"
---

<objective>
Build the queue-times.com ingestion worker: QueueTimesService (HTTP + Redis + DB) and QueueTimesProcessor (BullMQ processor + scheduler). This is the core of DATA-01 — the 5-minute polling loop that starts the 8-week accumulation clock.

Purpose: Once this plan executes and deploys (plan 02-12), wait:{ride_id} Redis keys start accumulating and wait_times_history rows start growing.

Output: IngestionModule with queue-times processor fully tested. Wire into WorkerModule.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@apps/api/src/worker.module.ts
@packages/db/src/schema/timeseries.ts
@packages/db/src/schema/catalog.ts
@apps/api/tests/fixtures/queue-times-response.json

<interfaces>
<!-- queue-times.com API (RESEARCH.md) -->
<!-- WDW Park IDs: EPCOT=5, MK=6, HS=7, AK=8 -->
<!-- Endpoint: GET https://queue-times.com/parks/{id}/queue_times.json -->
<!-- Response: { lands: [{ id, name, rides: [{ id, name, is_open, wait_time, last_updated }] }], rides: [] } -->

<!-- Field mapping (RESEARCH.md Pitfall 3 — critical): -->
<!-- ride.id → attractions.queue_times_id (integer) → look up UUID from catalog -->
<!-- ride.wait_time → wait_times_history.minutes -->
<!-- ride.is_open → wait_times_history.is_open -->
<!-- new Date() at fetch time → fetched_at (NOT ride.last_updated) -->
<!-- source: hardcode 'queue-times' -->

<!-- Redis key: wait:{ride_uuid} -->
<!-- Redis value: JSON.stringify({ minutes, fetched_at: ISO8601, source: 'queue-times', is_stale: false }) -->
<!-- Normal TTL: 2 * 60 = 120 seconds -->
<!-- Failure TTL extension: +10 * 60 = +600 seconds via EXPIRE (not SET) -->

<!-- BullMQ Pattern 3 (RESEARCH.md): -->
<!-- @Processor('wait-times', { concurrency: 1, settings: { backoffStrategy: ... } }) -->
<!-- extends WorkerHost -->
<!-- async process(job: Job): Promise<void> -->
<!-- @OnWorkerEvent('failed') onFailed(job, error) — check attemptsMade >= maxAttempts for dead-letter -->

<!-- Pattern 4 (RESEARCH.md) — upsertJobScheduler: -->
<!-- await this.waitTimesQueue.upsertJobScheduler('fetch-wait-times-scheduler', { every: 5 * 60 * 1000 }, { name: 'fetch_queue_times', opts: { attempts: 5, backoff: { type: 'fixed', delay: 30_000 } } }) -->

From packages/db/src/schema/catalog.ts:
```typescript
export const attractions = pgTable('attractions', {
  id: uuid('id').primaryKey(),
  queueTimesId: integer('queue_times_id'),
  // ...
});
```

From packages/db/src/schema/timeseries.ts:
```typescript
export const waitTimesHistory = pgTable('wait_times_history', {
  rideId: uuid('ride_id').notNull(),
  ts: timestamp('ts', { withTimezone: true }).notNull(),
  minutes: integer('minutes').notNull(),
  isOpen: boolean('is_open').notNull(),
  source: text('source').notNull(),
  fetchedAt: timestamp('fetched_at', { withTimezone: true }).notNull().defaultNow(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QueueTimesService — HTTP fetch, Redis write, DB insert</name>
  <files>
    apps/api/src/ingestion/queue-times.service.ts,
    apps/api/src/ingestion/queue-times.service.spec.ts
  </files>
  <behavior>
    - pollPark(queueTimesParksId: number): fetch rides from queue-times.com, resolve UUID from catalog, write Redis + DB
    - Redis key wait:{rideUuid}: JSON value { minutes, fetched_at, source: 'queue-times', is_stale: false }; SET with EX 120
    - DB insert: insert into wait_times_history (rideId, ts, minutes, isOpen, source, fetchedAt) — ts = fetched_at
    - On fetch failure (catch): for each known ride_uuid already in Redis, call redis.expire(key, 600) — DO NOT write to DB
    - is_stale field in Redis value: always false on write; reading endpoint sets it at read time based on fetched_at age
    - resolveAttractionIds(parkId): pre-query catalog for attractions.queue_times_id → id mapping; cache in-memory for the process lifetime
    - Test DATA-01a: mock fetch returns fixture; verify correct field mapping (wait_time→minutes, is_open preserved, source='queue-times')
    - Test DATA-01c: after successful poll, redis.set called with key 'wait:{uuid}' and EX 120
    - Test DATA-01d: on fetch throw, redis.expire called with +600 on existing keys; db.insert NOT called
  </behavior>
  <action>
    Create apps/api/src/ingestion/queue-times.service.ts:
    - @Injectable() class QueueTimesService
    - Constructor injects: db (Drizzle via DATABASE token), ioredis client, ConfigService
    - WDW park IDs as constant: PARK_IDS = [5, 6, 7, 8] (EPCOT, MK, HS, AK)
    - async resolveAttractionIds(): Map<number, string> (queue_times_id integer → internal UUID string). Use Drizzle: db.select({ id, queueTimesId }).from(attractions).where(isNotNull(attractions.queueTimesId))
    - async pollPark(queueTimesId: number): Promise<void>
      1. Fetch https://queue-times.com/parks/${queueTimesId}/queue_times.json using Node 22 native fetch
      2. Flatten lands[].rides into flat rides array
      3. fetchedAt = new Date() — NOT ride.last_updated
      4. For each ride: look up UUID from resolveAttractionIds map; skip if not found
      5. redis.set(`wait:${uuid}`, JSON.stringify({ minutes: ride.wait_time, fetched_at: fetchedAt.toISOString(), source: 'queue-times', is_stale: false }), 'EX', 120)
      6. db.insert(waitTimesHistory).values({ rideId: uuid, ts: fetchedAt, minutes: ride.wait_time, isOpen: ride.is_open, source: 'queue-times', fetchedAt })
    - async extendTtlOnFailure(): Promise<void> — get all keys matching wait:* from Redis; call redis.expire(key, 600) for each
    - Use onConflictDoNothing() on insert to handle duplicate (ride_id, ts) pairs gracefully

    Create queue-times.service.spec.ts: Vitest unit tests. Mock fetch using vi.fn() or globalThis.fetch = vi.fn(). Import queue-times-response.json fixture. Test all three DATA-01 behaviors.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- --run queue-times.service</automated>
  </verify>
  <done>DATA-01a, 01c, 01d tests all green; no live network calls in tests (fetch mocked)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QueueTimesProcessor — BullMQ processor + scheduler + IngestionModule</name>
  <files>
    apps/api/src/ingestion/queue-times.processor.ts,
    apps/api/src/ingestion/queue-times.processor.spec.ts,
    apps/api/src/ingestion/ingestion.module.ts,
    apps/api/src/worker.module.ts
  </files>
  <behavior>
    - Processor registers job scheduler (upsertJobScheduler) for 5-minute interval on startup
    - process(job): calls queueTimesService.pollPark for each of 4 WDW parks; then lagAlertService.checkAndAlert()
    - onFailed: when attemptsMade >= maxAttempts → slackAlerterService.sendDeadLetter(...) + Sentry.captureException
    - onCompleted: slackAlerterService.resetConsecutiveCount('wait-times')
    - Test DATA-06a: spy on Sentry.captureException; trigger onFailed with attemptsMade = maxAttempts; verify called
    - Test: onFailed NOT calling Sentry when attemptsMade < maxAttempts (transient failure)
  </behavior>
  <action>
    Create apps/api/src/ingestion/queue-times.processor.ts:
    - @Processor('wait-times', { concurrency: 1, settings: { backoffStrategy: (n: number) => n * 30_000 } }) class QueueTimesProcessor extends WorkerHost
    - Inject: QueueTimesService, AlertingModule's SlackAlerterService, LagAlertService, @InjectQueue('wait-times') waitTimesQueue
    - async onModuleInit(): call this.waitTimesQueue.upsertJobScheduler('fetch-wait-times-scheduler', { every: 5 * 60 * 1000 }, { name: 'fetch_queue_times', data: {}, opts: { attempts: 5, backoff: { type: 'fixed', delay: 30_000 } } })
    - async process(job: Job): Promise<void>
      1. For each park ID in [5, 6, 7, 8]: await this.queueTimesService.pollPark(parkId)
      2. await this.lagAlertService.checkAndAlert()
    - @OnWorkerEvent('failed') async onFailed(job: Job, error: Error): check job.attemptsMade >= (job.opts.attempts ?? 1); if so: Sentry.captureException + slackAlerter.sendDeadLetter
    - @OnWorkerEvent('completed') async onCompleted(): slackAlerter.resetConsecutiveCount('wait-times')

    Create queue-times.processor.spec.ts: unit tests with mocked services. Verify dead-letter branch triggers Sentry + Slack only at maxAttempts.

    Create apps/api/src/ingestion/ingestion.module.ts:
    - BullModule.registerQueue({ name: 'wait-times' })
    - providers: [QueueTimesService, QueueTimesProcessor]
    - imports: [BullModule.registerQueue({ name: 'wait-times' }), AlertingModule]
    - exports: [QueueTimesService]

    Update apps/api/src/worker.module.ts: add IngestionModule to imports array.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- --run "queue-times.processor|queue-times.service"</automated>
  </verify>
  <done>All DATA-01 and DATA-06a unit tests green; IngestionModule compiles and is wired into WorkerModule</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm --filter @wonderwaltz/api test -- --run "queue-times"` — all DATA-01a/01c/01d and DATA-06a tests pass
2. `pnpm --filter @wonderwaltz/api tsc --noEmit` — no TypeScript errors
3. Confirm processor uses upsertJobScheduler (not queue.add with repeat) — grep for upsertJobScheduler
4. Confirm fetched_at = new Date() in pollPark, NOT ride.last_updated (Pitfall 3 guard)
</verification>

<success_criteria>
- DATA-01a: queue-times.com response correctly mapped (wait_time→minutes, is_open, source='queue-times')
- DATA-01c: Redis key wait:{uuid} set with EX 120 on success
- DATA-01d: on fetch failure, redis.expire(key, 600) called; no DB insert
- DATA-06a: Sentry.captureException called on dead-letter (attemptsMade >= maxAttempts)
- QueueTimesProcessor wired into IngestionModule → WorkerModule
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-04-SUMMARY.md`
</output>
