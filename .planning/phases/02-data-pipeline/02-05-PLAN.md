---
phase: 02-data-pipeline
plan: "05"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/api/src/ingestion/themeparks.service.ts
  - apps/api/src/ingestion/themeparks.processor.ts
  - apps/api/src/ingestion/themeparks.service.spec.ts
  - apps/api/src/ingestion/ingestion.module.ts
  - apps/api/src/worker.module.ts
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "Every 6 hours, themeparks.wiki is polled for schedule (hours) and live data for all 4 WDW parks"
    - "Park hours are written to Redis key park_hours:{park_uuid}:{YYYY-MM-DD} with appropriate TTL"
    - "Show showtimes are written to Redis key showtimes:{park_uuid}:{YYYY-MM-DD}"
    - "Wait times from themeparks.wiki can also be written to wait_times_history with source='themeparks-wiki' as fallback"
    - "Conflict resolution: most recent fetched_at wins when both sources report the same ride"
  artifacts:
    - path: "apps/api/src/ingestion/themeparks.service.ts"
      provides: "HTTP calls to themeparks.wiki /schedule and /live endpoints; Redis write; DB insert"
      exports: ["ThemeparksService"]
    - path: "apps/api/src/ingestion/themeparks.processor.ts"
      provides: "@Processor('park-hours') WorkerHost; upsertJobScheduler every 6hr"
      exports: ["ThemeparksProcessor"]
    - path: "apps/api/src/ingestion/themeparks.service.spec.ts"
      provides: "DATA-02a/02b tests: schedule endpoint field mapping, STANDBY.waitTime mapping"
  key_links:
    - from: "apps/api/src/ingestion/themeparks.service.ts"
      to: "https://api.themeparks.wiki/v1/entity/{entityId}/schedule"
      via: "Node native fetch"
      pattern: "themeparks.wiki"
    - from: "apps/api/src/ingestion/themeparks.service.ts"
      to: "wait_times_history table"
      via: "db.insert(waitTimesHistory).values({ source: 'themeparks-wiki' })"
      pattern: "themeparks-wiki"
---

<objective>
Build the themeparks.wiki secondary ingestion worker: ThemeparksService (schedule + live data) and ThemeparksProcessor (6-hour BullMQ job). This provides park hours, entertainment showtimes, and fallback wait-time data.

Purpose: DATA-02 — park hours feed the solver's opening/closing time constraints; showtimes feed entertainment scheduling. The 6hr polling cadence is independent of the primary 5min cycle — it NEVER fills in when queue-times.com is down.

Output: ThemeparksService + ThemeparksProcessor with full tests. Registered alongside QueueTimesProcessor in IngestionModule.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@apps/api/src/ingestion/ingestion.module.ts
@apps/api/tests/fixtures/themeparks-wiki-response.json
@packages/db/src/schema/catalog.ts

<interfaces>
<!-- themeparks.wiki WDW Entity IDs (RESEARCH.md) -->
<!-- MK: 75ea578a-adc8-4116-a54d-dccb60765ef9 -->
<!-- EPCOT: 47f90d2c-e191-4239-a466-5892ef59a88b -->
<!-- HS: 288747d1-8b4f-4a64-867e-ea7c9b27bad8 -->
<!-- AK: 1c84a229-8862-4648-9c71-378ddd2c7693 -->

<!-- Endpoints: -->
<!-- GET https://api.themeparks.wiki/v1/entity/{entityId}/schedule -->
<!-- GET https://api.themeparks.wiki/v1/entity/{entityId}/live -->

<!-- Schedule response (from RESEARCH.md): -->
<!-- { schedule: [{ date, type: 'OPERATING'|'TICKETED_EVENT', openingTime, closingTime }] } -->

<!-- Live response (from RESEARCH.md): -->
<!-- { liveData: [{ id (themeparks_wiki_id), entityType, status, lastUpdated, queue: { STANDBY: { waitTime } }, showtimes? }] } -->

<!-- Field mapping: -->
<!-- entity.id → attractions.themeparks_wiki_id → look up internal UUID -->
<!-- queue.STANDBY.waitTime → wait_times_history.minutes -->
<!-- status 'OPERATING' → is_open: true; anything else → is_open: false -->
<!-- source: 'themeparks-wiki' -->

<!-- CONTEXT.md: do NOT poll secondary out-of-schedule as primary failover -->
<!-- The 6hr cycle is its own clock; it is NOT triggered by queue-times.com failures -->

<!-- Conflict resolution: most recent fetched_at wins in Redis -->
<!-- DB: insert with onConflictDoNothing (same ride_id + ts should not happen but guard against it) -->

<!-- Redis keys for park hours: park_hours:{park_uuid}:{YYYY-MM-DD} -->
<!--   Value: JSON { openingTime, closingTime, type, date } -->
<!--   TTL: 24 hours (86400) -->
<!-- Redis keys for showtimes: showtimes:{park_uuid}:{YYYY-MM-DD} -->
<!--   Value: JSON array of { entityId, name, showtimes: [{ startTime, endTime }] } -->
<!--   TTL: 24 hours -->

<!-- upsertJobScheduler pattern (every 6 hours): -->
<!-- { every: 6 * 60 * 60 * 1000 } = 21600000 ms -->
<!-- Stagger startup: offset by cron pattern to avoid simultaneous firing with other workers -->
<!-- Use cron: '0 1,7,13,19 * * *' (1am, 7am, 1pm, 7pm) to stagger from queue-times -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ThemeparksService — schedule + live endpoints + Redis + DB</name>
  <files>
    apps/api/src/ingestion/themeparks.service.ts,
    apps/api/src/ingestion/themeparks.service.spec.ts
  </files>
  <behavior>
    - pollSchedule(entityId, parkUuid): fetch schedule endpoint; write park_hours and showtimes to Redis
    - pollLiveData(entityId, parkUuid): fetch live endpoint; for ATTRACTION entities write wait_times_history + Redis; for SHOW entities write showtimes
    - Test DATA-02a: mock schedule endpoint fixture; verify park_hours Redis key set with correct { openingTime, closingTime }
    - Test DATA-02b: mock live fixture; verify queue.STANDBY.waitTime mapped to minutes in wait_times_history insert
    - Test: SHOW entityType populates showtimes Redis key; ATTRACTION entityType writes wait_times_history
    - Test: status != 'OPERATING' maps to is_open: false
  </behavior>
  <action>
    Create apps/api/src/ingestion/themeparks.service.ts:
    - @Injectable() class ThemeparksService
    - ENTITY_IDS constant: Record<string, string> mapping park_uuid → themeparks_wiki_id (hardcoded from RESEARCH.md)
    - Inject: db, ioredis client, ConfigService
    - async resolveAttractionIds(): Map<string, string> (themeparks_wiki_id UUID string → internal UUID). Query attractions.themeparks_wiki_id IS NOT NULL
    - async pollSchedule(entityId: string, parkUuid: string): Promise<void>
      1. Fetch https://api.themeparks.wiki/v1/entity/${entityId}/schedule
      2. For today and next 7 days: write Redis key park_hours:${parkUuid}:${date} = JSON { openingTime, closingTime, type }; EX 86400
    - async pollLiveData(entityId: string, parkUuid: string): Promise<void>
      1. Fetch https://api.themeparks.wiki/v1/entity/${entityId}/live
      2. fetchedAt = new Date()
      3. For ATTRACTION with STANDBY queue: look up UUID; write wait_times_history + Redis key wait:{uuid} with EX 120 ONLY IF no existing key (NX flag) — conflict resolution: don't overwrite if queue-times wrote more recently
      4. Actually: read existing Redis key; compare fetched_at timestamps; overwrite only if this fetched_at is more recent
      5. For SHOW entities: aggregate showtimes into Redis key showtimes:${parkUuid}:${date}; EX 86400

    Create themeparks.service.spec.ts: unit tests using fixture from tests/fixtures/themeparks-wiki-response.json. Test all DATA-02a/02b behaviors.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- --run themeparks.service</automated>
  </verify>
  <done>DATA-02a and DATA-02b tests green; schedule mapping correct; STANDBY waitTime mapped; no live network calls</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ThemeparksProcessor + update IngestionModule</name>
  <files>
    apps/api/src/ingestion/themeparks.processor.ts,
    apps/api/src/ingestion/ingestion.module.ts,
    apps/api/src/worker.module.ts
  </files>
  <action>
    Create apps/api/src/ingestion/themeparks.processor.ts:
    - @Processor('park-hours', { concurrency: 1, settings: { backoffStrategy: (n: number) => n * 30_000 } }) class ThemeparksProcessor extends WorkerHost
    - Inject: ThemeparksService, SlackAlerterService, @InjectQueue('park-hours') parkHoursQueue
    - async onModuleInit(): await this.parkHoursQueue.upsertJobScheduler('fetch-park-hours-scheduler', { pattern: '0 1,7,13,19 * * *' }, { name: 'fetch_themeparks_wiki_hours', data: {}, opts: { attempts: 5, backoff: { type: 'fixed', delay: 30_000 } } })
      Staggered hours (1am, 7am, 1pm, 7pm) avoid firing at the same time as wait-times scheduler
    - async process(job: Job): Promise<void>
      Loop over 4 parks: await themeparksService.pollSchedule(entityId, parkUuid) then pollLiveData(entityId, parkUuid)
    - @OnWorkerEvent('failed') dead-letter handler (same pattern as QueueTimesProcessor)
    - @OnWorkerEvent('completed') resetConsecutiveCount('park-hours')

    Update apps/api/src/ingestion/ingestion.module.ts:
    - Add 'park-hours' to BullModule.registerQueue list
    - Add ThemeparksService, ThemeparksProcessor to providers
    - Add ThemeparksService to exports
    Note: This file was created in 02-04; add to it, don't replace.

    Update apps/api/src/worker.module.ts: confirm IngestionModule is imported (already done by 02-04 but verify).
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api tsc --noEmit && pnpm --filter @wonderwaltz/api test -- --run themeparks</automated>
  </verify>
  <done>ThemeparksProcessor compiles; IngestionModule registers park-hours queue; no TypeScript errors</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm --filter @wonderwaltz/api test -- --run "themeparks|queue-times"` — all ingestion tests pass together
2. Verify ThemeparksProcessor uses cron pattern with offset (not `every: 6hr` starting at :00)
3. Confirm ThemeparksService does NOT poll live data when queue-times.com fails — it has no awareness of QueueTimesService state
</verification>

<success_criteria>
- DATA-02a: schedule endpoint openingTime/closingTime mapped to Redis correctly
- DATA-02b: queue.STANDBY.waitTime → minutes field in wait_times_history insert
- ThemeparksProcessor registers 6hr scheduled job with staggered cron offset
- IngestionModule now handles both 'wait-times' and 'park-hours' queues
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-05-SUMMARY.md`
</output>
