---
phase: 02-data-pipeline
plan: "07"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/api/src/crowd-index/crowd-index.module.ts
  - apps/api/src/crowd-index/crowd-index.service.ts
  - apps/api/src/crowd-index/crowd-index.processor.ts
  - apps/api/src/crowd-index/crowd-index.service.spec.ts
  - apps/api/src/crowd-index/crowd-index.processor.spec.ts
  - apps/api/src/worker.module.ts
autonomous: true
requirements:
  - DATA-04
must_haves:
  truths:
    - "crowd_index:{date} Redis key is updated hourly with a 0–100 normalized value"
    - "Per-park keys crowd_index:{park-slug}:{date} are set for all 4 WDW parks"
    - "Bootstrap mode (< 30 days of data): value = min(100, avg_wait × 1.2)"
    - "Percentile mode (≥ 30 days): 0–100 scale via percentile_cont against 90-day history"
    - "Bootstrap/percentile switch is automatic based on data window, not manual"
    - "Every response includes confidence metadata: { value, confidence, sample_size_days }"
  artifacts:
    - path: "apps/api/src/crowd-index/crowd-index.service.ts"
      provides: "Bootstrap formula, percentile calculation, bootstrap-switch gate, Redis write"
      exports: ["CrowdIndexService"]
    - path: "apps/api/src/crowd-index/crowd-index.processor.ts"
      provides: "@Processor('crowd-index') WorkerHost; upsertJobScheduler every 1hr"
      exports: ["CrowdIndexProcessor"]
    - path: "apps/api/src/crowd-index/crowd-index.service.spec.ts"
      provides: "DATA-04a (bootstrap formula), DATA-04b (percentile), DATA-04c (5 Redis keys), DATA-04d (confidence metadata)"
  key_links:
    - from: "apps/api/src/crowd-index/crowd-index.service.ts"
      to: "wait_times_history table"
      via: "db.execute(sql`SELECT percentile_cont...`)"
      pattern: "percentile_cont"
    - from: "apps/api/src/crowd-index/crowd-index.service.ts"
      to: "Redis keys crowd_index:{date} and crowd_index:{park}:{date}"
      via: "redis.set(key, JSON.stringify({ value, confidence, sample_size_days }), 'EX', ttl)"
      pattern: "crowd_index:"
---

<objective>
Build the crowd index worker (DATA-04): percentile-based normalization with automatic bootstrap→percentile switch and per-park emissions.

Purpose: The crowd index is the primary context signal for the Phase 3 solver. Building it in Phase 2 means 30+ days of data accumulate during engine development. The bootstrap formula keeps it useful from day 1.

Output: CrowdIndexService (pure calculations) + CrowdIndexProcessor (BullMQ 1hr job) with full unit test coverage.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@apps/api/src/worker.module.ts
@packages/db/src/schema/catalog.ts

<interfaces>
<!-- Crowd index formula from CONTEXT.md: -->
<!-- Top 20 rides = top 5 per park × 4 parks (MK, EPCOT, HS, AK) -->
<!-- Ranked by historical baseline wait time within each park -->

<!-- 0–100 scale via percentile mapping against last 90 days of history: -->
<!-- p0 → 0, p50 → 50, p95 → 95 (linear interpolation between anchor points) -->

<!-- Bootstrap period (first 30d): min(100, avg_wait × 1.2) -->
<!-- Switch trigger: SELECT COUNT(DISTINCT DATE(ts)) FROM wait_times_history ≥ 30 -->

<!-- Redis keys (5 total): -->
<!-- crowd_index:{YYYY-MM-DD} — global -->
<!-- crowd_index:magic-kingdom:{YYYY-MM-DD} -->
<!-- crowd_index:epcot:{YYYY-MM-DD} -->
<!-- crowd_index:hollywood-studios:{YYYY-MM-DD} -->
<!-- crowd_index:animal-kingdom:{YYYY-MM-DD} -->

<!-- Redis value shape: JSON.stringify({ value: number|null, confidence: 'bootstrap'|'percentile', sample_size_days: number }) -->
<!-- TTL: 2 hours (7200 seconds) — hourly job will refresh before expiry -->

<!-- Bootstrap detection query: -->
<!-- SELECT COUNT(DISTINCT DATE(ts)) AS day_count FROM wait_times_history -->
<!-- if day_count >= 30 → percentile mode; else → bootstrap mode -->

<!-- Pattern 7 (RESEARCH.md) percentile SQL: -->
<!-- Uses WITH top_rides AS (...), current_avg AS (...), historical_dist AS (...) -->
<!-- Returns: avg_wait, p0, p50, p95 -->
<!-- Map current avg_wait to 0–100 via linear interpolation: -->
<!--   if avg_wait ≤ p0 → 0 -->
<!--   if avg_wait ≥ p95 → 95 (clamp at 95, not 100) -->
<!--   else → linear interpolation between anchor points -->

<!-- Per-park calculation: run same query but filtered to park's top 5 rides -->
<!-- Global calculation: union of top 5 from each park (20 rides total) -->

<!-- Parks slugs for Redis keys: -->
<!-- parks.external_id values from catalog: 'magic-kingdom', 'epcot', 'hollywood-studios', 'animal-kingdom' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CrowdIndexService — bootstrap/percentile formulas + Redis write</name>
  <files>
    apps/api/src/crowd-index/crowd-index.service.ts,
    apps/api/src/crowd-index/crowd-index.service.spec.ts
  </files>
  <behavior>
    - getSampleSizeDays(): number — query COUNT(DISTINCT DATE(ts)) from wait_times_history
    - computeBootstrap(avgWait): number — return Math.min(100, avgWait * 1.2)
    - computePercentile(avgWait, p0, p50, p95): number — linear interpolation between anchor points
    - computeForPark(parkSlug): Promise<{ value, confidence, sample_size_days }> — run park-scoped query; pick formula based on sampleSizeDays
    - computeGlobal(): same but union of all 4 parks' top 5 rides
    - writeToRedis(date, global, perPark): write all 5 keys with 7200s TTL
    - Test DATA-04a: computeBootstrap(50) = min(100, 60) = 60; computeBootstrap(90) = 100
    - Test DATA-04b: computePercentile(avg, p0=10, p50=35, p95=70) — given known inputs, returns value in [0,100]
    - Test DATA-04c: writeToRedis sets 5 keys (global + 4 parks) — mock redis.set and verify 5 calls
    - Test DATA-04d: each Redis value has { value, confidence, sample_size_days } shape
  </behavior>
  <action>
    Create apps/api/src/crowd-index/crowd-index.service.ts:
    - @Injectable() class CrowdIndexService
    - Inject: db, ioredis redis client
    - PARK_SLUGS = ['magic-kingdom', 'epcot', 'hollywood-studios', 'animal-kingdom'] — must match parks.external_id
    - async getSampleSizeDays(): Promise<number> — raw SQL query
    - computeBootstrap(avgWait: number): number — pure function, min(100, avgWait * 1.2)
    - computePercentileIndex(avgWait: number, p0: number, p50: number, p95: number): number — linear interpolation:
      - if avgWait <= p0: return 0
      - if avgWait >= p95: return 95
      - if avgWait <= p50: return ((avgWait - p0) / (p50 - p0)) * 50
      - else: return 50 + ((avgWait - p50) / (p95 - p50)) * 45
    - async getTopRidesForPark(parkSlug: string): Promise<string[]> — return top 5 ride UUIDs by 90-day avg wait
    - async computeIndexForRides(rideUuids: string[]): Promise<{ avgWait: number, p0: number, p50: number, p95: number } | null>
      — run Pattern 7 SQL filtered to given ride UUIDs
    - async refreshAll(date: string): Promise<void>
      1. sampleSizeDays = await getSampleSizeDays()
      2. For each park: compute value; use bootstrap or percentile based on sampleSizeDays >= 30
      3. Global: union of all parks' top 5 rides
      4. Write all 5 Redis keys via writeToRedis
    - writeToRedis(date: string, global: CrowdIndexValue, parks: Record<string, CrowdIndexValue>): Promise<void>
      — set crowd_index:${date} and crowd_index:${slug}:${date} with EX 7200

    Create crowd-index.service.spec.ts: test pure functions with direct invocation; mock db and redis for integration-level tests. Cover all four DATA-04 behaviors.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- --run crowd-index.service</automated>
  </verify>
  <done>DATA-04a/04b/04c/04d tests all green; bootstrap and percentile formulas produce correct values for known inputs</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CrowdIndexProcessor + CrowdIndexModule</name>
  <files>
    apps/api/src/crowd-index/crowd-index.processor.ts,
    apps/api/src/crowd-index/crowd-index.processor.spec.ts,
    apps/api/src/crowd-index/crowd-index.module.ts,
    apps/api/src/worker.module.ts
  </files>
  <action>
    Create apps/api/src/crowd-index/crowd-index.processor.ts:
    - @Processor('crowd-index', { concurrency: 1 }) class CrowdIndexProcessor extends WorkerHost
    - Inject: CrowdIndexService, SlackAlerterService, @InjectQueue('crowd-index') crowdIndexQueue
    - async onModuleInit(): upsertJobScheduler('crowd-index-scheduler', { pattern: '0 * * * *' }, { name: 'refresh_crowd_index', data: {}, opts: { attempts: 3, backoff: { type: 'fixed', delay: 30_000 } } })
      Runs at :00 of each hour (pg_cron also runs at :00 but for a different table — no conflict)
    - async process(job: Job): Promise<void>
      const today = new Date().toISOString().split('T')[0];
      await this.crowdIndexService.refreshAll(today);
    - Dead-letter handler (same pattern as other processors)

    Create crowd-index.processor.spec.ts: verify crowdIndexService.refreshAll is called with today's date.

    Create apps/api/src/crowd-index/crowd-index.module.ts:
    ```typescript
    @Module({
      imports: [BullModule.registerQueue({ name: 'crowd-index' }), AlertingModule],
      providers: [CrowdIndexService, CrowdIndexProcessor],
      exports: [CrowdIndexService],
    })
    export class CrowdIndexModule {}
    ```

    Update apps/api/src/worker.module.ts: add CrowdIndexModule to imports.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- --run "crowd-index"</automated>
  </verify>
  <done>CrowdIndexProcessor compiles; module wired into WorkerModule; processor test confirms refreshAll is called</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm --filter @wonderwaltz/api test -- --run crowd-index` — all DATA-04 tests green
2. `pnpm --filter @wonderwaltz/api tsc --noEmit` — no TypeScript errors
3. Confirm computeBootstrap(90) = 100 (clamped by min(100, ...))
4. Confirm all 5 Redis keys are written in a single refreshAll() call
5. Confirm confidence field is 'bootstrap' when sampleSizeDays < 30, 'percentile' when >= 30
</verification>

<success_criteria>
- DATA-04a: bootstrap formula min(100, avg × 1.2) verified for known inputs
- DATA-04b: percentile formula returns value in [0,100] for known distribution
- DATA-04c: 5 Redis keys set after each run (global + 4 parks)
- DATA-04d: every Redis value has { value, confidence, sample_size_days } shape
- Auto-switch: service picks formula based on day_count, not a manual flag
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-07-SUMMARY.md`
</output>
