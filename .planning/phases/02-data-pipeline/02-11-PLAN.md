---
phase: 02-data-pipeline
plan: "11"
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - packages/content/legal/attribution.en.json
  - packages/content/src/attribution.spec.ts
  - apps/web/src/components/Footer.tsx
autonomous: true
requirements:
  - DATA-05
must_haves:
  truths:
    - "packages/content/legal/attribution.en.json exists with text: 'Data source: queue-times.com'"
    - "apps/web footer imports the attribution text from packages/content and renders it as plain text"
    - "Attribution text is identical on all platforms (web footer, iOS About screen, Android About screen)"
    - "The content file is the single source of truth — no platform hardcodes the string"
  artifacts:
    - path: "packages/content/legal/attribution.en.json"
      provides: "Single-source attribution text for all platforms"
      contains: "Data source: queue-times.com"
    - path: "packages/content/src/attribution.spec.ts"
      provides: "DATA-05 automated test: file exists + correct text"
    - path: "apps/web/src/components/Footer.tsx"
      provides: "Web footer with attribution text imported from packages/content"
  key_links:
    - from: "apps/web/src/components/Footer.tsx"
      to: "packages/content/legal/attribution.en.json"
      via: "import attribution from '@wonderwaltz/content/legal/attribution.en.json'"
      pattern: "attribution\\.en\\.json"
---

<objective>
Create the attribution content file (DATA-05) and wire it into the web footer. iOS and Android About screen wiring is deferred to Phase 5/7 per CONTEXT.md — only the content file and web import are in Phase 2 scope.

Purpose: queue-times.com provides free data access with attribution required. "Data source: queue-times.com" must appear on all client surfaces. Centralizing the string in packages/content prevents divergence.

Output: attribution.en.json file, automated content test, web footer component with imported attribution.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@packages/content/legal/disclaimer.en.json
@apps/web/src/app/layout.tsx

<interfaces>
<!-- Attribution decisions from CONTEXT.md: -->
<!-- Exact text: "Data source: queue-times.com" (no marketing language, no hyperlink) -->
<!-- Placement: UI surfaces only — web footer + app About screen -->
<!-- Content source: packages/content/legal/attribution.en.json -->
<!-- Pattern: mirrors disclaimer.en.json exactly -->

<!-- From RESEARCH.md: -->
<!-- attribution.en.json: { "text": "Data source: queue-times.com" } -->
<!-- No shortText needed — single platform-agnostic string -->

<!-- Deferred: iOS/Android About screen wiring → Phase 5/7 -->
<!-- Web footer is in scope for Phase 2 -->

<!-- Footer implementation: plain text, not a hyperlink (per CONTEXT.md decision) -->
<!-- Place attribution text in web app footer -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Attribution content file + test + web footer</name>
  <files>
    packages/content/legal/attribution.en.json,
    packages/content/src/attribution.spec.ts,
    apps/web/src/components/Footer.tsx
  </files>
  <behavior>
    - attribution.en.json exists at packages/content/legal/attribution.en.json
    - attribution.en.json contains { "text": "Data source: queue-times.com" }
    - attribution.spec.ts: readFileSync the JSON; verify text field equals exact string
    - Footer.tsx: renders the attribution text as a <p> or <span> element
    - Test DATA-05: file exists + text matches exactly
  </behavior>
  <action>
    Create packages/content/legal/attribution.en.json:
    ```json
    {
      "text": "Data source: queue-times.com"
    }
    ```
    This mirrors disclaimer.en.json. No other fields needed.

    Create packages/content/src/attribution.spec.ts:
    - import { readFileSync } from 'fs'; import { join } from 'path';
    - Read packages/content/legal/attribution.en.json
    - Assert: JSON.parse(content).text === 'Data source: queue-times.com'
    - Assert: file exists (readFileSync won't throw)
    This is the DATA-05 automated signal.

    Create apps/web/src/components/Footer.tsx:
    - Import attribution from the JSON file or from a re-exported constant in packages/content/src/index.ts
    - Render a footer element containing the disclaimer text AND attribution text
    - Structure:
      ```tsx
      import attributionData from '../../../../packages/content/legal/attribution.en.json';

      export function Footer() {
        return (
          <footer className="...">
            <p className="text-sm text-muted">
              WonderWaltz is an unofficial app. Not affiliated with The Walt Disney Company.
            </p>
            <p className="text-sm text-muted">{attributionData.text}</p>
          </footer>
        );
      }
      ```
    - Use the attribution text as plain text — NO <a> tag (deferred per CONTEXT.md)
    - Import the attribution JSON directly (Next.js supports JSON imports natively)

    If a Footer component already exists in apps/web, update it to add the attribution line. Do not create a duplicate footer.

    If packages/content/src/index.ts doesn't export attribution yet, add:
    ```typescript
    export { default as attribution } from '../legal/attribution.en.json';
    ```
    But check if the ESM/CJS import boundary issue (noted in STATE.md for disclaimer) applies here too. If it does, use the direct JSON file path import in the web app.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/content test -- --run attribution</automated>
  </verify>
  <done>attribution.en.json created with correct text; attribution.spec.ts passes green; Footer.tsx renders attribution text</done>
</task>

</tasks>

<verification>
After task:
1. `pnpm --filter @wonderwaltz/content test -- --run attribution` — DATA-05 test green
2. `cat packages/content/legal/attribution.en.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['text']=='Data source: queue-times.com'"` — passes
3. `pnpm --filter @wonderwaltz/web tsc --noEmit` — Footer.tsx compiles
</verification>

<success_criteria>
- DATA-05 automated test passes: file exists with exact text "Data source: queue-times.com"
- Web footer imports from packages/content (not hardcoded)
- Attribution is plain text (no hyperlink) per CONTEXT.md decision
- iOS/Android wiring explicitly marked as deferred to Phase 5/7
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-11-SUMMARY.md`
</output>
