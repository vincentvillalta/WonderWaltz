---
phase: 02-data-pipeline
plan: "08"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/api/src/weather/weather.module.ts
  - apps/api/src/weather/weather.service.ts
  - apps/api/src/weather/weather.service.spec.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements:
  - DATA-08
must_haves:
  truths:
    - "GET /v1/weather?date=YYYY-MM-DD returns cached weather or fetches from OpenWeather and caches"
    - "Cache key weather:orlando:{YYYY-MM-DD} has 6-hour TTL"
    - "Dates beyond 8 days from today return null (weather field omitted)"
    - "OpenWeather API failure returns null silently — no exception thrown to caller"
    - "A single OneCall API request caches all 8 days' forecasts simultaneously"
  artifacts:
    - path: "apps/api/src/weather/weather.service.ts"
      provides: "Cache-aside weather lookup; OpenWeather One Call 3.0; 8-day horizon cap"
      exports: ["WeatherService"]
    - path: "apps/api/src/weather/weather.service.spec.ts"
      provides: "DATA-08a/08b/08c/08d tests: horizon skip, cache hit, cache miss, API failure"
  key_links:
    - from: "apps/api/src/weather/weather.service.ts"
      to: "Redis key weather:orlando:{YYYY-MM-DD}"
      via: "redis.get/set with EX 21600"
      pattern: "weather:orlando:"
    - from: "apps/api/src/weather/weather.service.ts"
      to: "https://api.openweathermap.org/data/3.0/onecall"
      via: "Node native fetch with appid query param"
      pattern: "openweathermap.org"
---

<objective>
Build the on-demand weather cache-aside module (DATA-08). No worker — weather is fetched on-demand when the API endpoint is called, cached per date with 6-hour TTL, and returned null beyond the 8-day horizon.

Purpose: Trip date weather forecasts feed the Phase 3 DayPlan output. Building the service in Wave 2 ensures it's ready when the API endpoint (plan 02-09) wires it in.

Output: WeatherService with full unit test coverage for all four DATA-08 behaviors. Registered in AppModule for HTTP endpoints to inject.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@apps/api/src/app.module.ts
@apps/api/tests/fixtures/openweather-response.json

<interfaces>
<!-- OpenWeather One Call 3.0 (RESEARCH.md): -->
<!-- GET https://api.openweathermap.org/data/3.0/onecall?lat=28.5421&lon=-81.3723&appid={KEY}&units=imperial&exclude=minutely,hourly,alerts -->
<!-- Orlando coords: lat=28.5421, lon=-81.3723 -->
<!-- Use &units=imperial to get Fahrenheit directly (avoids Kelvin conversion) -->
<!-- Response: { daily: [8 items, index 0=today, index 7=7 days out] } -->

<!-- CRITICAL HORIZON: 8 days, NOT 16. CONTEXT.md says "skip beyond 16-day horizon" but -->
<!-- One Call 3.0 only provides 8 days. Cap at 8 days from today. -->

<!-- Mapping (with units=imperial): -->
<!-- day.temp.max → high_f (already Fahrenheit) -->
<!-- day.temp.min → low_f -->
<!-- day.weather[0].main → condition (pass through: "Rain", "Clouds", "Clear", etc.) -->
<!-- Math.round(day.pop * 100) → precipitation_pct -->
<!-- day.humidity → humidity_pct -->
<!-- Math.round(day.uvi) → uv_index -->

<!-- Cache key: weather:orlando:{YYYY-MM-DD} -->
<!-- Cache TTL: 6 * 60 * 60 = 21600 seconds -->

<!-- Optimization: on cache miss, fetch full 8-day forecast and cache ALL 8 days -->
<!-- This gives 8 cached keys per API call (1 API call serves 8 potential date queries) -->

<!-- Beyond 8 days: return null (omit weather field from response) -->

<!-- Failure handling: catch all errors, return null, no retry, no circuit breaker -->

<!-- Client payload shape (from CONTEXT.md): -->
<!-- { high_f: number, low_f: number, condition: string, precipitation_pct: number, humidity_pct: number, uv_index: number } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WeatherService — cache-aside + OpenWeather + horizon cap + WeatherModule</name>
  <files>
    apps/api/src/weather/weather.service.ts,
    apps/api/src/weather/weather.service.spec.ts,
    apps/api/src/weather/weather.module.ts,
    apps/api/src/app.module.ts
  </files>
  <behavior>
    - getForecast(dateStr: string): Promise<WeatherDto | null>
      1. Check if date is within 8 days from today; if not, return null immediately
      2. Check Redis cache hit → return parsed cached value
      3. On cache miss: fetch One Call API; cache all 8 days; return the requested date's entry
      4. On any error (network, parse, Redis): return null (best-effort)
    - Test DATA-08a: date = today + 9 days → returns null without any fetch call
    - Test DATA-08b: cache hit → returns cached value; fetch is NOT called
    - Test DATA-08c: cache miss → fetch called; redis.set called with EX 21600 for each of 8 days; returns correct WeatherDto
    - Test DATA-08d: fetch throws → returns null; no exception propagated
  </behavior>
  <action>
    Create apps/api/src/weather/weather.service.ts:
    - @Injectable() class WeatherService
    - Inject: ConfigService (for OPENWEATHER_API_KEY), ioredis redis client
    - ORLANDO_LAT = 28.5421, ORLANDO_LON = -81.3723
    - isWithinHorizon(dateStr: string): boolean
      const today = new Date(); today.setHours(0,0,0,0);
      const target = new Date(dateStr);
      const diffDays = (target.getTime() - today.getTime()) / 86_400_000;
      return diffDays >= 0 && diffDays <= 7; // 0 = today, 7 = 7 days out (8 days total)
    - async getForecast(dateStr: string): Promise<WeatherDto | null>
      1. if (!this.isWithinHorizon(dateStr)) return null
      2. const cached = await this.redis.get(`weather:orlando:${dateStr}`)
         if (cached) return JSON.parse(cached) as WeatherDto
      3. try { return await this.fetchAndCacheAll(dateStr) } catch { return null }
    - private async fetchAndCacheAll(requestedDate: string): Promise<WeatherDto | null>
      1. Fetch One Call API with units=imperial, exclude=minutely,hourly,alerts
      2. For each item in response.daily (max 8):
         - Compute date from dt: new Date(day.dt * 1000).toISOString().split('T')[0]
         - Map to WeatherDto: { high_f: day.temp.max, low_f: day.temp.min, condition: day.weather[0].main, precipitation_pct: Math.round(day.pop * 100), humidity_pct: day.humidity, uv_index: Math.round(day.uvi) }
         - await redis.set(`weather:orlando:${date}`, JSON.stringify(dto), 'EX', 21600)
      3. Return the entry that matches requestedDate; null if not found in response

    Create weather.service.spec.ts: Vitest tests with vi.fn() for fetch (globalThis.fetch) and redis. Import openweather-response.json fixture. Test all four DATA-08 behaviors including the 8-cache-keys-per-call optimization.

    Create apps/api/src/weather/weather.module.ts:
    ```typescript
    @Module({ providers: [WeatherService], exports: [WeatherService] })
    export class WeatherModule {}
    ```

    Update apps/api/src/app.module.ts: add WeatherModule to imports array. The HTTP endpoints (plan 02-09) will inject WeatherService from this module.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- --run weather.service</automated>
  </verify>
  <done>DATA-08a/08b/08c/08d tests all green; cache miss fetches API and caches all 8 days; no exceptions thrown from getForecast; WeatherModule wired into AppModule</done>
</task>

</tasks>

<verification>
After task:
1. `pnpm --filter @wonderwaltz/api test -- --run weather` — all DATA-08 tests pass
2. `pnpm --filter @wonderwaltz/api tsc --noEmit` — no TypeScript errors
3. Verify getForecast never throws — all paths return T | null
4. Verify redis.set is called 8 times per API call (cache all days optimization)
5. Confirm units=imperial in the API URL — no Kelvin conversion needed
</verification>

<success_criteria>
- DATA-08a: date > 8 days → null, no API call
- DATA-08b: cache hit → cached value returned, no API call
- DATA-08c: cache miss → OpenWeather called, all 8 days cached, correct WeatherDto returned
- DATA-08d: API failure → null returned without throwing
- WeatherModule exported and registered in AppModule
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-08-SUMMARY.md`
</output>
