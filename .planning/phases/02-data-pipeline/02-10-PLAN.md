---
phase: 02-data-pipeline
plan: "10"
type: execute
wave: 3
depends_on:
  - "02-03"
  - "02-09"
files_modified:
  - packages/shared-openapi/openapi.v1.snapshot.json
  - scripts/generate-openapi-snapshot.ts
autonomous: true
requirements:
  - DATA-06
must_haves:
  truths:
    - "The committed openapi.v1.snapshot.json reflects the final live endpoint implementations from plans 02-09"
    - "Phase 3/4 stubs are documented as 501 Not Implemented with full response schemas in the snapshot"
    - "CI snapshot diff gate is clean — no drift between generated spec and committed snapshot"
    - "packages/shared-openapi/openapi.v1.snapshot.json is the frozen v1 spec that mobile clients generate against"
  artifacts:
    - path: "packages/shared-openapi/openapi.v1.snapshot.json"
      provides: "Final committed OpenAPI v1 snapshot — frozen spec for mobile client generation"
      contains: "WonderwWaltz API v1 with all routes documented"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "packages/shared-openapi/openapi.v1.snapshot.json"
      via: "generate-openapi-snapshot.ts && git diff --exit-code"
      pattern: "git diff.*openapi.v1.snapshot"
---

<objective>
Regenerate and commit the final OpenAPI v1 snapshot after all live endpoint implementations are complete. This is the spec freeze that completes Phase 2's OpenAPI commitment.

Purpose: Plan 02-03 committed a snapshot of stub controllers. Plans 02-04 through 02-09 implemented real services and may have refined DTOs. This plan runs the snapshot script one final time, commits the result, and confirms CI gate passes.

Output: Final committed openapi.v1.snapshot.json. This is a small but critical plan — the spec freeze is a Phase 2 hard requirement.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@scripts/generate-openapi-snapshot.ts
@packages/shared-openapi/openapi.v1.snapshot.json

<interfaces>
<!-- Snapshot generation (from RESEARCH.md Pattern 5): -->
<!-- node --loader ts-node/esm scripts/generate-openapi-snapshot.ts -->
<!-- writes to packages/shared-openapi/openapi.v1.snapshot.json -->
<!-- CI gate: git diff --exit-code packages/shared-openapi/openapi.v1.snapshot.json -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Final snapshot regeneration and freeze commit</name>
  <files>
    packages/shared-openapi/openapi.v1.snapshot.json
  </files>
  <action>
    Run the snapshot generation script against the completed NestJS app:
    ```bash
    cd /Users/vincentvillalta/Documents/Projects/WonderWaltz
    node --loader ts-node/esm apps/api/scripts/generate-openapi-snapshot.ts
    ```
    (Or whatever the correct invocation is — check scripts/ path and tsconfig)

    Review the generated openapi.v1.snapshot.json:
    1. Confirm all live routes are present: GET /v1/parks, GET /v1/parks/{parkId}/waits, GET /v1/crowd-index, GET /v1/weather
    2. Confirm Phase 3 stubs present: POST /v1/trips, GET /v1/trips/{id}, POST /v1/trips/{id}/generate-plan, GET /v1/plans/{id}, POST /v1/trips/{id}/rethink-today
    3. Confirm Phase 4 stubs: POST /v1/auth/anonymous, GET /v1/users/me
    4. Confirm response schemas reflect envelope wrapper: { data: T, meta: { disclaimer: string } }
    5. Confirm GuestInputDto uses age_bracket enum (not birthdate)
    6. Confirm all 501 routes have documented response body shapes (not just empty 501)

    If any routes are missing or misshapen, fix the relevant controller/DTO and regenerate.

    Commit the final snapshot:
    ```bash
    git add packages/shared-openapi/openapi.v1.snapshot.json
    git commit -m "chore(02-data-pipeline): freeze OpenAPI v1 snapshot"
    ```

    Run CI gate check locally to confirm no drift:
    ```bash
    node --loader ts-node/esm apps/api/scripts/generate-openapi-snapshot.ts
    git diff --exit-code packages/shared-openapi/openapi.v1.snapshot.json
    ```
    This must exit 0 — meaning the committed snapshot matches what the script generates.
  </action>
  <verify>
    <automated>node --loader ts-node/esm apps/api/scripts/generate-openapi-snapshot.ts && git diff --exit-code packages/shared-openapi/openapi.v1.snapshot.json</automated>
  </verify>
  <done>Snapshot committed; CI gate exits 0 (no drift); all v1 routes present in JSON; Phase 3/4 stubs have documented schemas</done>
</task>

</tasks>

<verification>
After task:
1. `git diff --exit-code packages/shared-openapi/openapi.v1.snapshot.json` — exits 0
2. `cat packages/shared-openapi/openapi.v1.snapshot.json | python3 -m json.tool > /dev/null` — valid JSON
3. Check route count: should have at least 11 routes (4 live + 5 Phase 3 stubs + 2 Phase 4 stubs)
4. Confirm swagger-annotated envelope in response schemas
</verification>

<success_criteria>
- openapi.v1.snapshot.json committed with all v1 routes
- CI gate `git diff --exit-code` passes
- All Phase 3/4 stubs have schemas for Trip, Plan, DayPlan, PlanItem, User, auth
- Spec freeze is complete — this file becomes the mobile client generation source
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-10-SUMMARY.md`
</output>
