---
phase: 05-ios-core
plan: 10
type: execute
wave: 2
depends_on: ["05-08"]
files_modified:
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/WizardContainerView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/WizardViewModel.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/DatesPartyStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/ResortTicketsStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/DiningStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/PacePrioritiesStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/DatesStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/ParksStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/GuestsStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/BudgetStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/LodgingStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/MustDoRidesStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/MealPrefsStepView.swift
  - apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/ReviewStepView.swift
  - apps/ios/WonderWaltzTests/WizardTests.swift
autonomous: true
gap_closure: true
requirements:
  - IOS-06
  - IOS-17

must_haves:
  truths:
    - "Wizard has exactly 4 steps: Dates & Party, Resort & Tickets, Dining ADRs, Pace & Priorities"
    - "Back button + 'Step X of 4' header on each step"
    - "Gold progress bar (#E8B547) fills proportionally across 4 steps"
    - "Navy gradient icon (64pt, rounded-2xl) with gold SF Symbol on each step"
    - "Card-based form fields with rounded-xl bg-card border"
    - "Park selector shows 2x2 grid with emoji + park-color highlight when selected"
    - "Radio groups have gold border-2 on selected option"
    - "Pace slider with emoji labels (Chill/Balanced/Commando)"
    - "Must-do attractions grouped by park with park-colored headers"
  artifacts:
    - path: "apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/DatesPartyStepView.swift"
      provides: "Step 1: dates + party members combined"
    - path: "apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/ResortTicketsStepView.swift"
      provides: "Step 2: accommodation + resort + park selection + hopper"
    - path: "apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/DiningStepView.swift"
      provides: "Step 3: dining suggestions toggle + budget + character dining + reservations"
    - path: "apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/PacePrioritiesStepView.swift"
      provides: "Step 4: pace slider + must-do attractions by park"
  key_links:
    - from: "WizardViewModel"
      to: "WizardDraftStoreProtocol"
      via: "auto-save on step transition"
      pattern: "saveDraft|loadDraft"
    - from: "WizardContainerView"
      to: "WWButton"
      via: "navy CTA button at bottom"
      pattern: "WWButton.*primary"
---

<objective>
Restructure the trip wizard from 8 separate steps to 4 combined steps matching React TripSetup.tsx. Current wizard has: dates, parks, guests, budget, lodging, must-do rides, meals, review. New wizard combines these into: (1) Dates & Party, (2) Resort & Tickets, (3) Dining ADRs, (4) Pace & Priorities. All visual patterns must match the React reference.

Purpose: The 4-step wizard is faster, friendlier, and matches the canonical design. "Like a Disney expert walking you through it."
Output: Restructured wizard with 4 combined steps, card-based inputs, park color coding.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ios-core/05-CONTEXT.md
@.planning/phases/05-ios-core/05-04-SUMMARY.md

<interfaces>
<!-- WizardViewModel contract (will be updated) -->
From WizardViewModel.swift:
```swift
@Observable @MainActor
public final class WizardViewModel {
    public var startDate: Date
    public var endDate: Date
    public var selectedParks: Set<String>
    public var parkHopper: Bool
    public var guests: [GuestInput]
    public var budgetTier: BudgetTier
    public var lodgingType: LodgingType
    // ... more state
    public func advance() { ... }
    public func goBack() { ... }
    public func submitTrip() async { ... }
}
```

<!-- WizardDraftStoreProtocol -->
```swift
public protocol WizardDraftStoreProtocol: Sendable {
    func saveDraft(_ draft: WizardDraftData) async throws
    func loadDraft() async throws -> WizardDraftData?
    func clearDraft() async throws
}
```

<!-- From Plan 08 -->
WWButton: .primary (navy + shimmer), .secondary, .compact
ParkColor enum: .magicKingdom, .epcot, .hollywoodStudios, .animalKingdom
WWTheme.border: navy at 10% opacity
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite WizardContainerView + WizardViewModel for 4 steps</name>
  <files>
    apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/WizardContainerView.swift,
    apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/WizardViewModel.swift
  </files>
  <action>
    Read the React reference file at /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/TripSetup.tsx

    1. UPDATE `WizardViewModel.swift`:
       - Change `WizardStep` enum from 8 steps to 4:
         ```swift
         public enum WizardStep: Int, CaseIterable {
             case datesParty = 0      // "Dates & Party" (Calendar icon)
             case resortTickets = 1   // "Resort & Tickets" (Users icon)
             case dining = 2          // "Dining ADRs" (UtensilsCrossed icon)
             case pacePriorities = 3  // "Pace & Priorities" (Gauge icon)

             public var title: String { ... }
             public var subtitle: String { ... }  // Per TripSetup.tsx lines 88-91
             public var systemImage: String { ... }
         }
         ```
       - Keep ALL existing state properties (startDate, endDate, selectedParks, parkHopper, guests, budgetTier, lodgingType, mustDoRides, mealPrefs, etc.). These properties are just REGROUPED into 4 steps, not removed.
       - `stepCount` returns 4 (not 8).
       - `progress` = `(currentStep.rawValue + 1) / 4.0`
       - Keep auto-save on step transition via WizardDraftStoreProtocol.
       - Keep draft restoration from WizardDraftStoreProtocol.
       - Add new state for Step 3 (Dining): `diningBudget: DiningBudget` (budget/moderate/premium), `characterDining: CharacterDiningPref` (must/nice/skip), `existingReservations: [DiningReservation]`
       - Add new state for Step 4: `paceValue: Double` (0-100, default 60)

    2. REWRITE `WizardContainerView.swift` matching TripSetup.tsx layout:
       - Header (matching TripSetup.tsx lines 38-67):
         a. Back button (left): `chevron.left` in 40pt circle with muted bg. Only visible when `currentStep > 0`. On tap calls `viewModel.goBack()`.
         b. Step indicator (center): "Step X of 4" in muted-foreground, `WWTypography.subheadline`.
         c. Spacer (right): 40pt width for balance.
       - Progress bar: 4pt tall (`h-1`), muted bg, gold fill animating to `progress` width. Use `WWTheme.muted` for track, `WWTheme.accent` for fill.
       - Content: Scrollable area with step view switching. Animate transitions with opacity + horizontal offset (x: 20 on enter, matching TripSetup.tsx `initial={{ opacity: 0, x: 20 }}`). Gate animation on reduce-motion.
       - Each step has the navy gradient icon + Fraunces title (matching lines 80-93):
         a. Icon container: 64pt (w-16), rounded-2xl, `LinearGradient` from navy (#1B2A4E) to lighter navy (#2A3A5E). Icon in gold at 32pt.
         b. Title: `WWTypography.title` (Fraunces 28pt). text-3xl equivalent.
         c. Subtitle: `WWTypography.body`, muted-foreground.
       - Bottom CTA (matching lines 571-583): `WWButton` with `.primary` style. Label "Continue" for steps 0-2, "Generate Itinerary" for step 3. Full-width with `border-t` separator.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>WizardContainerView renders 4-step flow with back button, step indicator, gold progress bar, navy icon headers, navy CTA. WizardViewModel manages 4 steps with all state preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 combined step views + delete old 8 step files + update tests</name>
  <files>
    apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/DatesPartyStepView.swift,
    apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/ResortTicketsStepView.swift,
    apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/DiningStepView.swift,
    apps/ios/Packages/WWTripWizard/Sources/WWTripWizard/Steps/PacePrioritiesStepView.swift,
    apps/ios/WonderWaltzTests/WizardTests.swift
  </files>
  <action>
    Read the React reference file at /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/TripSetup.tsx

    **DELETE old step files** (they are being replaced):
    - DatesStepView.swift, ParksStepView.swift, GuestsStepView.swift, BudgetStepView.swift
    - LodgingStepView.swift, MustDoRidesStepView.swift, MealPrefsStepView.swift, ReviewStepView.swift

    **CREATE 4 new step views:**

    1. `DatesPartyStepView.swift` (matching TripSetup.tsx `currentStep === 0`, lines 96-249):
       - Trip dates: two date pickers in a 2-column grid. Labels "Check-in" and "Check-out" in caption muted text above each. Card-based: `rounded-xl bg-card border border-border` (white bg, navy 10% border).
       - Party members: scrollable list of member cards. Each card is `p-3 rounded-xl bg-card border border-border`:
         - Adult: avatar circle (32pt, navy/10% bg, person emoji), name text field, dietary dropdown.
         - Child: avatar circle (32pt, park-color tinted bg -- MK pink for first child, EPCOT teal for second), name field, age + height fields in 3-column grid, dietary dropdown.
         - Each text field: `h-9 px-3 text-sm rounded-lg bg-background border border-border`.
       - "Add Family Member" button: dashed border, full width, `h-10 rounded-xl border-2 border-dashed`.
       - Bind to `viewModel.guests` array. Include DAS flag toggle, mobility, sensory per guest (combine guest details into this view).

    2. `ResortTicketsStepView.swift` (matching TripSetup.tsx `currentStep === 1`, lines 252-342):
       - Accommodation type: 3 radio options (Disney Resort, Off-Property, Not Sure Yet). Each in card: `p-3 rounded-xl bg-card border-2`. **Selected option gets gold border-2** (`border-[#E8B547]`), unselected gets default border. Radio input 20pt.
       - Resort selector (conditional on Disney Resort): card container with dropdown.
       - Park selector: 2x2 grid. Each park is a `h-24 rounded-xl bg-card border-2`. Uses `ParkColor` enum for emoji + name. **Selected park**: border in park color + light park-color bg at 8% (`parkColor.opacity(0.08)`). Unselected: default border.
       - Park Hopper toggle: card with checkbox, "Park Hopper" title, "Visit multiple parks per day" subtitle.
       - Bind to `viewModel.selectedParks`, `viewModel.parkHopper`, `viewModel.lodgingType`.

    3. `DiningStepView.swift` (matching TripSetup.tsx `currentStep === 2`, lines 345-434):
       - Dining suggestions toggle: gold-tinted card (`from-[#E8B547]/10`). Toggle switch. Title "Get Dining Suggestions".
       - Dining budget: 3-column grid of budget buttons ($, $$, $$$). Selected: gold border-2 + gold/5% bg. Cards `h-20 rounded-xl`.
       - Character dining: 3 radio options (Must have, Nice to have, Not interested). Same radio card pattern as accommodation. Selected: gold border for "must", park-color for others.
       - Existing reservations: list of reservation cards + "Add Reservation" dashed button.
       - Bind to `viewModel.diningBudget`, `viewModel.characterDining`, `viewModel.existingReservations`.

    4. `PacePrioritiesStepView.swift` (matching TripSetup.tsx `currentStep === 3`, lines 437-564):
       - Pace slider: `Slider(value: $viewModel.paceValue, in: 0...100)` styled with gold thumb. Inside a card. 3 labels below: "Chill" (relax emoji), "Balanced" (lightning emoji), "Commando" (rocket emoji), each with subtitle text.
       - Must-do attractions: grouped by park. Each park section has:
         - Header: park emoji in 24pt rounded-lg tinted container + park name in park color, `WWTypography.subheadline` weight medium.
         - Attraction list: each row is `h-10 px-3 rounded-lg bg-card border border-border` with checkbox (navy accent) + attraction name in `WWTypography.subheadline`.
         - Use the same attraction lists from TripSetup.tsx (Seven Dwarfs Mine Train, Space Mountain, etc for MK; Guardians, Test Track, etc for EPCOT; Rise of the Resistance, etc for HS; Flight of Passage, etc for AK).
       - Bind to `viewModel.mustDoRides`, `viewModel.paceValue`.

    **UPDATE `WizardTests.swift`:**
    - Change step count assertions from 8 to 4.
    - Update step navigation tests for 4 steps (advance from 0->1->2->3, cannot go past 3).
    - Keep auto-save test: verify `saveDraft` called on each step transition.
    - Keep draft restoration test.
    - Update progress calculation test: step 0 = 25%, step 1 = 50%, step 2 = 75%, step 3 = 100%.
    - Remove any tests specific to old step views (ReviewStepView tap-to-edit, etc).

    **IMPORTANT**: Also remove the old step files from the Xcode project (update project.pbxproj) or from the Package.swift sources if they are explicitly listed. Since these are in an SPM package, just deleting the .swift files should be sufficient -- SPM auto-discovers sources.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild test -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing WonderWaltzTests/WizardTests -quiet 2>&1 | tail -10</automated>
  </verify>
  <done>4 combined step views created matching TripSetup.tsx. Old 8 step files deleted. WizardTests pass with 4-step assertions.</done>
</task>

</tasks>

<verification>
- `xcodebuild build` passes with no errors
- WizardTests all pass with 4-step flow
- Old step files no longer exist in the Steps/ directory
- Park selector shows 2x2 grid with park colors
- Pace slider has emoji labels
- Must-do attractions grouped by park with park-colored headers
</verification>

<success_criteria>
- Wizard shows exactly 4 steps: "Dates & Party", "Resort & Tickets", "Dining ADRs", "Pace & Priorities"
- Header: back button + "Step X of 4" + gold progress bar
- Each step: navy gradient icon (64pt) + Fraunces title + subtitle
- Card-based inputs with rounded-xl, white bg, navy 10% border
- Park selector: 2x2 grid, emoji, park-color highlight
- Radio groups: gold border on selected
- Pace slider: 0-100 with Chill/Balanced/Commando labels
- Must-do attractions: grouped by park with park-colored section headers
- Navy CTA: "Continue" on steps 1-3, "Generate Itinerary" on step 4
- Auto-save preserved on step transitions
- All WizardTests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-core/05-10-SUMMARY.md`
</output>
