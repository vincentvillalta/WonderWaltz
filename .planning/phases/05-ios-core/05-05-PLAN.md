---
phase: 05-ios-core
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanContainerView.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanViewModel.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTabPicker.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTimelineView.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemCard.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemDetailView.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/RethinkLoadingView.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/LockedDayOverlay.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/ForecastBanner.swift
  - apps/ios/WonderWaltzTests/PlanViewTests.swift
autonomous: true
requirements:
  - IOS-07

must_haves:
  truths:
    - "User sees day tabs to switch between plan days"
    - "Timeline shows chronological items with walking times between them"
    - "Item cards display correctly for all types: attraction, meal, show, LL reminder, rest, walk"
    - "Rethink-my-day shows inline loading with plan visible but dimmed"
    - "Locked days (Days 2+, free tier) show blurred cards with Unlock CTA"
    - "Forecast confidence banner appears at top of plan view"
    - "Notification permission is requested after user sees Day 1 plan for the first time"
    - "PlanViewTests pass in WonderWaltzTests target"
  artifacts:
    - path: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanContainerView.swift"
      provides: "Root plan view with day tabs"
      contains: "PlanContainerView"
    - path: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanViewModel.swift"
      provides: "Plan state management with notification permission trigger"
      contains: "@Observable"
    - path: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTimelineView.swift"
      provides: "Scrollable timeline of plan items"
      contains: "DayTimelineView"
    - path: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/LockedDayOverlay.swift"
      provides: "Blur overlay for locked days"
      contains: "LockedDayOverlay"
    - path: "apps/ios/WonderWaltzTests/PlanViewTests.swift"
      provides: "Unit tests for plan view model"
      contains: "PlanViewTests"
  key_links:
    - from: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanViewModel.swift"
      to: "apps/ios/Packages/WWCore/Sources/WWCore/Protocols/APIClientProtocol.swift"
      via: "GET /plans/:id and POST /trips/:id/rethink-today"
      pattern: "getPlan\\|rethinkToday"
    - from: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanViewModel.swift"
      to: "apps/ios/Packages/WWCore/Sources/WWCore/Protocols/OfflineStoreProtocol.swift"
      via: "read cached plan when offline"
      pattern: "getCachedPlan"
    - from: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanViewModel.swift"
      to: "UNUserNotificationCenter"
      via: "notification permission request after first plan load"
      pattern: "requestAuthorization"
---

<objective>
Build the WWPlanView package: plan container with day tabs, scrollable timeline with item cards, item detail view, rethink-my-day loading overlay, locked day blur overlay, forecast confidence banner, and notification permission request after first plan load. Includes PlanViewTests.

Purpose: The plan view is the primary output surface — where users see their personalized Disney day plan. It reads from API when online and from SwiftData cache when offline. Notification permission is requested here per CONTEXT.md locked decision.
Output: Complete plan viewing experience with all item types, rethink flow, entitlement-based locking, notification permission trigger, and PlanViewTests passing.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ios-core/05-CONTEXT.md
@.planning/phases/05-ios-core/05-RESEARCH.md
@.planning/phases/05-ios-core/05-01-SUMMARY.md
@.planning/phases/05-ios-core/05-02-SUMMARY.md

@docs/design/BRAND.md
@docs/design/COMPONENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlanContainerView + PlanViewModel (with notification permission) + DayTabPicker + ForecastBanner</name>
  <files>
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanContainerView.swift
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanViewModel.swift
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTabPicker.swift
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/ForecastBanner.swift
    apps/ios/WonderWaltzTests/PlanViewTests.swift
  </files>
  <action>
    1. Create `PlanViewModel.swift` — `@Observable @MainActor`:
       - `var plan: PlanData?` — loaded from API or cache
       - `var selectedDayIndex: Int = 0`
       - `var isLoading: Bool`
       - `var isRethinking: Bool`
       - `var error: String?`
       - `var isOffline: Bool` — from SyncCoordinator state
       - Computed: `var currentDay: PlanDayData?` — the selected day's data
       - Computed: `var days: [PlanDayData]` — all days from plan
       - `func loadPlan(planId: String)`: try API first (via APIClientProtocol), fall back to cache (via OfflineStoreProtocol) if offline. After API load, cache the result via OfflineStore. Trigger OfflinePackageDownloader if not yet cached. **After first successful plan load, call `requestNotificationPermissionIfNeeded()`.**
       - `func rethinkToday(completedItemIds: [String])`: call `POST /trips/:id/rethink-today` with current time + completed items. Set `isRethinking = true`. On success, animate items to new positions. On failure (offline), show gentle banner: "Connect to internet to rethink your day"
       - `func selectDay(index: Int)`: update selectedDayIndex. If day is locked, show locked overlay instead of timeline
       - Inject `APIClientProtocol`, `OfflineStoreProtocol`, `AnalyticsProtocol`
       - Track `plan_viewed` analytics event (without age data, per IOS-15)

       **Notification Permission (CONTEXT.md locked decision: "Notification permission requested after plan generation -- after the user sees their Day 1 plan for the first time"):**
       - `private func requestNotificationPermissionIfNeeded()`:
         - Check UserDefaults flag `hasRequestedNotificationPermission`. If already requested, return early.
         - Call `UNUserNotificationCenter.current().requestAuthorization(options: [.alert, .badge, .sound])` with completion handler.
         - Context string for the request: "Want reminders for Lightning Lane booking windows?" (per CONTEXT.md)
         - Set UserDefaults `hasRequestedNotificationPermission = true` after requesting (regardless of user's choice).
         - NOTE: The actual permission dialog is system-provided. The context string can be shown as a pre-prompt alert before the system dialog to explain WHY notifications are useful, improving opt-in rate.
         - Log analytics event `notification_permission_requested` with result (granted/denied).

    2. Create `PlanContainerView.swift`:
       - Layout: ForecastBanner at top (if present) -> DayTabPicker -> DayTimelineView or LockedDayOverlay
       - "Rethink My Day" floating button at bottom-right (only on unlocked, current day)
       - RethinkLoadingView overlays when `isRethinking`
       - NavigationStack for detail push
       - `.refreshable { await viewModel.loadPlan(...) }` for pull-to-refresh
       - Empty state: if no plan loaded, show "Your plan is being created..." with loading animation
       - Error state: show error with retry button

    3. Create `DayTabPicker.swift`:
       - Horizontal scrollable tab bar
       - One tab per day: "Day 1\nMK", "Day 2\nEPCOT", etc. (day number + park abbreviation)
       - Selected tab: gold underline + bold text. Unselected: gray text
       - Locked days: show lock icon on tab
       - Min 44pt tap target per tab
       - Accessibility: each tab announces day number, park name, and locked state

    4. Create `ForecastBanner.swift`:
       - Per CONTEXT.md: "separate info section/banner at top of plan view explaining 'Wait time forecasts are in beta' -- NOT per-item badges. One-time framing"
       - Dismissible info banner with cream background and subtle border
       - Text: "Wait time forecasts are in beta. Actual wait times may vary."
       - Dismiss button (X) — persisted via UserDefaults so it doesn't reappear
       - Satisfies FC-05 requirement for "Beta Forecast" framing

    **Test File (Wave 0 — Nyquist compliance):**

    5. Create `apps/ios/WonderWaltzTests/PlanViewTests.swift`:
       - `import XCTest` and `@testable import WWPlanView`
       - Test `PlanViewModel.loadPlan` with mock API returning plan data — verify `plan` is populated
       - Test `PlanViewModel.loadPlan` with API failure + mock OfflineStore returning cached data — verify fallback to cache
       - Test `PlanViewModel.selectDay(index:)` updates `selectedDayIndex` and `currentDay`
       - Test `requestNotificationPermissionIfNeeded` sets UserDefaults flag after first call
       - Test `requestNotificationPermissionIfNeeded` is a no-op on second call (flag already set)
       - Test `isRethinking` is set to true during rethink and false after completion
       - Use mock implementations of `APIClientProtocol`, `OfflineStoreProtocol`, `AnalyticsProtocol`
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild test -only-testing WonderWaltzTests/PlanViewTests -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <done>
    - PlanContainerView displays forecast banner, day tabs, and timeline/locked overlay
    - PlanViewModel loads plan from API (online) or cache (offline)
    - PlanViewModel requests notification permission after first successful plan load (per CONTEXT.md)
    - Notification permission uses UserDefaults guard to request only once
    - DayTabPicker shows all days with park names and lock indicators
    - ForecastBanner is dismissible and persists dismissal
    - Rethink button appears on unlocked current day
    - plan_viewed analytics event tracked
    - PlanViewTests pass — plan load, cache fallback, day selection, notification permission guard
  </done>
</task>

<task type="auto">
  <name>Task 2: DayTimelineView + PlanItemCard + PlanItemDetailView + RethinkLoadingView + LockedDayOverlay</name>
  <files>
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTimelineView.swift
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemCard.swift
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemDetailView.swift
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/RethinkLoadingView.swift
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/LockedDayOverlay.swift
  </files>
  <action>
    1. Create `DayTimelineView.swift`:
       - ScrollView with LazyVStack of plan items in chronological order
       - Between each item: walking time indicator ("5 min walk" with walking icon, per CONTEXT.md: "Walking times shown inline between items in the timeline")
       - Day narrative intro at top (if present)
       - Time column on left side (start time of each item)
       - Vertical timeline line connecting items
       - Items ordered by `sortOrder`
       - Each item is a `PlanItemCard` wrapped in a `NavigationLink` to detail

    2. Create `PlanItemCard.swift` — polymorphic card for all item types:
       - Uses `WWCard` from design system as base
       - Item types and their visual treatment:
         - **Attraction**: ride icon + name + wait time (if forecasted) + LL badge (if LL booking). Gold accent
         - **Meal**: utensils icon + restaurant name + cuisine type. "Mobile Order" badge for quick-service
         - **Show/Parade/Fireworks**: calendar icon + name + start time. Fixed time indicator
         - **LL Reminder**: bell icon + "Book Lightning Lane for X" + booking window time. Alert styling
         - **Rest**: bed icon + "Rest Break" + duration. Muted styling
         - **Walk**: walking icon + "Walk to X" + walk time. Inline between items (handled by timeline)
       - All cards: min 44pt tap target, VoiceOver label with item type + name + time
       - Narrative tip shown as subtitle text on the card (if present)
       - Use Phosphor Icons from the design system

    3. Create `PlanItemDetailView.swift`:
       - Full detail view pushed via NavigationStack
       - Shows: item name, type, time range, narrative tip (full text), walking time to next item
       - For attractions: wait time forecast, height requirement, LL status
       - For meals: cuisine type, mobile order availability, dietary accommodations
       - Map placeholder (for future Phase 6 static map integration)
       - "Mark as Done" toggle (for rethink-my-day — tracks completed items)
       - Back navigation via standard NavigationStack

    4. Create `RethinkLoadingView.swift`:
       - Per CONTEXT.md: "inline loading with current plan visible. Plan stays visible but slightly dimmed. Floating progress indicator says 'Rethinking your day...'. Items animate to new positions when done"
       - Overlay on top of DayTimelineView
       - Semi-transparent background (plan visible but dimmed)
       - Centered floating card with spinner + "Rethinking your day..." text
       - When rethink completes: dismiss overlay, animate items to new positions using `.animation(.spring, value: items)`
       - Respect `@Environment(\.accessibilityReduceMotion)` — if true, instant transition instead of animation

    5. Create `LockedDayOverlay.swift`:
       - Per CONTEXT.md: "blurred cards with centered 'Unlock Trip $9.99' CTA overlaying the blurred content. User can see they have a multi-day plan but can't read details"
       - Uses `WWBlurOverlay` from design system
       - Shows blurred timeline content underneath
       - Centered: lock icon + "Unlock Trip" title + "$9.99" button
       - Button triggers paywall (in Phase 6 — for now, just calls an `onUnlockTap` closure)
       - accessibilityLabel: "Day is locked. Unlock full trip for nine dollars and ninety-nine cents"
       - Uses `.ultraThinMaterial` for the blur effect
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - DayTimelineView shows chronological items with walking times between them
    - PlanItemCard renders correctly for all 6 item types with appropriate icons and styling
    - PlanItemDetailView shows full item details with navigation
    - RethinkLoadingView shows inline loading with dimmed plan and respects reduce motion
    - LockedDayOverlay shows blur + unlock CTA for free-tier locked days
    - All views use design system, accessible fonts, and VoiceOver labels
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild test -only-testing WonderWaltzTests/PlanViewTests` passes
2. `xcodebuild build` succeeds
3. Plan view displays day tabs, timeline, and all item card types
4. Locked days show blur overlay with unlock CTA
5. Rethink loading shows dimmed plan with floating indicator
6. Item detail view accessible via tap on card
7. Forecast banner appears and is dismissible
8. Notification permission requested after first plan load (once only)
</verification>

<success_criteria>
- Complete plan viewing experience matching IOS-07 specification
- All 6 item types render with correct visual treatment
- Day switching via tab picker works with lock indicators
- Rethink flow: inline loading, dimmed plan, animated transition (with reduce motion support)
- Locked overlay with blur + paywall CTA placeholder
- Offline: plan loads from SwiftData cache with no error state
- Beta Forecast banner satisfies FC-05
- Notification permission requested after first plan load per CONTEXT.md locked decision
- PlanViewTests green
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-core/05-05-SUMMARY.md`
</output>
