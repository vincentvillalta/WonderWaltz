---
phase: 05-ios-core
plan: 07
type: execute
wave: 4
depends_on: ["05-04", "05-05", "05-06"]
files_modified: []
autonomous: false
requirements:
  - IOS-01
  - IOS-02
  - IOS-03
  - IOS-04
  - IOS-05
  - IOS-06
  - IOS-07
  - IOS-14
  - IOS-15
  - IOS-16
  - IOS-17
  - IOS-18

must_haves:
  truths:
    - "Full flow works on simulator: splash -> onboarding -> wizard -> plan view"
    - "Offline mode works: airplane mode shows cached plan without errors"
    - "VoiceOver navigates entire flow without getting stuck"
    - "Dynamic Type at accessibility5 does not clip critical text"
    - "Build succeeds on Xcode Cloud CI"
    - "Notification permission dialog appears after first plan load"
    - "All unit tests pass: DesignTokenTests, AnalyticsTests, OfflineTests, OfflineSyncTests, OnboardingTests, WizardTests, PlanViewTests"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: validate the complete iOS app flow on simulator and confirm all Phase 5 requirements are met before marking the phase complete.

Purpose: Catch integration issues, visual regressions, and accessibility gaps that automated builds cannot detect.
Output: Confirmed working app or list of issues to fix.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ios-core/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <action>
    Run the complete WonderWaltzTests suite to verify all unit tests pass before human verification.

    1. Run `cd apps/ios && xcodebuild test -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet` to execute all tests.
    2. Verify all 7 test files pass: DesignTokenTests, AnalyticsTests, OfflineTests, OfflineSyncTests, OnboardingTests, WizardTests, PlanViewTests.
    3. If any tests fail, diagnose and fix before proceeding to the human verification checkpoint.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild test -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -15</automated>
  </verify>
  <done>All 7 test files pass: DesignTokenTests, AnalyticsTests, OfflineTests, OfflineSyncTests, OnboardingTests, WizardTests, PlanViewTests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full iOS app verification on simulator</name>
  <files></files>
  <what-built>Complete iOS app flow built: Tuist project with 7 SPM packages, splash + onboarding, 8-step trip wizard with auto-save, plan view with all item types + rethink + locked overlay, offline sync (catalog + walking graph, no static maps -- deferred to Phase 6), Sentry/PostHog analytics, notification permission after first plan load, full WCAG 2.2 AA accessibility, and 7 unit test files.</what-built>
  <how-to-verify>
    1. Build and launch:
       - cd apps/ios and run tuist generate and xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16'
       - Launch app in iPhone 16 simulator

    2. Splash + Auth flow:
       - App shows splash screen with WonderWaltz branding
       - Console shows silent anonymous auth call (may fail without backend -- that is OK)
       - Transitions to onboarding after ~1.5 seconds

    3. Onboarding:
       - Swipe through 3-4 value prop pages
       - Skip button works from any page
       - Get Started on last page transitions to main screen
       - Relaunch app -- onboarding should be skipped

    4. Trip Wizard:
       - Tap Start Planning to open wizard
       - Progress bar shows step 1/8
       - Complete each step: dates, parks, guests, budget, lodging, must-do rides, meals, review
       - Kill app mid-wizard, relaunch -- should resume at saved step

    5. Plan View (may need mock data if no backend):
       - Day tabs visible with park names
       - Timeline shows items with walking times between them
       - Item cards render for different types
       - Locked days show blur overlay with Unlock Trip CTA
       - Rethink button visible on unlocked day

    6. Notification Permission:
       - After first successful plan load, verify the notification permission dialog appears
       - Context: "Want reminders for Lightning Lane booking windows?"
       - On subsequent plan loads, the dialog should NOT appear again

    7. Offline mode:
       - Enable airplane mode on simulator
       - Plan view should still display cached data (if previously loaded)
       - Gentle banner appears when trying to rethink/generate
       - Note: static map tiles are NOT included in offline package -- that is Phase 6

    8. Accessibility (enable VoiceOver in simulator: Settings > Accessibility > VoiceOver):
       - Navigate through onboarding with VoiceOver -- all pages announced
       - Navigate through wizard -- all inputs labeled
       - Navigate plan view -- all items announced with type + name + time
       - No focus traps (VoiceOver never gets stuck)

    9. Dynamic Type:
       - Settings > Accessibility > Larger Text > max slider
       - Check onboarding, wizard steps, plan view -- no text clipping

    10. Reduce Motion:
        - Settings > Accessibility > Motion > Reduce Motion ON
        - Wizard step transitions should be instant (no slide animation)
        - Rethink loading should appear/dismiss instantly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All Phase 5 requirements verified through automated tests + manual testing on simulator.
</verification>

<success_criteria>
- All 7 unit test files pass
- Complete flow works: splash -> onboarding -> wizard (8 steps with auto-save) -> plan view
- Notification permission requested after first plan load (once only)
- Offline mode: cached plan accessible without errors (no static maps expected)
- VoiceOver: full flow navigable
- Dynamic Type: no clipping at accessibility5
- Reduce motion: animations suppressed
- Build green
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-core/05-07-SUMMARY.md`
</output>
