---
phase: 05-ios-core
plan: 07
type: execute
wave: 5
depends_on: ["05-06"]
files_modified: []
autonomous: false
requirements:
  - IOS-01
  - IOS-02
  - IOS-03
  - IOS-04
  - IOS-05
  - IOS-06
  - IOS-07
  - IOS-14
  - IOS-15
  - IOS-16
  - IOS-17
  - IOS-18
must_haves:
  truths:
    - "App builds and runs on iOS Simulator"
    - "Full flow is testable: splash -> onboarding -> wizard -> plan view"
    - "VoiceOver navigates without getting stuck"
    - "Offline mode works after caching"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: run the complete app on iOS Simulator and verify the full user flow end-to-end.

Purpose: Confirm the app works as a cohesive experience before marking Phase 5 complete.
Output: Verified working app or list of issues to address.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ios-core/05-CONTEXT.md
@.planning/phases/05-ios-core/05-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full iOS app end-to-end verification</name>
  <files></files>
  <action>
  Human verifies the complete iOS app on simulator. Steps:

  1. Build and run: `xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16'` then open in Simulator
  2. First launch: verify splash screen appears, then onboarding (3 pages). Swipe through pages, verify skip button works
  3. Complete onboarding: verify navigation to main screen
  4. Start trip wizard: verify all 8 steps in order (dates, parks, guests with DAS/mobility/sensory/diet, budget, lodging, must-do rides, meals, review)
  5. Close app mid-wizard: reopen and verify wizard resumes at the correct step (auto-save)
  6. Complete wizard and review: verify summary shows all selections
  7. Submit trip: verify loading state and navigation to plan view (plan generation requires running backend -- if no backend, verify the API call structure is correct and error handling works)
  8. Plan view: verify day tabs, timeline with item cards, walking times inline (if data available)
  9. VoiceOver test: Enable VoiceOver in Simulator (Cmd+F5), navigate all screens -- verify no stuck points
  10. Dynamic Type test: Settings > Accessibility > Display > Larger Text > set to maximum (accessibility5). Verify no text clips or truncates
  11. Reduce motion: Settings > Accessibility > Motion > Reduce Motion ON. Verify animations are suppressed
  12. Verify all tests pass: `xcodebuild test -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16'`
  </action>
  <verify>
    <automated>cd apps/ios && xcodebuild test -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
  </verify>
  <done>Human confirms full flow works on simulator, VoiceOver navigation passes, Dynamic Type does not break layout, test suite green. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- Full test suite passes
- App runs without crashes on simulator
- All 12 IOS requirements addressed
</verification>

<success_criteria>
- Human confirms full flow works on simulator
- VoiceOver navigation passes
- Dynamic Type does not break layout
- Test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-core/05-07-SUMMARY.md`
</output>
