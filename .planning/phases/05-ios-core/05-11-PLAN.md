---
phase: 05-ios-core
plan: 11
type: execute
wave: 2
depends_on: ["05-08"]
files_modified:
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanContainerView.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanViewModel.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayPillPicker.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTimelineView.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemCard.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemDetailView.swift
  - apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTabPicker.swift
  - apps/ios/WonderWaltzTests/PlanViewTests.swift
autonomous: true
gap_closure: true
requirements:
  - IOS-07
  - IOS-17

must_haves:
  truths:
    - "Day selector is horizontal scrollable pills (not tabs) with park-color border when selected"
    - "Timeline cards have park-colored icon (36pt rounded-xl) + vertical connector line in park color at 20% opacity"
    - "LL badge is gold bg with navy text, ADR badge is green bg with white text"
    - "Drag handle (GripVertical equivalent) on right side of each card"
    - "Cards are swipeable: left for Skip (red), right for Done (green)"
    - "Detail bottom sheet has spring animation, park-color time badge, hero gradient, height req card, Pro Tip gold card, walk time card, navy 'Start Navigation' CTA"
    - "Empty state uses WWEmptyState component"
  artifacts:
    - path: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayPillPicker.swift"
      provides: "Horizontal scrollable day pills with park-color borders"
    - path: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemCard.swift"
      provides: "Swipeable timeline card with park-color icon + connector + badges"
    - path: "apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemDetailView.swift"
      provides: "Bottom sheet detail matching StopDetailSheet.tsx"
  key_links:
    - from: "PlanContainerView"
      to: "DayPillPicker"
      via: "day selection binding"
      pattern: "selectedDay|DayPillPicker"
    - from: "PlanItemCard"
      to: "ParkColor"
      via: "park-coded icon and connector colors"
      pattern: "ParkColor|parkColor"
    - from: "PlanItemDetailView"
      to: "WWButton"
      via: "navy Start Navigation CTA"
      pattern: "WWButton.*primary|Start Navigation"
---

<objective>
Rewrite WWPlanView to match React Itinerary.tsx + StopDetailSheet.tsx. Current implementation uses tab-style day picker and basic cards. Needs: horizontal scrollable day pills with park-color borders, timeline cards with park-colored icons + connector lines, swipeable skip/done, bottom sheet detail with spring animation.

Purpose: Plan view is where users spend the most time. Must match the polished, park-coded React reference.
Output: Visually faithful plan view with day pills, timeline cards, swipeable actions, bottom sheet detail.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ios-core/05-CONTEXT.md
@.planning/phases/05-ios-core/05-05-SUMMARY.md

<interfaces>
<!-- From Plan 08 (design system updates) -->
WWButton: .primary (navy + shimmer), .secondary, .compact
WWEmptyState: gradient icon + Fraunces title + navy CTA
ParkColor enum: .magicKingdom, .epcot, .hollywoodStudios, .animalKingdom
  - .color: Color, .emoji: String, .shortName: String, .displayName: String, .tintedBackground: Color
WWTheme.border: navy at 10% opacity
WWTabBar: 3-tab bar with gold pill

<!-- Existing PlanViewModel contract -->
From PlanViewModel.swift:
```swift
@Observable @MainActor
public final class PlanViewModel {
    public var planData: PlanData?
    public var selectedDayIndex: Int = 0
    public var isLoading: Bool = false
    public var isRethinking: Bool = false
    public var showForecastBanner: Bool = true
    public func loadPlan(tripId: String) async { ... }
    public func rethinkDay(tripId: String) async { ... }
}

public struct PlanData { public var days: [PlanDayData] }
public struct PlanDayData { public var date: String; public var parkName: String; public var items: [PlanItemData]; public var isLocked: Bool }
public struct PlanItemData { public var id: String; public var type: PlanItemType; public var title: String; public var location: String; public var time: String; public var waitMinutes: Int?; public var durationMinutes: Int?; public var isLL: Bool; public var isADR: Bool }
public enum PlanItemType: String { case attraction, meal, show, llReminder, rest, walk }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DayPillPicker + PlanContainerView + PlanItemCard with swipe</name>
  <files>
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayPillPicker.swift,
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanContainerView.swift,
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTimelineView.swift,
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemCard.swift,
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanViewModel.swift,
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/DayTabPicker.swift
  </files>
  <action>
    Read the React reference files at:
    - /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/Itinerary.tsx
    - /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/EmptyState.tsx

    1. CREATE `DayPillPicker.swift` (replacing DayTabPicker -- rename the file or create new and delete old):
       Matching Itinerary.tsx lines 328-352:
       - Horizontal `ScrollView(.horizontal, showsIndicators: false)` with HStack gap 8pt.
       - Each day is a button pill: `px-16pt py-12pt rounded-xl`. Contains:
         a. Day abbreviation (Mon/Tue/Wed) in caption, muted text.
         b. Date (Jul 15) in `WWTypography.subheadline` weight medium.
         c. Park short name in caption, park color when selected.
       - Selected pill: white bg (`bg-card`), 2pt border in park color, shadow-sm.
       - Unselected pill: muted/50% bg, transparent border.
       - `@Binding var selectedIndex: Int`, `let days: [PlanDayData]`.
       - DELETE old `DayTabPicker.swift` if not reusing the same file.

    2. REWRITE `PlanContainerView.swift` matching Itinerary.tsx:
       - Header: Fraunces large title "Your Trip", subtitle with trip dates in muted text. Same layout as Itinerary.tsx lines 302-310.
       - If no itinerary (planData == nil): show `WWEmptyState` with sparkles icon, "No Itinerary Yet" title, "Create your first trip..." description, "Plan Your Trip" CTA.
       - Day pill picker: `DayPillPicker(selectedIndex: $viewModel.selectedDayIndex, days: viewModel.planData!.days)`.
       - Hint banner: gold-tinted card with lightbulb emoji + "Tap cards for details -- Swipe to skip/complete". `rounded-xl bg-gradient from gold/10%`. Only show once (use `@AppStorage`).
       - Timeline: `DayTimelineView` below.
       - Pull-to-refresh hint at bottom: "Swipe down to re-optimize from current time" with chevron-down bouncing. Gold-tinted card.
       - Keep ForecastBanner, RethinkLoadingView, LockedDayOverlay as-is (they are fine).

    3. UPDATE `PlanViewModel.swift`:
       - Add `@Published var skippedItems: Set<String>` to track skipped/done items.
       - Add `func skipItem(_ id: String)` and `func markDone(_ id: String)`.
       - Add `parkColor(for day: PlanDayData) -> ParkColor` helper that maps parkName string to ParkColor enum.
       - Keep all existing load/rethink/notification logic unchanged.

    4. REWRITE `PlanItemCard.swift` matching Itinerary.tsx `SwipeableTimelineCard`:
       - Each card is a swipeable row using `DragGesture`:
         a. Swipe background: behind card, shows "Skip" (HS red) on left reveal, "Done" (AK green) on right reveal.
         b. Card content: `p-4 rounded-2xl bg-card border border-border`.
         c. Left: park-colored icon. 36pt (w-9) rounded-xl container with park-color at 8% bg. SF Symbol icon in park-color (ticket for attraction, fork.knife for meal/dining, clock for break, mappin for default).
         d. Center: title in headline weight. Below: time in park-color + bullet + location + optional wait time. All in `WWTypography.subheadline`, muted.
         e. Right top: badges. LL badge = gold bg (#E8B547), navy text, 6pt horizontal padding, 2pt vertical, rounded. ADR badge = AK green bg, white text, same sizing.
         f. Right: drag handle icon (line.3.horizontal SF Symbol) in muted/30%.
       - Swipe threshold: 100pt. On exceed left -> onSkip callback. On exceed right -> onDone callback. Snap back with spring otherwise.
       - Skipped items render at 50% opacity with checkmark.
       - On tap: calls `onTap` to open detail sheet.

    5. REWRITE `DayTimelineView.swift`:
       - Vertical connector line: between each card (not last). `Rectangle()` 2pt wide, park-color at 20% opacity. Positioned at left-center of the icon column (18pt from leading edge). Extends from bottom of icon to top of next card.
       - Cards animate in with staggered delay (index * 0.03s, opacity + y offset 10pt). Gate on reduce-motion.
       - Walking time indicators: walk-type items filtered from display but their duration shown as inline capsule badge on the connector line between items (e.g., "5 min walk" in caption text).
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Day pills with park-color borders, timeline cards with park-color icons + connector lines + LL/ADR badges + swipe skip/done, drag handles, empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite PlanItemDetailView as bottom sheet + update tests</name>
  <files>
    apps/ios/Packages/WWPlanView/Sources/WWPlanView/PlanItemDetailView.swift,
    apps/ios/WonderWaltzTests/PlanViewTests.swift
  </files>
  <action>
    Read the React reference file at /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/StopDetailSheet.tsx

    1. REWRITE `PlanItemDetailView.swift` as a bottom sheet matching StopDetailSheet.tsx:
       - Present as a `.sheet` or custom bottom sheet overlay (prefer custom for spring animation control).
       - Backdrop: black at 40% opacity, tappable to dismiss.
       - Sheet: slides up from bottom with spring animation (damping 30, stiffness 300). `rounded-t-3xl` (24pt top corners). White bg. Max height 85vh (use GeometryReader * 0.85).
       - Handle: centered 40pt x 4pt rounded-full capsule in muted color.
       - Header:
         a. Badges row: LL badge ("Lightning Lane", gold bg navy text) and/or ADR badge ("ADR", green bg white text).
         b. Title: Fraunces `WWTypography.title2` (22pt).
         c. Location: mappin icon + location text in muted subheadline.
         d. Close button: 36pt circle, muted bg, X icon.
       - Time + stats row: HStack of capsule badges:
         a. Time badge: park-color tinted bg (parkColor at 8%), clock icon in park-color, time text.
         b. Wait badge (if applicable): muted bg, users icon, "X min wait".
         c. Duration badge (if applicable): muted bg, clock icon, duration.
       - Scrollable content:
         a. Hero image placeholder: full-width, 192pt tall (h-48), rounded-2xl, gradient from parkColor/12% to parkColor/3%. Large emoji centered (ticket for attraction, plate for meal, mask for show, coffee for break).
         b. Height requirement card (if applicable): card with users icon in gold, "Height Requirement" title, "Minimum X tall to ride", divider, per-guest check/cross list. Use checkmark.circle (green) and xmark.circle (red).
         c. Pro Tip card: gold-tinted card (`gold/10% bg, gold/20% border`). Sparkles icon in gold. "Pro Tip" title. Tip text.
         d. Walk time card: card with location.north icon, "From previous stop" title, "X minute walk" subtitle, distance on right.
       - Action buttons at bottom (pinned):
         a. Navy CTA: `WWButton("Start Navigation", style: .primary)` with location.north icon.
         b. Two half-width buttons below: "Swap" (arrow.left.arrow.right icon, secondary style) and "Skip" (trash icon, red-tinted).

    2. Wire detail sheet into PlanContainerView:
       - `@State private var selectedItemIndex: Int?`
       - When PlanItemCard tapped, set selectedItemIndex.
       - Show PlanItemDetailView as overlay when selectedItemIndex != nil.

    3. UPDATE `PlanViewTests.swift`:
       - Keep load/cache/rethink/notification tests.
       - Update day selection test for DayPillPicker (selectedDayIndex binding).
       - Add test: `testSkipItem` -- calling skipItem adds to skippedItems set.
       - Add test: `testMarkDone` -- calling markDone adds to skippedItems set.
       - Add test: `testParkColorMapping` -- verify parkColor(for:) returns correct ParkColor.
       - Remove tests specific to old DayTabPicker if any.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild test -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing WonderWaltzTests/PlanViewTests -quiet 2>&1 | tail -10</automated>
  </verify>
  <done>Bottom sheet detail matches StopDetailSheet.tsx with spring animation, park-color badges, hero gradient, height req, Pro Tip, walk time, navy navigation CTA. PlanViewTests pass.</done>
</task>

</tasks>

<verification>
- `xcodebuild build` passes
- PlanViewTests all pass
- Day pills scroll horizontally with park-color borders
- Timeline cards show park-colored icons + connector lines
- Swipe left = Skip, swipe right = Done
- Detail sheet slides up with spring, shows all sections
- Empty state uses WWEmptyState component
</verification>

<success_criteria>
- Day selector: horizontal scrollable pills with park-color border on selected day
- Timeline cards: 36pt park-color icon in tinted container + vertical connector line at 20% opacity
- Badges: LL = gold bg + navy text, ADR = green bg + white text
- Drag handle on right side of each card
- Swipe left = Skip (red reveal), swipe right = Done (green reveal)
- Detail sheet: spring animation, park-color time badge, hero gradient, height req card with per-guest check, Pro Tip gold card, walk time card
- Navy "Start Navigation" CTA with shimmer
- Empty state: WWEmptyState with sparkles icon
- All PlanViewTests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-core/05-11-SUMMARY.md`
</output>
