---
phase: 05-ios-core
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWButton.swift
  - apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWCard.swift
  - apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWEmptyState.swift
  - apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWTabBar.swift
  - apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Theme.swift
  - apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/ParkColors.swift
autonomous: true
gap_closure: true
requirements:
  - IOS-05
  - IOS-07
  - IOS-17

must_haves:
  truths:
    - "WWButton primary style is navy (#1B2A4E) background with cream text and gold shimmer sweep animation"
    - "WWButton secondary style has outlined border with no fill"
    - "WWEmptyState shows gradient icon container (navy/gold 10%), Fraunces title, navy CTA"
    - "WWTabBar renders 3 tabs (Live/Zap, Plan/Calendar, Me/User) with gold pill indicator"
    - "ParkColors enum maps each WDW park to its canonical color"
    - "WWCard uses rounded-2xl (16pt), white bg, border with navy 10% opacity"
  artifacts:
    - path: "apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWButton.swift"
      provides: "Navy primary button with gold shimmer"
    - path: "apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWEmptyState.swift"
      provides: "Empty state pattern matching React EmptyState.tsx"
    - path: "apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWTabBar.swift"
      provides: "3-tab bar with gold pill indicator"
    - path: "apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/ParkColors.swift"
      provides: "Park color enum with MK pink, EPCOT teal, HS red, AK green"
  key_links:
    - from: "WWButton"
      to: "WWDesignTokens"
      via: "color references"
      pattern: "colorPrimitiveNavy|colorPrimitiveGold|colorPrimitiveCream"
    - from: "WWEmptyState"
      to: "WWTypography"
      via: "Fraunces display font for title"
      pattern: "WWTypography\\.title"
---

<objective>
Rewrite WWDesignSystem components to match the React reference app's visual patterns. The current WWButton uses gold background (wrong -- should be navy with gold shimmer). Missing components: WWEmptyState, WWTabBar, ParkColors helper.

Purpose: All downstream UI packages (WWOnboarding, WWTripWizard, WWPlanView) consume these components. Fixing the design system first means every view inherits correct styling.
Output: Updated WWButton, WWCard, new WWEmptyState, new WWTabBar, new ParkColors.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ios-core/05-CONTEXT.md
@.planning/phases/05-ios-core/05-01-SUMMARY.md

<interfaces>
<!-- Existing design system contracts the executor needs -->

From WWDesignTokens.swift:
```swift
public enum WWDesignTokens {
    public static let colorPrimitiveNavy = Color(red: 0.106, green: 0.165, blue: 0.306, opacity: 1)     // #1B2A4E
    public static let colorPrimitiveGold = Color(red: 0.910, green: 0.710, blue: 0.278, opacity: 1)     // #E8B547
    public static let colorPrimitiveCream = Color(red: 0.980, green: 0.965, blue: 0.937, opacity: 1)    // #FAF6EF
    public static let colorParkMagicKingdom = Color(red: 1.000, green: 0.420, blue: 0.616, opacity: 1)  // #FF6B9D
    public static let colorParkEpcot = Color(red: 0.000, green: 0.749, blue: 0.647, opacity: 1)         // #00BFA5
    public static let colorParkHollywoodStudios = Color(red: 0.902, green: 0.224, blue: 0.275, opacity: 1) // #E63946
    public static let colorParkAnimalKingdom = Color(red: 0.024, green: 0.839, blue: 0.627, opacity: 1) // #06D6A0
    public static let colorSemanticLightMuted = Color(red: 0.898, green: 0.867, blue: 0.816, opacity: 1) // #E5DDD0
    public static let colorSemanticLightCard = Color(red: 1.000, green: 1.000, blue: 1.000, opacity: 1)
    public static let radiusBase: CGFloat = 16
    public static let radiusXl: CGFloat = 20
    public static let iconographyMinTapTarget: CGFloat = 44
    public static let fontFamilyDisplay = "Fraunces"
    public static let fontFamilyUI = "Inter"
}
```

From WWTheme.swift:
```swift
public enum WWTheme {
    public static let background = WWDesignTokens.colorSemanticLightBackground
    public static let surface = WWDesignTokens.colorSemanticLightCard
    public static let muted = WWDesignTokens.colorSemanticLightMuted
    public static let textPrimary = WWDesignTokens.colorSemanticLightForeground
    public static let textSecondary = WWDesignTokens.colorSemanticLightMutedFg
    public static let textOnPrimary = WWDesignTokens.colorSemanticLightPrimaryFg
    public static let textOnAccent = WWDesignTokens.colorSemanticLightAccentFg
    public static let accent = WWDesignTokens.colorPrimitiveGold
    public static let primary = WWDesignTokens.colorPrimitiveNavy
}
```

From WWTypography.swift:
```swift
public enum WWTypography {
    public static let largeTitle: Font  // Fraunces 34pt
    public static let title: Font       // Fraunces 28pt
    public static let title2: Font      // Fraunces 22pt
    public static let title3: Font      // Fraunces 20pt
    public static let headline: Font    // Inter 17pt semibold
    public static let body: Font        // Inter 16pt
    public static let subheadline: Font // Inter 14pt
    public static let footnote: Font    // Inter 13pt
    public static let caption: Font     // Inter 12pt
    public static let button: Font      // Inter 16pt medium
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite WWButton to navy+shimmer + add ParkColors enum</name>
  <files>
    apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWButton.swift,
    apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/ParkColors.swift,
    apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Theme.swift
  </files>
  <action>
    Read the React reference files at:
    - /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/Onboarding.tsx (lines 149-161 for CTA button pattern)
    - /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/EmptyState.tsx (lines 60-71 for button pattern)
    - /Users/vincentvillalta/Downloads/WonderWaltz 2/src/styles/theme.css (full color system)

    1. REWRITE `WWButton.swift`:
       - `.primary` style: navy (#1B2A4E) background, cream (#FAF6EF) text. Full-width. 56pt height (h-14 = 3.5rem = 56pt). Rounded-2xl (16pt radius). Chevron-right icon after label text.
       - Gold shimmer sweep: overlay with `LinearGradient` from gold/0% -> gold/20% -> gold/0%, animated from leading to trailing on hover/tap using `GeometryReader` and offset animation. Use `@State private var shimmerOffset` with repeating animation. Gate animation on `@Environment(\.accessibilityReduceMotion)`.
       - `.secondary` style: transparent bg, navy border (1.5pt), navy text. Same dimensions.
       - `.compact` style (NEW): For inline contexts -- auto-width `px-6 py-3`, navy bg, cream text, rounded-2xl, gold shimmer. Used by EmptyState CTA.
       - Keep `isLoading` spinner support.
       - Keep 44pt minimum tap target and accessibility traits.

    2. CREATE `ParkColors.swift`:
       ```swift
       public enum ParkColor: String, CaseIterable, Sendable {
           case magicKingdom
           case epcot
           case hollywoodStudios
           case animalKingdom

           public var color: Color { ... }       // Maps to WWDesignTokens.colorPark*
           public var emoji: String { ... }      // MK: "castle emoji", EPCOT: "globe emoji", HS: "film emoji", AK: "lion emoji"
           public var shortName: String { ... }  // "MK", "EPCOT", "HS", "AK"
           public var displayName: String { ... } // Full names
           public var tintedBackground: Color { ... } // color.opacity(0.1) for card backgrounds
       }
       ```

    3. ADD to `WWTheme.swift`:
       - `public static let border = WWDesignTokens.colorPrimitiveNavy.opacity(0.1)` -- matches React `--border: rgba(27, 42, 78, 0.1)`
       - `public static let cardBackground = WWDesignTokens.colorSemanticLightCard` (alias for clarity)
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>WWButton renders navy bg + gold shimmer for .primary, outlined for .secondary, compact for inline CTA. ParkColors enum available. WWTheme.border added.</done>
</task>

<task type="auto">
  <name>Task 2: Create WWEmptyState + WWTabBar + update WWCard</name>
  <files>
    apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWEmptyState.swift,
    apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWTabBar.swift,
    apps/ios/Packages/WWDesignSystem/Sources/WWDesignSystem/Components/WWCard.swift
  </files>
  <action>
    Read the React reference files at:
    - /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/EmptyState.tsx
    - /Users/vincentvillalta/Downloads/WonderWaltz 2/src/app/components/TabBar.tsx

    1. CREATE `WWEmptyState.swift` matching EmptyState.tsx:
       - Parameters: `icon: String` (SF Symbol name), `title: String`, `description: String`, `actionLabel: String?`, `action: (() -> Void)?`
       - Layout: centered VStack with full-height.
       - Icon container: 96x96pt (w-24), rounded-3xl, gradient background from navy/10% to gold/10%, icon in gold at 48pt (w-12). Use `Image(systemName:)`.
       - Title: Fraunces display font (`WWTypography.title`), navy color.
       - Description: Inter body, muted-foreground color, max 320pt width, relaxed line spacing.
       - CTA button: Use `WWButton` with `.compact` style.
       - Gate appear animation on reduce-motion. Spring animation for icon (scale 0->1, slight rotate).

    2. CREATE `WWTabBar.swift` matching TabBar.tsx:
       - 3 tabs: Live (bolt.fill SF Symbol), Plan (calendar SF Symbol), Me (person.fill SF Symbol).
       - Tab model: `WWTab` enum with `id`, `label`, `systemImage`.
       - Parameters: `@Binding var selectedTab: WWTab`
       - Layout: HStack with equal spacing, 80pt height (h-20), card background, top border (navy 10%).
       - Active state: gold pill background (gold/10% opacity, rounded-xl) behind the active tab using `matchedGeometryEffect(id: "tab-indicator")`. Gate animation on reduce-motion.
       - Active icon: navy color, strokeWeight 2.5 (use `.fontWeight(.bold)` on SF Symbol). Inactive: muted-foreground, regular weight.
       - Active label: navy, Inter caption medium. Inactive: muted-foreground.
       - Safe area bottom padding.

    3. UPDATE `WWCard.swift`:
       - Ensure card uses: white background, rounded-2xl (16pt), border of navy at 10% opacity (`WWTheme.border`).
       - The React pattern is `bg-card border border-border rounded-2xl`. Translate to SwiftUI: `.background(WWTheme.surface)` + `.clipShape(RoundedRectangle(cornerRadius: WWDesignTokens.radiusBase))` + `.overlay(RoundedRectangle(...).stroke(WWTheme.border, lineWidth: 1))`.
       - Ensure `.accessibilityElement(children: .combine)` is preserved from the accessibility pass.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz/apps/ios && xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>WWEmptyState renders gradient icon + Fraunces title + navy CTA. WWTabBar renders 3 tabs with gold pill. WWCard uses correct border treatment.</done>
</task>

</tasks>

<verification>
- `xcodebuild build` passes with no errors
- DesignTokenTests still pass
- WWButton preview shows navy button with shimmer
- WWEmptyState preview shows gradient icon container
- WWTabBar preview shows 3 tabs with gold indicator
</verification>

<success_criteria>
- WWButton primary = navy bg + cream text + gold shimmer sweep
- WWEmptyState matches React EmptyState.tsx: gradient icon, Fraunces title, compact navy CTA
- WWTabBar matches React TabBar.tsx: 3 tabs, gold pill indicator, matched geometry
- WWCard has correct border treatment (navy 10% opacity)
- ParkColors enum provides color, emoji, names for all 4 WDW parks
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-core/05-08-SUMMARY.md`
</output>
