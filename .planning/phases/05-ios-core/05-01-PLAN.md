---
phase: 05-ios-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ios/WonderWaltz.xcodeproj/project.pbxproj
  - apps/ios/WonderWaltz/App/WonderWaltzApp.swift
  - apps/ios/WonderWaltz/App/AppState.swift
  - apps/ios/WonderWaltz/Core/API/openapi.json
  - apps/ios/WonderWaltz/Core/API/openapi-generator-config.yaml
  - apps/ios/WonderWaltz/Core/API/APIClient.swift
  - apps/ios/WonderWaltz/Core/API/AuthMiddleware.swift
  - apps/ios/WonderWaltz/Core/Auth/KeychainStore.swift
  - apps/ios/WonderWaltz/DesignSystem/WWDesignTokens.swift
  - apps/ios/WonderWaltz/DesignSystem/Theme.swift
  - apps/ios/WonderWaltz/DesignSystem/Typography.swift
  - apps/ios/WonderWaltz/Analytics/AnalyticsService.swift
  - apps/ios/WonderWaltz/Analytics/CrashReporting.swift
  - apps/ios/WonderWaltz/Resources/Localizable.xcstrings
  - apps/ios/WonderWaltzTests/DesignTokenTests.swift
  - apps/ios/WonderWaltzTests/AnalyticsTests.swift
autonomous: true
requirements:
  - IOS-01
  - IOS-02
  - IOS-03
  - IOS-14
  - IOS-15
  - IOS-16
must_haves:
  truths:
    - "Xcode project builds cleanly targeting iOS 17.0 with Swift 6 and strict concurrency"
    - "Swift OpenAPI Generator produces a compiled API client from the frozen OpenAPI snapshot at build time"
    - "Design tokens (colors, fonts) are accessible in SwiftUI views via WWDesignTokens constants"
    - "Sentry Cocoa is initialized before the first screen renders"
    - "PostHog captures events with guest age data filtered out"
    - "All user-facing strings use String Catalogs (.xcstrings)"
  artifacts:
    - path: "apps/ios/WonderWaltz/Core/API/openapi.json"
      provides: "OpenAPI spec for Swift code generation"
    - path: "apps/ios/WonderWaltz/Core/API/APIClient.swift"
      provides: "Configured API client with auth middleware"
      exports: ["APIClientProvider"]
    - path: "apps/ios/WonderWaltz/DesignSystem/WWDesignTokens.swift"
      provides: "Brand color and font constants"
    - path: "apps/ios/WonderWaltz/Analytics/AnalyticsService.swift"
      provides: "PostHog wrapper with age-data filtering"
      exports: ["AnalyticsService"]
    - path: "apps/ios/WonderWaltzTests/AnalyticsTests.swift"
      provides: "Test that age data is stripped from PostHog events"
  key_links:
    - from: "apps/ios/WonderWaltz/Core/API/openapi.json"
      to: "packages/shared-openapi/openapi.v1.snapshot.json"
      via: "file copy at build time"
      pattern: "openapi\\.json"
    - from: "apps/ios/WonderWaltz/Analytics/AnalyticsService.swift"
      to: "PostHogSDK"
      via: "filtered capture wrapper"
      pattern: "forbiddenProperties"
---

<objective>
Restructure the shell Xcode project into a modular iOS app foundation with all SPM dependencies, Swift OpenAPI Generator build-time client, design system tokens, Sentry/PostHog SDK initialization, String Catalog, and test target.

Purpose: Every subsequent plan depends on a compiling project with networking, design tokens, analytics, and i18n infrastructure in place.
Output: A building Xcode project with generated API client, design system module, analytics services, and test target.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ios-core/05-CONTEXT.md
@.planning/phases/05-ios-core/05-RESEARCH.md

@packages/shared-openapi/openapi.v1.snapshot.json
@packages/design-tokens/generated/WWDesignTokens.swift
@apps/ios/WonderWaltz.xcodeproj/project.pbxproj
@apps/ios/WonderWaltz/WonderWaltzApp.swift

<interfaces>
<!-- The existing Xcode project is a shell with WonderWaltzApp.swift + ContentView.swift -->
<!-- The OpenAPI snapshot at packages/shared-openapi/openapi.v1.snapshot.json is the frozen v1 spec -->
<!-- WWDesignTokens.swift contains SwiftUI Color constants (colorPrimitiveNavy, colorPrimitiveGold, etc.) -->
<!-- NestJS response envelope: { data: T, meta: { disclaimer: string } } — all responses wrapped -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Xcode project with SPM dependencies and module directories</name>
  <files>
    apps/ios/WonderWaltz.xcodeproj/project.pbxproj,
    apps/ios/WonderWaltz/App/WonderWaltzApp.swift,
    apps/ios/WonderWaltz/App/AppState.swift,
    apps/ios/WonderWaltz/Core/API/openapi.json,
    apps/ios/WonderWaltz/Core/API/openapi-generator-config.yaml,
    apps/ios/WonderWaltz/Core/API/APIClient.swift,
    apps/ios/WonderWaltz/Core/API/AuthMiddleware.swift,
    apps/ios/WonderWaltz/Core/Auth/KeychainStore.swift,
    apps/ios/WonderWaltz/DesignSystem/WWDesignTokens.swift,
    apps/ios/WonderWaltz/DesignSystem/Theme.swift,
    apps/ios/WonderWaltz/DesignSystem/Typography.swift,
    apps/ios/WonderWaltz/Resources/Localizable.xcstrings
  </files>
  <action>
  1. Create the module directory structure under apps/ios/WonderWaltz/:
     - App/ (move WonderWaltzApp.swift here, create AppState.swift)
     - Core/API/ (OpenAPI client)
     - Core/Auth/ (Keychain token storage)
     - DesignSystem/ (tokens, theme, typography)
     - Features/ (empty — populated by later plans)
     - Offline/ (empty — populated by plan 02)
     - Analytics/ (Sentry + PostHog)
     - Resources/ (String Catalog, fonts, assets)
     - Utilities/ (empty — populated later)

  INTERMEDIATE BUILD CHECK: After creating the directory structure, moving WonderWaltzApp.swift to App/, updating the Xcode project file references, and removing old ContentView.swift, run `cd apps/ios && xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5` to verify the restructured project skeleton compiles. Fix any broken file references before proceeding to SPM dependencies.

  2. Add SPM package dependencies to the Xcode project (edit project.pbxproj or use swift package commands):
     - apple/swift-openapi-generator 1.11.1
     - apple/swift-openapi-runtime (latest compatible)
     - apple/swift-openapi-urlsession (latest compatible)
     - getsentry/sentry-cocoa 9.8.0
     - PostHog/posthog-ios (latest)
     - kishikawakatsumi/KeychainAccess (latest)
     NOTE: PhosphorSwift can be added later when icons are needed in UI.

  3. Copy packages/shared-openapi/openapi.v1.snapshot.json to apps/ios/WonderWaltz/Core/API/openapi.json. Create openapi-generator-config.yaml with:
     ```yaml
     generate:
       - types
       - client
     ```
     Add both files to the target's build phase. Add the OpenAPIGenerator build tool plugin to Build Phases > Run Build Tool Plug-ins.

  4. Create APIClient.swift with an actor-based APIClientProvider that creates a Client with URLSessionTransport + AuthMiddleware. The AuthMiddleware injects Bearer token from KeychainStore into Authorization header. Include a server URL configurable via environment (default to staging).

  5. Create KeychainStore.swift wrapping KeychainAccess for JWT token get/save/delete. Use service name "com.wonderwaltz.auth".

  6. Copy packages/design-tokens/generated/WWDesignTokens.swift to apps/ios/WonderWaltz/DesignSystem/. Create Theme.swift with semantic helpers (e.g., Color.wwPrimary, Color.wwBackground). Create Typography.swift with Font helpers using Fraunces (display) and Inter (body) with `relativeTo:` parameter for Dynamic Type (e.g., Font.wwTitle -> Font.custom("Fraunces", size: 28, relativeTo: .title)). For now, use system fonts as fallback until font files are bundled — define the helpers so they can be swapped later.

  7. Create Localizable.xcstrings String Catalog file. Move the ContentView.swift placeholder text into it as the first entry. Configure Xcode to use String Catalogs for the target.

  8. Create WonderWaltzTests/ test target in the Xcode project if it doesn't exist.

  9. Update WonderWaltzApp.swift: move to App/ directory, keep it minimal — just WindowGroup with a placeholder ContentView. AppState.swift: @Observable class with auth token, navigation state stubs. Mark @MainActor.

  10. Remove the old ContentView.swift from root WonderWaltz/ directory (replaced by restructured project).

  IMPORTANT: The Xcode project.pbxproj must be updated to reflect all new files, groups, SPM dependencies, and build phases. This is the most complex part — ensure all file references, group hierarchies, and build phase memberships are correct.
  </action>
  <verify>
    <automated>cd apps/ios && xcodebuild build -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Xcode project builds cleanly with Swift OpenAPI Generator producing compiled types from the OpenAPI snapshot; design tokens are importable; String Catalog is configured; test target exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sentry + PostHog SDK initialization and analytics age-data filter</name>
  <files>
    apps/ios/WonderWaltz/Analytics/CrashReporting.swift,
    apps/ios/WonderWaltz/Analytics/AnalyticsService.swift,
    apps/ios/WonderWaltzTests/AnalyticsTests.swift,
    apps/ios/WonderWaltzTests/DesignTokenTests.swift
  </files>
  <behavior>
    - Test: AnalyticsService.capture strips properties containing "guest_age", "age_bracket", "age", "dob", "birthdate" keys (case-insensitive)
    - Test: AnalyticsService.capture passes through non-age properties unchanged
    - Test: DesignTokenTests verify WWDesignTokens.colorPrimitiveNavy and colorPrimitiveGold are non-nil SwiftUI Colors
  </behavior>
  <action>
  1. Create CrashReporting.swift: a static configure() method that calls SentrySDK.start with DSN from environment/config, enableSwiftAsyncStacktraces = true, tracesSampleRate = 0.2. Call this in WonderWaltzApp.init() before any view renders.

  2. Create AnalyticsService.swift: static capture(_ event: String, properties: [String: Any]) method that strips forbidden keys (guest_age, age_bracket, age, dob, birthdate, birth_date — case-insensitive check) before forwarding to PostHogSDK.shared.capture(). Static configure() method called from WonderWaltzApp.init() with PostHog API key from environment.

  3. Write AnalyticsTests.swift testing the age-data filter:
     - Properties with "guest_age" key are stripped
     - Properties with "Age_Bracket" (case-insensitive) are stripped
     - Non-age properties like "plan_id" pass through
     - Empty properties dict works without crash

  4. Write DesignTokenTests.swift testing that key design token colors are accessible:
     - WWDesignTokens.colorPrimitiveNavy is a valid Color
     - WWDesignTokens.colorPrimitiveGold is a valid Color
     - WWDesignTokens.colorPrimitiveCream is a valid Color

  5. Update WonderWaltzApp.swift init to call CrashReporting.configure() and AnalyticsService.configure() before body is evaluated. Use init() {} block in the App struct.

  NOTE: For Sentry DSN and PostHog API key, read from a Config.swift or environment. Use placeholder values that can be set via Xcode scheme environment variables — do NOT hardcode real keys. The SDK inits should gracefully no-op if keys are empty strings.
  </action>
  <verify>
    <automated>cd apps/ios && xcodebuild test -scheme WonderWaltz -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing WonderWaltzTests 2>&1 | tail -10</automated>
  </verify>
  <done>Sentry initializes before first screen; PostHog captures events with age data filtered; unit tests pass for analytics filter and design token accessibility</done>
</task>

</tasks>

<verification>
- `xcodebuild build` succeeds with zero errors and zero warnings related to our code
- Swift OpenAPI Generator produces types from the snapshot (generated types compile)
- `xcodebuild test` runs AnalyticsTests and DesignTokenTests successfully
- String Catalog file exists and is recognized by Xcode
</verification>

<success_criteria>
- Project builds targeting iOS 17.0 with Swift 6 strict concurrency
- OpenAPI client types are generated at build time from the frozen v1 snapshot
- WWDesignTokens colors are importable in any SwiftUI view
- Sentry + PostHog SDKs initialize without crash
- PostHog age-data filter verified by passing unit test
- Test target exists and runs at least 4 tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-ios-core/05-01-SUMMARY.md`
</output>
