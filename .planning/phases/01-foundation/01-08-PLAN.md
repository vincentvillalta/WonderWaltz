---
phase: 01-foundation
plan: "08"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-07"
files_modified:
  - packages/design-tokens/tokens.json
  - packages/design-tokens/style-dictionary.config.mjs
  - packages/design-tokens/generated/WWDesignTokens.swift
  - packages/design-tokens/generated/WWTheme.kt
  - packages/design-tokens/generated/tokens.css
  - packages/design-tokens/generated/tokens.ts
  - packages/design-tokens/tests/build.test.ts
autonomous: true
requirements:
  - DSGN-03
  - DSGN-04
  - DSGN-07

must_haves:
  truths:
    - "pnpm --filter @wonderwaltz/design-tokens build exits 0"
    - "generated/WWDesignTokens.swift contains Color (SwiftUI), not UIColor (UIKit)"
    - "generated/WWTheme.kt contains Compose Color values"
    - "generated/tokens.css contains @theme block (Tailwind v4)"
    - "generated/tokens.ts contains exported token constants"
    - "tokens.json has two-tier structure: color.primitive + color.semantic"
    - "dark mode semantic tokens defined for both light and dark variants"
    - "tokens.json documents Phosphor icon set choice (DSGN-07)"
  artifacts:
    - path: "packages/design-tokens/tokens.json"
      provides: "Complete two-tier token system: primitives + semantics, light + dark, spacing + typography"
    - path: "packages/design-tokens/style-dictionary.config.mjs"
      provides: "Style Dictionary 4 config with custom ColorSwiftUI transform and css/tailwind-v4 format"
    - path: "packages/design-tokens/generated/WWDesignTokens.swift"
      provides: "SwiftUI Color constants for iOS — no UIColor"
    - path: "packages/design-tokens/generated/tokens.css"
      provides: "@theme block for Tailwind v4 utility class generation"
  key_links:
    - from: "packages/design-tokens/style-dictionary.config.mjs"
      to: "color/ColorSwiftUI transform"
      via: "transforms array override"
      pattern: "ColorSwiftUI"
    - from: "packages/design-tokens/style-dictionary.config.mjs"
      to: "css/tailwind-v4 custom format"
      via: "StyleDictionary.registerFormat"
      pattern: "tailwind-v4"
---

<objective>
Complete the Style Dictionary 4 token pipeline: expand tokens.json with a full two-tier (primitive → semantic) system including dark mode, spacing, and typography. Rebuild generated outputs. Verify the build test passes.

Purpose: Plan 02 created the Style Dictionary scaffold with placeholder tokens. This plan adds a complete token system that downstream phases (iOS Phase 5, Android Phase 7, Web Phase 8) can consume immediately. The build must produce four platform outputs with no UIColor/UIKit references in the Swift output.

Output: Complete tokens.json with two-tier primitive/semantic structure + dark mode. All four generated outputs rebuilt. Build test green.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand tokens.json with full two-tier system + dark mode + typography</name>
  <files>packages/design-tokens/tokens.json, packages/design-tokens/style-dictionary.config.mjs</files>
  <behavior>
    - Test 1: Build produces WWDesignTokens.swift with SwiftUI Color, not UIColor
    - Test 2: Build produces tokens.css with @theme block
    - Test 3: Build produces WWTheme.kt with Compose Color
    - Test 4: tokens.json does not directly reference color.primitive in semantic tokens (two-tier discipline)
  </behavior>
  <action>
Replace tokens.json with the full two-tier token system. This is a PLACEHOLDER palette — Plan 09 (brand kickoff) will replace these colors with the final brand palette.

**packages/design-tokens/tokens.json** (complete):
```json
{
  "_comment": "PLACEHOLDER PALETTE — Brand kickoff in Plan 09 will replace color primitives.",
  "color": {
    "primitive": {
      "brand": {
        "50":  { "value": "#FFF7ED", "type": "color" },
        "100": { "value": "#FFEDD5", "type": "color" },
        "200": { "value": "#FED7AA", "type": "color" },
        "300": { "value": "#FDBA74", "type": "color" },
        "400": { "value": "#FB923C", "type": "color" },
        "500": { "value": "#F97316", "type": "color" },
        "600": { "value": "#EA580C", "type": "color" },
        "700": { "value": "#C2410C", "type": "color" },
        "800": { "value": "#9A3412", "type": "color" },
        "900": { "value": "#7C2D12", "type": "color" }
      },
      "neutral": {
        "0":   { "value": "#FFFFFF", "type": "color" },
        "50":  { "value": "#F9FAFB", "type": "color" },
        "100": { "value": "#F3F4F6", "type": "color" },
        "200": { "value": "#E5E7EB", "type": "color" },
        "300": { "value": "#D1D5DB", "type": "color" },
        "400": { "value": "#9CA3AF", "type": "color" },
        "500": { "value": "#6B7280", "type": "color" },
        "600": { "value": "#4B5563", "type": "color" },
        "700": { "value": "#374151", "type": "color" },
        "800": { "value": "#1F2937", "type": "color" },
        "900": { "value": "#111827", "type": "color" },
        "950": { "value": "#030712", "type": "color" }
      },
      "success": {
        "500": { "value": "#22C55E", "type": "color" },
        "900": { "value": "#14532D", "type": "color" }
      },
      "error": {
        "500": { "value": "#EF4444", "type": "color" },
        "900": { "value": "#7F1D1D", "type": "color" }
      },
      "warning": {
        "400": { "value": "#FACC15", "type": "color" },
        "900": { "value": "#713F12", "type": "color" }
      }
    },
    "semantic": {
      "brand": {
        "primary":   { "value": "{color.primitive.brand.500}", "type": "color" },
        "secondary": { "value": "{color.primitive.brand.700}", "type": "color" },
        "accent":    { "value": "{color.primitive.brand.300}", "type": "color" }
      },
      "surface": {
        "app": {
          "light": { "value": "{color.primitive.neutral.50}",  "type": "color" },
          "dark":  { "value": "{color.primitive.neutral.950}", "type": "color" }
        },
        "raised": {
          "light": { "value": "{color.primitive.neutral.0}",   "type": "color" },
          "dark":  { "value": "{color.primitive.neutral.800}", "type": "color" }
        },
        "sunken": {
          "light": { "value": "{color.primitive.neutral.100}", "type": "color" },
          "dark":  { "value": "{color.primitive.neutral.900}", "type": "color" }
        }
      },
      "text": {
        "primary": {
          "light": { "value": "{color.primitive.neutral.900}", "type": "color" },
          "dark":  { "value": "{color.primitive.neutral.50}",  "type": "color" }
        },
        "secondary": {
          "light": { "value": "{color.primitive.neutral.600}", "type": "color" },
          "dark":  { "value": "{color.primitive.neutral.400}", "type": "color" }
        },
        "disabled": {
          "light": { "value": "{color.primitive.neutral.400}", "type": "color" },
          "dark":  { "value": "{color.primitive.neutral.600}", "type": "color" }
        }
      },
      "border": {
        "default": {
          "light": { "value": "{color.primitive.neutral.200}", "type": "color" },
          "dark":  { "value": "{color.primitive.neutral.700}", "type": "color" }
        }
      },
      "status": {
        "success": { "value": "{color.primitive.success.500}", "type": "color" },
        "error":   { "value": "{color.primitive.error.500}",   "type": "color" },
        "warning": { "value": "{color.primitive.warning.400}", "type": "color" }
      }
    }
  },
  "spacing": {
    "primitive": {
      "0":   { "value": "0px",   "type": "dimension" },
      "1":   { "value": "2px",   "type": "dimension" },
      "2":   { "value": "4px",   "type": "dimension" },
      "3":   { "value": "6px",   "type": "dimension" },
      "4":   { "value": "8px",   "type": "dimension" },
      "5":   { "value": "10px",  "type": "dimension" },
      "6":   { "value": "12px",  "type": "dimension" },
      "8":   { "value": "16px",  "type": "dimension" },
      "10":  { "value": "20px",  "type": "dimension" },
      "12":  { "value": "24px",  "type": "dimension" },
      "16":  { "value": "32px",  "type": "dimension" },
      "20":  { "value": "40px",  "type": "dimension" },
      "24":  { "value": "48px",  "type": "dimension" },
      "32":  { "value": "64px",  "type": "dimension" },
      "40":  { "value": "80px",  "type": "dimension" },
      "48":  { "value": "96px",  "type": "dimension" }
    }
  },
  "radius": {
    "primitive": {
      "none":  { "value": "0px",   "type": "dimension" },
      "sm":    { "value": "4px",   "type": "dimension" },
      "md":    { "value": "8px",   "type": "dimension" },
      "lg":    { "value": "12px",  "type": "dimension" },
      "xl":    { "value": "16px",  "type": "dimension" },
      "2xl":   { "value": "24px",  "type": "dimension" },
      "full":  { "value": "9999px","type": "dimension" }
    }
  },
  "iconography": {
    "_comment": "DSGN-07: Phosphor Icons chosen for cross-platform permissive licensing (MIT). phosphor-react for web, phosphor-swift for iOS, phosphor-compose for Android. See docs/design/ICONOGRAPHY.md",
    "library": { "value": "phosphor", "type": "other" },
    "defaultWeight": { "value": "regular", "type": "other" },
    "minTapTarget": { "value": "44px", "type": "dimension" }
  }
}
```

The `style-dictionary.config.mjs` from Plan 02 is already correct. Re-run the build to regenerate outputs with the expanded token set:
```bash
pnpm --filter @wonderwaltz/design-tokens build
```

CRITICAL — verify two pitfall fixes are in place before building:
1. Pitfall 5: Swift output uses `color/ColorSwiftUI` NOT `color/UIColorSwift` — check the transforms array in style-dictionary.config.mjs
2. Pitfall 6: CSS output uses `css/tailwind-v4` format (wraps in `@theme {}`) NOT `css/variables` format (which uses `:root {}`)

If style-dictionary.config.mjs is missing either fix, update it before running the build.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz && pnpm --filter @wonderwaltz/design-tokens build && grep -c "@theme" packages/design-tokens/generated/tokens.css && ! grep "UIColor" packages/design-tokens/generated/WWDesignTokens.swift && echo "Build verification passed"</automated>
  </verify>
  <done>tokens.json has complete two-tier primitive/semantic structure. Build generates all four outputs. tokens.css has @theme block. WWDesignTokens.swift has SwiftUI Color, not UIColor. Iconography library documented as Phosphor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete build test assertions</name>
  <files>packages/design-tokens/tests/build.test.ts</files>
  <behavior>
    - Test 1: WWDesignTokens.swift exists and is non-empty
    - Test 2: WWTheme.kt exists and is non-empty
    - Test 3: tokens.css exists and contains @theme
    - Test 4: tokens.ts exists and is non-empty
    - Test 5: WWDesignTokens.swift does NOT contain UIColor
    - Test 6: tokens.css does NOT contain ":root" as the wrapper (Tailwind v4 uses @theme)
    - Test 7: WWTheme.kt contains "object" keyword (compose/object format)
  </behavior>
  <action>
Replace the Plan 02 placeholder test file with comprehensive assertions:

**packages/design-tokens/tests/build.test.ts**:
```typescript
/**
 * Design token build verification.
 * Run: pnpm --filter @wonderwaltz/design-tokens build && vitest run packages/design-tokens/tests/
 *
 * SC-4a: pnpm --filter @wonderwaltz/design-tokens build && test -f packages/design-tokens/generated/WWDesignTokens.swift
 * SC-4b: test -f packages/design-tokens/generated/WWTheme.kt
 * SC-4c: test -f packages/design-tokens/generated/tokens.css && grep -c "@theme"
 */
import { describe, it, expect, beforeAll } from 'vitest';
import { existsSync, readFileSync } from 'fs';
import { execSync } from 'child_process';
import { resolve } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const PACKAGE_ROOT = resolve(__dirname, '..');
const GENERATED = resolve(PACKAGE_ROOT, 'generated');

beforeAll(() => {
  // Run the build before testing outputs
  execSync('node style-dictionary.config.mjs', {
    cwd: PACKAGE_ROOT,
    stdio: 'pipe',
  });
});

describe('Design token build — all four platform outputs', () => {
  it('generates WWDesignTokens.swift (Swift/iOS)', () => {
    const file = `${GENERATED}/WWDesignTokens.swift`;
    expect(existsSync(file)).toBe(true);
    const content = readFileSync(file, 'utf8');
    expect(content.length).toBeGreaterThan(100);
  });

  it('WWDesignTokens.swift uses SwiftUI Color, NOT UIKit UIColor (Pitfall 5)', () => {
    const content = readFileSync(`${GENERATED}/WWDesignTokens.swift`, 'utf8');
    expect(content).not.toContain('UIColor');
    // Should contain SwiftUI Color
    expect(content).toContain('Color(');
  });

  it('generates WWTheme.kt (Compose/Android)', () => {
    const file = `${GENERATED}/WWTheme.kt`;
    expect(existsSync(file)).toBe(true);
    const content = readFileSync(file, 'utf8');
    expect(content.length).toBeGreaterThan(100);
    expect(content).toContain('object'); // compose/object format
  });

  it('generates tokens.css with @theme block for Tailwind v4 (Pitfall 6)', () => {
    const file = `${GENERATED}/tokens.css`;
    expect(existsSync(file)).toBe(true);
    const content = readFileSync(file, 'utf8');
    expect(content).toContain('@theme');
    // Tailwind v4 uses @theme, NOT :root — verify correct format
    expect(content).not.toMatch(/^:root\s*\{/m);
  });

  it('generates tokens.ts (TypeScript)', () => {
    const file = `${GENERATED}/tokens.ts`;
    expect(existsSync(file)).toBe(true);
    const content = readFileSync(file, 'utf8');
    expect(content.length).toBeGreaterThan(50);
  });
});

describe('tokens.json structural validation', () => {
  it('has two-tier color structure (primitive + semantic)', () => {
    const tokens = JSON.parse(
      readFileSync(resolve(PACKAGE_ROOT, 'tokens.json'), 'utf8')
    );
    expect(tokens.color.primitive).toBeDefined();
    expect(tokens.color.semantic).toBeDefined();
  });

  it('has dark mode variants in semantic tokens', () => {
    const tokens = JSON.parse(
      readFileSync(resolve(PACKAGE_ROOT, 'tokens.json'), 'utf8')
    );
    expect(tokens.color.semantic.surface.app.light).toBeDefined();
    expect(tokens.color.semantic.surface.app.dark).toBeDefined();
  });

  it('iconography library is specified (DSGN-07)', () => {
    const tokens = JSON.parse(
      readFileSync(resolve(PACKAGE_ROOT, 'tokens.json'), 'utf8')
    );
    expect(tokens.iconography).toBeDefined();
    // Phosphor or Lucide — both are acceptable
    const lib = tokens.iconography.library.value;
    expect(['phosphor', 'lucide']).toContain(lib);
  });
});
```

Run the test: `pnpm --filter @wonderwaltz/design-tokens test`
All 8 tests should pass.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz && pnpm --filter @wonderwaltz/design-tokens test 2>&1 | tail -10</automated>
  </verify>
  <done>All 8 design token build tests pass. Tests cover: file existence, SwiftUI Color not UIColor, @theme not :root, two-tier structure, dark mode variants, iconography library declaration.</done>
</task>

</tasks>

<verification>
After all tasks complete:

```bash
cd /Users/vincentvillalta/Documents/Projects/WonderWaltz

# SC-4a: Swift output exists
pnpm --filter @wonderwaltz/design-tokens build && test -f packages/design-tokens/generated/WWDesignTokens.swift

# SC-4b: Compose output exists
test -f packages/design-tokens/generated/WWTheme.kt

# SC-4c: CSS output has @theme
test -f packages/design-tokens/generated/tokens.css && grep -c "@theme" packages/design-tokens/generated/tokens.css

# No UIColor in Swift output (Pitfall 5 verification)
! grep "UIColor" packages/design-tokens/generated/WWDesignTokens.swift && echo "UIColor check passed"

# Run build test suite
pnpm --filter @wonderwaltz/design-tokens test
```
</verification>

<success_criteria>
- tokens.json has complete two-tier (primitive/semantic) structure with dark mode variants for all surface, text, and border tokens
- Style Dictionary 4 build produces all four outputs without error
- WWDesignTokens.swift uses SwiftUI Color (not UIColor) — Pitfall 5 avoidance
- tokens.css uses @theme block (not :root) — Pitfall 6 avoidance
- tokens.json includes iconography.library = "phosphor" (DSGN-07)
- All 8 build tests pass
- Build test file contains real assertions (not todos)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md`
</output>
