---
phase: 01-foundation
plan: "01"
subsystem: infra
tags: [pnpm, turborepo, typescript, eslint, prettier, husky, commitlint, lint-staged, vitest]

# Dependency graph
requires: []
provides:
  - pnpm 10 workspaces with apps/* and packages/* globs
  - Turborepo v2 pipeline (tasks key) for build/lint/typecheck/test/dev
  - Shared tsconfig.base.json with strict/ES2022/NodeNext
  - ESLint 9 flat config with typescript-eslint recommendedTypeChecked
  - Prettier with singleQuote/trailingComma/printWidth 100
  - husky pre-commit (lint-staged) and commit-msg (commitlint) hooks
  - Root vitest.config.ts with projects array for per-package test isolation
  - Wave 0 test placeholder files for RLS, interceptor, and design tokens
affects:
  - 01-02
  - 01-03
  - 01-04
  - 01-05
  - 01-06
  - 01-07
  - 01-08
  - all downstream phases

# Tech tracking
tech-stack:
  added:
    - pnpm@10.33.0
    - turbo@2.9.6
    - typescript@6.0.2
    - eslint@9.39.4
    - typescript-eslint@8.58.1
    - "@eslint/js@9.39.4"
    - eslint-config-prettier@10.1.8
    - prettier@3.8.2
    - husky@9.1.7
    - lint-staged@15.5.2
    - "@commitlint/cli@19.8.1"
    - "@commitlint/config-conventional@19.8.1"
    - vitest@4.1.3
    - "@vitest/coverage-v8@4.1.3"
  patterns:
    - Turborepo v2 tasks key (NOT pipeline) for all build orchestration
    - Per-package tsconfig.json extending tsconfig.base.json
    - ESLint projectService for type-aware linting across workspaces
    - Conventional commits enforced via commitlint + husky commit-msg hook
    - lint-staged runs ESLint+Prettier on staged TS files only

key-files:
  created:
    - package.json
    - pnpm-workspace.yaml
    - pnpm-lock.yaml
    - .nvmrc
    - .node-version
    - .gitignore
    - turbo.json
    - tsconfig.base.json
    - tsconfig.json
    - eslint.config.mjs
    - .prettierrc.json
    - .prettierignore
    - commitlint.config.mjs
    - lint-staged.config.mjs
    - .husky/commit-msg
    - .husky/pre-commit
    - vitest.config.ts
    - packages/db/tsconfig.json
    - packages/design-tokens/tsconfig.json
    - apps/api/tsconfig.json
    - packages/db/tests/rls.integration.test.ts
    - apps/api/src/common/interceptors/response-envelope.interceptor.spec.ts
    - packages/design-tokens/tests/build.test.ts
  modified: []

key-decisions:
  - "Node 22 installed via nvm; pnpm enabled on both Node 20 and Node 22 via corepack for git hook compatibility"
  - "Root tsconfig.json added (not in plan) to satisfy ESLint projectService for vitest.config.ts and per-package test files"
  - "Per-package tsconfig.json files added to packages/db, packages/design-tokens, apps/api for Wave 0 test placeholder lint resolution"
  - "@eslint/js pinned to v9 (not v10) to match eslint@9 peer requirement"

patterns-established:
  - "Turbo v2: always use tasks key, never pipeline"
  - "Commit format: type(scope): lowercase subject - enforced by commitlint"
  - "ESLint requires tsconfig.json in each package for projectService type-aware linting"

requirements-completed:
  - FND-01
  - FND-02
  - FND-03
  - FND-12

# Metrics
duration: 355min
completed: 2026-04-10
---

# Phase 1 Plan 01: Monorepo Root Scaffold Summary

**pnpm workspaces + Turborepo v2 pipeline + strict TypeScript base + ESLint 9 flat config + Prettier + husky/commitlint/lint-staged + root Vitest config with Wave 0 test scaffolding**

## Performance

- **Duration:** ~355 min (includes Node 22 installation and pnpm bootstrap from scratch)
- **Started:** 2026-04-10T14:39:50Z
- **Completed:** 2026-04-10T20:15:00Z
- **Tasks:** 3 of 3
- **Files modified:** 23 created, 0 modified

## Accomplishments

- Monorepo root fully scaffolded: pnpm 10 workspaces, Turborepo v2 pipeline, shared TS base config
- All commit quality enforcement wired: commitlint blocks non-conventional commits, lint-staged auto-formats on commit
- Wave 0 test infrastructure laid down: root vitest.config.ts + placeholder test files for RLS, disclaimer interceptor, and design tokens

## Task Commits

Each task was committed atomically:

1. **Task 1: Root manifest + workspace + toolchain pins** - `5be4c74` (chore)
2. **Task 2: Turborepo + shared TypeScript config** - `922447e` (chore)
3. **Task 3: ESLint + Prettier + husky + commitlint + lint-staged + Vitest** - `967d4e8` (chore)

## Files Created/Modified

- `package.json` - Root manifest with engines (node>=22), packageManager (pnpm@10.33.0), workspace scripts
- `pnpm-workspace.yaml` - Workspace globs for apps/* and packages/* (excludes .planning)
- `pnpm-lock.yaml` - Lockfile generated by pnpm install
- `.nvmrc` / `.node-version` - Node 22 pin
- `.gitignore` - Standard Node ignores plus .turbo/, packages/design-tokens/generated/
- `turbo.json` - Turborepo v2 pipeline with tasks key (NOT pipeline) for build/lint/typecheck/test/dev
- `tsconfig.base.json` - Shared TypeScript config: strict, ES2022, NodeNext, exactOptionalPropertyTypes
- `tsconfig.json` - Root tsconfig extending base, includes vitest.config.ts
- `eslint.config.mjs` - ESLint 9 flat config with typescript-eslint recommendedTypeChecked + eslint-config-prettier
- `.prettierrc.json` - Prettier: singleQuote, trailingComma all, printWidth 100, semi, tabWidth 2
- `.prettierignore` - Excludes .planning/, node_modules/, pnpm-lock.yaml
- `commitlint.config.mjs` - Extends @commitlint/config-conventional, scope-empty: warn
- `lint-staged.config.mjs` - ESLint+Prettier on TS, Prettier on JS/JSON/YAML/MD
- `.husky/commit-msg` - Runs commitlint --edit $1
- `.husky/pre-commit` - Runs pnpm exec lint-staged
- `vitest.config.ts` - Root config with projects array pointing to packages/*/vitest.config.ts and apps/api/vitest.config.ts
- `packages/db/tsconfig.json` - Extends tsconfig.base.json, includes src/** and tests/**
- `packages/design-tokens/tsconfig.json` - Extends tsconfig.base.json, includes src/** and tests/**
- `apps/api/tsconfig.json` - Extends tsconfig.base.json, includes src/**
- `packages/db/tests/rls.integration.test.ts` - Wave 0 placeholder: RLS policy tests (implemented in Plan 06)
- `apps/api/src/common/interceptors/response-envelope.interceptor.spec.ts` - Wave 0 placeholder: disclaimer interceptor tests (implemented in Plan 10)
- `packages/design-tokens/tests/build.test.ts` - Wave 0 placeholder: design token build tests (implemented in Plan 08)

## Decisions Made

- **Node 22 via nvm:** Node 22 was not pre-installed; installed via `nvm install 22` and enabled pnpm via corepack on both Node 20 (git hook PATH) and Node 22 (dev PATH)
- **Root tsconfig.json added:** ESLint's `projectService` requires each TS file to be covered by a tsconfig. Added root `tsconfig.json` (not in original plan) and per-package tsconfigs to resolve ESLint parse errors on Wave 0 placeholder files
- **@eslint/js pinned to v9:** `@eslint/js@10` requires `eslint@^10` but we use `eslint@9`. Pinned `@eslint/js@^9.0.0` to avoid peer conflict

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added root tsconfig.json and per-package tsconfig.json files**
- **Found during:** Task 3 (ESLint flat config + husky hooks)
- **Issue:** ESLint `projectService: true` requires TS files to be covered by a tsconfig.json. Without it, lint-staged blocked the commit with 4 "not found by the project service" errors for vitest.config.ts and all 3 Wave 0 placeholder test files
- **Fix:** Created `tsconfig.json` at repo root (includes vitest.config.ts) and `tsconfig.json` in each of packages/db, packages/design-tokens, apps/api (include their respective test/src dirs)
- **Files modified:** tsconfig.json, packages/db/tsconfig.json, packages/design-tokens/tsconfig.json, apps/api/tsconfig.json
- **Verification:** `pnpm exec eslint vitest.config.ts packages/db/tests/rls.integration.test.ts` exits 0
- **Committed in:** 967d4e8 (Task 3 commit)

**2. [Rule 1 - Bug] Pinned @eslint/js to v9 to match eslint peer requirement**
- **Found during:** Task 3 (ESLint dependency installation)
- **Issue:** `pnpm add @eslint/js` resolved v10 which has `eslint@^10` as peer but we use eslint@9, causing peer conflict warning
- **Fix:** Ran `pnpm add -Dw @eslint/js@^9.0.0` to pin to v9 series
- **Files modified:** package.json, pnpm-lock.yaml
- **Verification:** No peer conflict warnings in pnpm install output
- **Committed in:** 967d4e8 (Task 3 commit)

**3. [Rule 3 - Blocking] Enabled pnpm on Node 20 (git hook PATH)**
- **Found during:** Task 3 (first commit attempt with husky hooks active)
- **Issue:** Git hooks run in the system PATH which uses Node 20 bin dir; pnpm was only installed under Node 22. The pre-commit hook exited with "pnpm: command not found"
- **Fix:** Ran `/Users/vincentvillalta/.nvm/versions/node/v20.17.0/bin/corepack enable && corepack prepare pnpm@10.33.0 --activate` to make pnpm available in Node 20's bin dir
- **Files modified:** None (system-level corepack state)
- **Verification:** Subsequent commit with husky hooks succeeded
- **Committed in:** 967d4e8 (Task 3 commit)

---

**Total deviations:** 3 auto-fixed (1 blocking config, 1 bug, 1 blocking path)
**Impact on plan:** All fixes necessary for the commit pipeline to function. No scope creep — the tsconfig files are minimal scaffolds downstream plans will extend.

## Issues Encountered

- First commit attempt with husky active failed twice (pnpm PATH + commitlint body-max-line-length). Both resolved inline without deferred work.

## User Setup Required

None - no external service configuration required for this plan.

## Next Phase Readiness

- Monorepo root is fully operational with quality enforcement active
- All downstream plans can add packages under `apps/*` or `packages/*` and immediately inherit build pipeline, linting, formatting, and test infrastructure
- Per-package tsconfig stubs exist for db, design-tokens, and api — Plan 02+ will add content

---
*Phase: 01-foundation*
*Completed: 2026-04-10*
