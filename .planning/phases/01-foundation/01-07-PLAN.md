---
phase: 01-foundation
plan: "07"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - docs/ops/SERVICES.md
autonomous: true
requirements:
  - FND-05
  - FND-06
  - FND-07
  - FND-08
  - FND-09
  - FND-10
  - FND-11
  - FND-12

user_setup:
  - service: supabase
    why: "Postgres + Auth + TimescaleDB + PostGIS extension (DB foundation for entire project)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project → Settings → Database → Connection string (direct, port 5432)"
      - name: SUPABASE_URL
        source: "Supabase Dashboard → Project → Settings → API → Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project → Settings → API → Project API keys → anon public"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project → Settings → API → Project API keys → service_role"
    dashboard_config:
      - task: "Enable TimescaleDB extension"
        location: "Supabase Dashboard → Database → Extensions → search 'timescaledb' → Enable"
      - task: "Enable PostGIS extension"
        location: "Supabase Dashboard → Database → Extensions → search 'postgis' → Enable (usually pre-enabled)"
      - task: "Enable anonymous sign-ins"
        location: "Supabase Dashboard → Authentication → Providers → Anonymous → Enable"
  - service: railway
    why: "Hosts NestJS API and BullMQ worker processes"
    env_vars:
      - name: RAILWAY_API_URL
        source: "Railway Dashboard → Project → Deployments → Service URL"
    dashboard_config:
      - task: "Create Railway project named 'wonderwaltz'"
        location: "Railway Dashboard → New Project → Empty Project"
      - task: "Create 'api' service from GitHub repo, start command: node dist/main.js, root directory: apps/api"
        location: "Railway Dashboard → New Service → GitHub Repo"
      - task: "Create 'worker' service from GitHub repo, start command: node dist/worker.js, root directory: apps/api"
        location: "Railway Dashboard → New Service → GitHub Repo"
      - task: "Add all env vars from .env.example to both Railway services"
        location: "Railway Dashboard → Service → Variables"
  - service: upstash
    why: "Serverless Redis for BullMQ queues and live wait-time cache"
    env_vars:
      - name: REDIS_URL
        source: "Upstash Console → Database → REST API → UPSTASH_REDIS_REST_URL (use ioredis-compatible URL)"
    dashboard_config:
      - task: "Create Redis database named 'wonderwaltz'"
        location: "Upstash Console → Create Database → Redis → Global → Named 'wonderwaltz'"
  - service: vercel
    why: "Hosts Next.js web app with production + preview deploys"
    env_vars:
      - name: VERCEL_URL
        source: "Vercel Dashboard → Project → Settings → Domains"
      - name: TURBO_TOKEN
        source: "Vercel Dashboard → Team → Settings → Tokens → Create token"
      - name: TURBO_TEAM
        source: "Vercel Dashboard → Team Settings → Team slug"
    dashboard_config:
      - task: "Import GitHub repo into Vercel, set root directory to apps/web"
        location: "Vercel Dashboard → Add New → Project → Import Git Repository"
      - task: "Add TURBO_TOKEN and TURBO_TEAM to GitHub Actions secrets"
        location: "GitHub → Repository → Settings → Secrets and variables → Actions"
  - service: revenuecat
    why: "IAP receipt validation and entitlement management"
    env_vars:
      - name: REVENUECAT_WEBHOOK_SECRET
        source: "RevenueCat Dashboard → Project → Integrations → Webhooks → Authorization header value"
    dashboard_config:
      - task: "Create RevenueCat project named 'WonderWaltz'"
        location: "RevenueCat Dashboard → New Project"
      - task: "Add iOS app (App Store Connect app ID)"
        location: "RevenueCat Dashboard → Project → Apps → Add App → iOS"
      - task: "Add Android app (Google Play package name: com.wonderwaltz)"
        location: "RevenueCat Dashboard → Project → Apps → Add App → Android"
      - task: "Configure webhook endpoint (POST /webhooks/revenuecat) after Plan 02 API is deployed"
        location: "RevenueCat Dashboard → Project → Integrations → Webhooks"
  - service: sentry
    why: "Error tracking and performance monitoring for all platforms"
    env_vars:
      - name: SENTRY_DSN_API
        source: "Sentry Dashboard → Projects → wonderwaltz-api → Settings → Client Keys → DSN"
      - name: SENTRY_DSN_WEB
        source: "Sentry Dashboard → Projects → wonderwaltz-web → Settings → Client Keys → DSN"
      - name: SENTRY_DSN_IOS
        source: "Sentry Dashboard → Projects → wonderwaltz-ios → Settings → Client Keys → DSN"
      - name: SENTRY_DSN_ANDROID
        source: "Sentry Dashboard → Projects → wonderwaltz-android → Settings → Client Keys → DSN"
    dashboard_config:
      - task: "Create 4 Sentry projects: wonderwaltz-api (Node.js), wonderwaltz-web (Next.js), wonderwaltz-ios (iOS/Swift), wonderwaltz-android (Android/Kotlin)"
        location: "Sentry Dashboard → Projects → Create Project"
  - service: posthog
    why: "Product analytics with event schema audit blocking guest age data"
    env_vars:
      - name: POSTHOG_KEY
        source: "PostHog Dashboard → Project → Project Settings → Project API key"
    dashboard_config:
      - task: "Create PostHog project named 'WonderWaltz'"
        location: "PostHog Dashboard → New Project"
      - task: "Create event schema audit rule blocking properties containing 'age_bracket', 'age', 'birthdate', 'dob' (LEGL-07, FND-11)"
        location: "PostHog Dashboard → Data Management → Schema → Property definitions → Add blocked properties"

must_haves:
  truths:
    - "Supabase project provisioned with TimescaleDB and PostGIS extensions enabled"
    - "Railway project has api and worker services configured"
    - "Upstash Redis database created and REDIS_URL is accessible"
    - "Vercel project linked to GitHub repo with apps/web as root"
    - "RevenueCat project has iOS and Android app configurations"
    - "4 Sentry projects created with DSNs"
    - "PostHog project created with blocked age properties"
    - "All env vars documented in docs/ops/SERVICES.md and .env.example"
    - "No secrets committed to git"
  artifacts:
    - path: "docs/ops/SERVICES.md"
      provides: "Service provisioning checklist and environment variable reference"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "DATABASE_URL environment variable"
      via: "@nestjs/config ConfigModule"
      pattern: "DATABASE_URL"
    - from: ".github/workflows/ci.yml"
      to: "TURBO_TOKEN + TURBO_TEAM"
      via: "GitHub Actions secrets"
      pattern: "secrets.TURBO_TOKEN"
---

<objective>
Provision all external services: Supabase (DB), Railway (API + Worker), Upstash Redis, Vercel (Web), RevenueCat (IAP), Sentry (4 projects), and PostHog. Document all environment variables.

Purpose: These services must exist before Phase 2 data ingestion can start. Provisioning is a human-required setup task — Claude documents what to do; the founder executes it. All env vars are documented in docs/ops/SERVICES.md and .env.example. No secrets are committed.

Output: docs/ops/SERVICES.md with complete provisioning checklist. .env.example updated with all service-specific vars. Services wired in the correct order (Supabase extensions first, then Railway, then observability).
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/ops/SERVICES.md provisioning guide</name>
  <files>docs/ops/SERVICES.md</files>
  <action>
Create a comprehensive provisioning document. This is the source of truth for service setup.

**docs/ops/SERVICES.md**:
```markdown
# External Services — WonderWaltz

All services must be provisioned before Phase 2 data ingestion starts.
Follow each section in order — dependencies are listed where relevant.

## 1. Supabase (Database + Auth)

**Purpose:** Postgres OLTP database with TimescaleDB for wait-time history,
PostGIS for attraction geolocation, and Supabase Auth for user accounts.

**Plan:** Free tier supports dev/staging. Upgrade to Pro ($25/mo) before Phase 10 launch.

### Steps:
1. Create project at https://supabase.com → New Project
   - Name: `wonderwaltz`
   - Database password: generate strong password, save securely
   - Region: US East (closest to WDW servers)

2. Enable extensions (Database → Extensions):
   - `timescaledb` → Enable (required for wait_times_history hypertable)
   - `postgis` → Enable (required for attraction location_point)
   - `uuid-ossp` → Enable (for UUID generation, usually pre-enabled)

3. Enable anonymous sign-ins (Authentication → Providers → Anonymous → Enable)

4. Collect env vars:
   - `DATABASE_URL`: Settings → Database → Connection string → Direct (port 5432)
   - `SUPABASE_URL`: Settings → API → Project URL
   - `SUPABASE_ANON_KEY`: Settings → API → anon public key
   - `SUPABASE_SERVICE_ROLE_KEY`: Settings → API → service_role key

5. Apply migrations:
   ```bash
   DATABASE_URL="<your-direct-connection-url>" \
     pnpm --filter @wonderwaltz/db exec drizzle-kit migrate
   ```

### Important Notes:
- Use the Direct connection URL (port 5432) for DATABASE_URL in Railway.
  Do NOT use the connection pooler URL (port 6543) for long-lived processes.
- The service_role key bypasses RLS. Never expose it to clients. Railway env var only.

---

## 2. Upstash Redis

**Purpose:** BullMQ queue backing + live wait-time cache (2-min TTL) +
rate-limit counters.

### Steps:
1. Create account at https://upstash.com
2. Create Redis database:
   - Name: `wonderwaltz`
   - Type: Global (multi-region for low latency)
   - Consistency: Eventual (sufficient for cache use)
3. Collect env var:
   - `REDIS_URL`: from the ioredis-compatible connection string
     (format: `redis://:password@hostname:port`)

---

## 3. Railway

**Purpose:** NestJS HTTP API and BullMQ worker processes.

### Steps:
1. Create project at https://railway.app → New Project → Empty Project
   - Name: `wonderwaltz`
2. Create `api` service:
   - Source: GitHub repo → `wonderwaltz`
   - Root Directory: `apps/api`
   - Start Command: `node dist/main.js`
   - Build Command: `cd ../.. && pnpm install && turbo run build --filter=@wonderwaltz/api...`
3. Create `worker` service (same repo, different entry point):
   - Root Directory: `apps/api`
   - Start Command: `node dist/worker.js` (Phase 2 will create worker.ts)
4. Add all env vars from `.env.example` to both services (Variables tab).

---

## 4. Vercel (Next.js Web)

**Purpose:** Marketing website + admin panel.

### Steps:
1. Import GitHub repo at https://vercel.com → Add New → Project
   - Root Directory: `apps/web`
   - Framework: Next.js (auto-detected)
2. Connect to Vercel team for Turborepo Remote Cache:
   ```bash
   npx turbo login
   npx turbo link
   ```
3. Add `TURBO_TOKEN` and `TURBO_TEAM` as GitHub Actions secrets:
   - GitHub → Repository → Settings → Secrets and variables → Actions
4. Add env vars to Vercel project (env vars from .env.example that start with NEXT_).

---

## 5. RevenueCat (IAP)

**Purpose:** Receipt validation and entitlement management for trip_unlock IAP.

### Steps:
1. Create account at https://www.revenuecat.com
2. Create project: `WonderWaltz`
3. Add iOS app:
   - Platform: App Store
   - App name: WonderWaltz
   - Bundle ID: com.wonderwaltz
   - App Store Connect API key (Phase 6)
4. Add Android app:
   - Platform: Google Play
   - Package name: com.wonderwaltz
   - Service account JSON (Phase 7)
5. Configure webhook after API is deployed (Phase 2+):
   - Endpoint: POST https://api.wonderwaltz.app/webhooks/revenuecat
   - Authorization header: generate secret → store as `REVENUECAT_WEBHOOK_SECRET`

---

## 6. Sentry (Error Tracking)

**Purpose:** Crash reporting and performance monitoring for all 4 platforms.

### Steps:
1. Create account at https://sentry.io
2. Create 4 projects:
   | Project Name | Platform | SDK |
   |---|---|---|
   | wonderwaltz-api | Node.js | @sentry/nestjs 10.47.0 |
   | wonderwaltz-web | Next.js | @sentry/nextjs 10.47.0 |
   | wonderwaltz-ios | iOS/Swift | sentry-cocoa 9.8.0 |
   | wonderwaltz-android | Android/Kotlin | sentry-android 8.38.0 |
3. Collect DSN for each project:
   - Settings → Client Keys → DSN
   - Store as: SENTRY_DSN_API, SENTRY_DSN_WEB, SENTRY_DSN_IOS, SENTRY_DSN_ANDROID

---

## 7. PostHog (Product Analytics)

**Purpose:** Event tracking with LEGL-07 enforcement (no guest age in events).

### Steps:
1. Create account at https://posthog.com
2. Create project: `WonderWaltz`
3. Set up blocked properties (LEGL-07 / FND-11):
   - Data Management → Property definitions
   - Mark as blocked: `age_bracket`, `guest_age`, `birthdate`, `dob`, `age`
   - Note: Only `has_young_children: boolean` is allowed (derived, not stored)
4. Collect env var:
   - `POSTHOG_KEY`: Project → Project Settings → Project API key
   - `POSTHOG_HOST`: `https://us.i.posthog.com` (US cloud)

---

## Environment Variables Reference

| Variable | Service | Where Used | Example Value |
|---|---|---|---|
| DATABASE_URL | Supabase | apps/api, packages/db | postgresql://postgres:pass@db.xxx.supabase.co:5432/postgres |
| SUPABASE_URL | Supabase | apps/api | https://xxx.supabase.co |
| SUPABASE_ANON_KEY | Supabase | apps/api (auth verification) | eyJ... |
| SUPABASE_SERVICE_ROLE_KEY | Supabase | apps/api (admin) | eyJ... |
| REDIS_URL | Upstash | apps/api | redis://:pass@xxx.upstash.io:6379 |
| SENTRY_DSN_API | Sentry | apps/api | https://xxx@o0.ingest.sentry.io/0 |
| SENTRY_DSN_WEB | Sentry | apps/web | https://xxx@o0.ingest.sentry.io/0 |
| SENTRY_DSN_IOS | Sentry | apps/ios | https://xxx@o0.ingest.sentry.io/0 |
| SENTRY_DSN_ANDROID | Sentry | apps/android | https://xxx@o0.ingest.sentry.io/0 |
| POSTHOG_KEY | PostHog | apps/api, apps/web | phc_xxx |
| POSTHOG_HOST | PostHog | apps/api, apps/web | https://us.i.posthog.com |
| REVENUECAT_WEBHOOK_SECRET | RevenueCat | apps/api | wh_secret_xxx |
| TURBO_TOKEN | Vercel | CI (GitHub Actions) | vc_xxx |
| TURBO_TEAM | Vercel | CI (GitHub Actions) | your-team-slug |

**Security rules:**
- Never commit any of these values to git
- Local dev: copy .env.example to .env.local and fill in values
- Railway: add all vars via the Railway dashboard Variables tab
- Vercel: add NEXT_PUBLIC_* vars in Vercel dashboard
- iOS/Android: use Xcode build settings / build config for DSN values
```
  </action>
  <verify>
    <automated>test -f /Users/vincentvillalta/Documents/Projects/WonderWaltz/docs/ops/SERVICES.md && grep "TimescaleDB" /Users/vincentvillalta/Documents/Projects/WonderWaltz/docs/ops/SERVICES.md && echo "SERVICES.md created"</automated>
  </verify>
  <done>docs/ops/SERVICES.md exists with provisioning steps for all 7 services. Environment variable reference table covers all required vars. Security rules documented.</done>
</task>

<task type="checkpoint:human-action">
  <what-built>Complete provisioning documentation in docs/ops/SERVICES.md covering: Supabase (DB + Auth + TimescaleDB + PostGIS), Upstash Redis, Railway (API + Worker), Vercel (Web + Turbo cache), RevenueCat (iOS + Android), Sentry (4 projects), PostHog (with LEGL-07 event schema audit).</what-built>
  <how-to-verify>
Follow docs/ops/SERVICES.md in order:

1. **Supabase** — Create project, enable TimescaleDB + PostGIS extensions, enable anonymous sign-ins. Copy DATABASE_URL, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY to .env.local

2. **Upstash Redis** — Create Redis database. Copy REDIS_URL to .env.local

3. **Railway** — Create project with `api` and `worker` services. Add env vars from .env.local.

4. **Vercel** — Import repo, set root to apps/web. Run `npx turbo login && npx turbo link`. Add TURBO_TOKEN and TURBO_TEAM to GitHub Actions secrets.

5. **RevenueCat** — Create project with iOS (com.wonderwaltz) and Android (com.wonderwaltz) apps. Copy REVENUECAT_WEBHOOK_SECRET.

6. **Sentry** — Create 4 projects (api, web, ios, android). Copy all 4 DSNs.

7. **PostHog** — Create project. Add blocked properties: age_bracket, guest_age, birthdate, dob, age. Copy POSTHOG_KEY.

8. Apply Supabase migrations: `supabase start && DATABASE_URL="..." pnpm --filter @wonderwaltz/db exec drizzle-kit migrate`

9. Verify TimescaleDB hypertable: `psql $DATABASE_URL -c "SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name='wait_times_history'"`

After provisioning, update .env.local with all real values (never commit this file).
  </how-to-verify>
  <resume-signal>Type "provisioned" when all 7 services are set up and .env.local is populated, or describe what's blocked</resume-signal>
</task>

</tasks>

<verification>
After provisioning complete:

```bash
# Verify docs exist
test -f docs/ops/SERVICES.md

# Verify .env.local has been populated (should NOT be committed)
test -f .env.local && echo ".env.local exists locally"

# Verify .env.local is in .gitignore
grep "\.env\.local" .gitignore

# If supabase start is running and migrations applied:
# psql $DATABASE_URL -c "SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name='wait_times_history'"
# (Should return 1 row)
```
</verification>

<success_criteria>
- docs/ops/SERVICES.md exists with provisioning steps for all 7 external services
- All environment variables documented in both SERVICES.md and .env.example
- .env.local is in .gitignore (no secrets in git — FND-12)
- Supabase project provisioned with TimescaleDB + PostGIS extensions enabled
- Railway project has api and worker service configurations
- Upstash Redis database accessible
- Vercel project linked to repo; TURBO_TOKEN and TURBO_TEAM set as GitHub Actions secrets
- RevenueCat project has iOS and Android app configurations
- 4 Sentry projects created with DSNs
- PostHog project with blocked age properties
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
