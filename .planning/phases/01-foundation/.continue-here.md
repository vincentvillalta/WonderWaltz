---
phase: 01-foundation
task: 9
total_tasks: 11
status: paused_pending_mcp
last_updated: 2026-04-14T12:11:13.155Z
---

<current_state>
Phase 01 is 10/11 plans complete. Plan 01-09 (Brand Direction & Design Docs)
is deferred because the original plan called for the ui-designer agent to
produce three brand directions, but the decision was made on 2026-04-14 to
have Figma MCP drive brand/UI/tokens instead. The session is being restarted
so the freshly-configured Figma MCP server is picked up on next boot.

Task 1 of 01-09 already committed (`54098de`) — three ui-designer
directions written to `docs/design/BRAND.md`. That file is now disposable
reference material; real brand lock will come from Figma.
</current_state>

<completed_work>

- 01-01 through 01-08: Complete (monorepo, apps scaffold, CI, DB schema + migrations, services doc, design tokens pipeline)
- 01-10: Complete — NestJS response envelope interceptor (LEGL-02/03)
- 01-11: Complete — WDW catalog YAML + idempotent seed script

**Infra provisioned this session:**
- Supabase project connected via MCP; 21 tables, RLS policies, pg_cron jobs all applied
- TimescaleDB replaced with materialized view + pg_cron (Supabase Cloud dropped Timescale)
- `.env.local` populated with Supabase/Upstash/Sentry/PostHog credentials
- `.mcp.json` now contains Supabase MCP server config
</completed_work>

<remaining_work>

- **01-09 (deferred):** Restructure the plan to pull brand tokens from Figma via MCP
  instead of running ui-designer. Todo file: `.planning/todos/pending/restructure-01-09-figma-mcp.md`
  - Connect Figma MCP (pending this restart)
  - Rewrite `01-09-PLAN.md`:
    - Drop Task 1 (ui-designer exploration — already committed, to be overwritten)
    - New Task 1: pull tokens from Figma via MCP → `tokens.json`
    - New Task 2: rebuild Style Dictionary outputs (Swift/Kotlin/CSS/TS)
    - New Task 3: write BRAND.md as pointer to Figma + create COMPONENTS.md, ACCESSIBILITY.md, ICONOGRAPHY.md
  - Execute the restructured plan
- After 01-09 completes: run `/gsd:verify-work 01` to close out phase
- Requirements that MUST still be satisfied by the new 01-09: DSGN-01, DSGN-02, DSGN-05, DSGN-06, DSGN-08
</remaining_work>

<decisions_made>

- **Brand/UI source of truth = Figma (via MCP), not ui-designer agent.** Rationale:
  Figma AI will own design generation and iteration; manual agent-produced brand
  directions duplicate that work and create drift between code and design file.
- **TimescaleDB removed from stack.** Supabase Cloud no longer offers it. Replaced
  with standard materialized view (`wait_times_1h`) refreshed hourly via pg_cron,
  plus a daily pg_cron job for 2-year data retention. Schema comments and
  migration files 0001/0002 updated to reflect this; no production impact since
  DB was never using hypertables (no data yet).
- **`.mcp.json` is project-scoped and committed.** Currently contains Supabase;
  Figma server config will be added here so it travels with the repo.
- **Phase 01 paused rather than abandoned.** 01-09 is a hard dependency for
  Phase 02 completeness (DSGN-01..08 coverage must be ≥95% in REQUIREMENTS.md),
  so skipping it is not an option.
</decisions_made>

<blockers>
- **Figma MCP not yet visible in tool registry.** Added during this session but
  requires a session restart to register. This is WHY we're pausing.
- **TURBO_TOKEN / TURBO_TEAM not filled in.** User could not locate Vercel team
  slug and token. Not blocking phase 01 completion — remote caching is an
  optimization, not a gate. Revisit when Vercel team exists.
- **Supabase anonymous sign-ins not yet enabled.** Requires manual toggle in
  Supabase dashboard (Authentication → Providers → Anonymous). Not blocking
  phase 01 but blocks Phase 04 auth flows.
</blockers>

<context>
We're in a clean pause state. All code committed. `.env.local` populated.
Supabase schema deployed. Design tokens build. API envelope working.

When the session restarts, the FIRST thing to check is whether Figma MCP
tools are visible. Search for tools with names matching `mcp__figma*` or
`mcp__figma-dev-mode*`. If visible → proceed to restructure 01-09. If not
visible → diagnose the .mcp.json or Claude Desktop config before any work.

Mental model: Phase 01 is a "foundation" phase — it provisions infrastructure
and establishes patterns. Everything except the brand/design surface is locked.
Once Figma is wired in and 01-09 lands, we verify the phase and move to
Phase 02 (data pipeline / wait-time ingestion), which has no design dependency
but DOES need the catalog seed (01-11, already complete) to attach rides to.

Current `docs/design/BRAND.md` contents are three ui-designer alternatives
(Vintage Travel Poster / Warm Modern Minimalism / Painterly Whimsy). Treat
as reference only — the Figma file will be canonical.
</context>

<next_action>
1. Verify Figma MCP is connected:
   - Use ToolSearch: query "figma" with max_results=10
   - If tools appear (e.g. `mcp__figma__get_file`, `mcp__figma-dev-mode-mcp-server__*`), proceed
   - If no tools: read `.mcp.json`, check Claude Desktop/IDE MCP settings, diagnose
2. Ask user for the Figma file URL/key and which page/frame contains brand tokens
3. Read `.planning/todos/pending/restructure-01-09-figma-mcp.md` and
   `.planning/phases/01-foundation/01-09-PLAN.md` to understand what needs rewriting
4. Restructure 01-09-PLAN.md per the todo spec (keep requirement coverage intact)
5. Run `/gsd:execute-phase 01` — it will skip completed plans and pick up at 01-09
6. After 01-09 green: `/gsd:verify-work 01` to close out the phase
</next_action>
