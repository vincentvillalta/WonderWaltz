---
phase: 03-engine
plan: 07
type: execute
wave: 4
depends_on: [03-05, 03-06]
files_modified:
  - packages/solver/src/score.ts
  - packages/solver/src/construct.ts
  - packages/solver/src/meals.ts
  - packages/solver/src/shows.ts
  - packages/solver/src/index.ts
  - packages/solver/tests/score.test.ts
  - packages/solver/tests/construct-pinning.test.ts
  - packages/solver/tests/meals.test.ts
  - packages/solver/tests/shows.test.ts
autonomous: true
requirements: [SOLV-03, SOLV-05, SOLV-06]
must_haves:
  truths:
    - "`score(attraction, context)` returns a deterministic number from the formula `enjoyment_weight / (time_cost + wait_cost + walk_cost)` with equal weights"
    - "Greedy construction places must-do attractions FIRST as hard pins at their optimal time window, then fills by score"
    - "Table-service dining reservations are hard pins; quick-service meals are inserted in rides-free windows"
    - "Parades/fireworks/shows are scored as OPTIONAL blocks — kept in plan only if score > insertion cost"
    - "Construction is deterministic: same filtered-attractions + guests + forecasts input → byte-identical ordering"
  artifacts:
    - path: "packages/solver/src/score.ts"
      provides: "score() + individual cost helpers"
    - path: "packages/solver/src/construct.ts"
      provides: "constructDay(filteredAttractions, guests, mustDo, forecasts, walkingGraph, parkHours) → PlanItem[]"
    - path: "packages/solver/src/meals.ts"
      provides: "insertMeals(items, tableServiceReservations, guests) → PlanItem[]"
    - path: "packages/solver/src/shows.ts"
      provides: "insertShows(items, scheduledShows, preferences) → PlanItem[]"
  key_links:
    - from: "packages/solver/src/construct.ts"
      to: "packages/solver/src/score.ts"
      via: "score() called in greedy loop"
      pattern: "score\\("
    - from: "packages/solver/src/construct.ts"
      to: "packages/solver/src/walkingGraph.ts"
      via: "shortestPath for walk_cost"
      pattern: "shortestPath"
---

<objective>
Ship the solver's greedy-construction core: scoring function, must-do pinning, meal insertion, show/parade/fireworks scoring. Local search + fatigue + LL allocation land in 03-08 and 03-09.

Output:
  - Pure scoring + construction functions
  - Unit tests per concern (scoring, pinning, meals, shows)
  - Construction output is ordered and time-coherent (no overlapping items)
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-RESEARCH.md
@packages/solver/src/types.ts
@packages/solver/src/walkingGraph.ts
@packages/solver/src/filter.ts

<interfaces>
From packages/solver/src/types.ts:
- SolverGuest, SolverPreferences, Attraction, Dining, Show, WalkingGraph, PlanItem, DayPlan

From packages/solver/src/walkingGraph.ts:
- shortestPath(graph, fromNodeId, toNodeId): number // seconds
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scoring function + cost helpers</name>
  <files>packages/solver/src/score.ts, packages/solver/tests/score.test.ts, packages/solver/src/index.ts</files>
  <behavior>
    - `score({ attraction, predictedWaitMinutes, walkSeconds, timeBudgetMinutes, confidence }): number`
    - Formula: `enjoyment_weight / (time_cost + wait_cost + walk_cost)` where each cost multiplier = 1.0.
    - `enjoyment_weight = attraction.enjoymentScore` (from YAML, 1-100; headliners 80+).
    - `time_cost = attraction.rideDurationMinutes + 5` (5 min staging).
    - `wait_cost = predictedWaitMinutes * (confidence === 'low' ? 1.2 : 1.0)` per CONTEXT.md.
    - `walk_cost = walkSeconds / 60`.
    - Pure — no randomness, no side effects.
    - Returns a finite number (never NaN/Infinity) given valid inputs.
  </behavior>
  <action>
    1. Write `score.ts` with the formula + typed input record.
    2. `score.test.ts` table-driven with 6+ canonical rows:
       - Headliner low wait → high score
       - Headliner high wait → low score
       - Low-intensity + low wait → moderate score
       - Low-confidence wait bucket adds 1.2× penalty (same input except confidence → score decreases)
       - Same inputs → byte-identical score (determinism)
    3. Export from index.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/score.test.ts --run</automated>
  </verify>
  <done>Score function deterministic and table-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: constructDay greedy + must-do hard pinning</name>
  <files>packages/solver/src/construct.ts, packages/solver/tests/construct-pinning.test.ts, packages/solver/src/index.ts</files>
  <behavior>
    - `constructDay({ filteredAttractions, mustDoAttractionIds, guests, parkHours, walkingGraph, forecastFn, date }): PlanItem[]`
    - Must-do attractions placed FIRST in their optimal time window: query `forecastFn(rideId, ts)` for every 30-min slot in park hours; pick the slot with minimum predicted wait; pin there.
    - After pinning, iterate over remaining time slots by time order; at each unfilled slot, pick the highest-scoring remaining attraction that fits (including walk time from previous item).
    - No overlapping items; items sorted by `startTime` ascending.
    - Attractions not scheduled by end of day are dropped (not queued).
    - Deterministic: same inputs → byte-identical output. Tie-breaking by `attraction.id` lexicographic.
  </behavior>
  <action>
    1. Implement in `construct.ts`:
       - Sort park hours into 30-min slots.
       - For each must-do ID, query forecastFn for every slot, pick min-wait, pin.
       - Greedy fill: for each remaining slot, compute score for each remaining attraction using the walk-cost from the most-recent-placed item; pick max; place; advance cursor by `rideDurationMinutes + walk_minutes`.
       - Return `PlanItem[]` with deterministic IDs: `planItemId = sha256(attractionId + startTime).slice(0, 16)`.
    2. Tests in `construct-pinning.test.ts`:
       - Must-do pinned to optimal window (forecasted low-wait slot) even when it's not the start of day.
       - Greedy places highest-scoring remaining attraction next.
       - Determinism: same input × 10 runs → identical output.
       - Tie-breaking by id-lex when scores equal.
       - Empty must-do + empty filtered → empty day plan (no crash).
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/construct-pinning.test.ts --run</automated>
  </verify>
  <done>Construction deterministic; pinning places must-dos at forecast-optimal windows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Meal + show/parade/fireworks insertion</name>
  <files>packages/solver/src/meals.ts, packages/solver/src/shows.ts, packages/solver/tests/meals.test.ts, packages/solver/tests/shows.test.ts, packages/solver/src/index.ts</files>
  <behavior>
    - `insertMeals(items, tableServiceReservations, guests, budgetTier): PlanItem[]`
      - Table-service reservations are hard pins at their reserved time; surrounding attractions shift around them.
      - Quick-service meals inserted in rides-free windows (>60-min gap) — tagged `notes: 'Mobile order recommended'`.
    - `insertShows(items, showsInPark, preferredShowIds, parkHours): PlanItem[]`
      - Preferred shows with fixed schedules (parades, fireworks) scored as optional blocks: keep if `showScore > displacementCost` (where displacement = score loss of attractions that must move to accommodate).
      - Non-preferred shows → skipped.
    - Both are pure, deterministic.
  </behavior>
  <action>
    1. `meals.ts`:
       - TS reservations: insert as `{ type: 'dining', startTime: res.time, endTime: res.time + 90min }` and remove conflicting attractions.
       - QS meals: find the largest gap > 60 min between 11:00-13:30 (lunch) and 17:00-19:30 (dinner); insert `{ type: 'dining', ... }` if budget allows meals there.
    2. `shows.ts`:
       - For each preferred show, compute `showScore = show.enjoymentWeight` vs `displacementCost = sum of scores of attractions whose start times fall within show window`.
       - Keep show if net positive.
    3. Tests:
       - `meals.test.ts`: TS reservation at 13:00 → that slot is dining; surrounding attractions don't overlap.
       - `meals.test.ts`: No TS, budget tier 'fairy' → QS meal inserted in a 60+ min gap.
       - `shows.test.ts`: Preferred fireworks at 21:00 → kept; non-preferred parade → skipped.
       - `shows.test.ts`: Preferred show whose window conflicts with high-scoring attraction → show skipped if net cost > benefit.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/meals.test.ts tests/shows.test.ts --run</automated>
  </verify>
  <done>Meal and show insertion deterministic and table-tested.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/solver test --run` green
</verification>

<success_criteria>
- SOLV-03, SOLV-05, SOLV-06 each have unit test coverage.
- Construction output is time-coherent (no overlaps).
</success_criteria>

<output>
`.planning/phases/03-engine/03-07-SUMMARY.md` — scoring formula, pinning algorithm, meal/show insertion rules.
</output>
