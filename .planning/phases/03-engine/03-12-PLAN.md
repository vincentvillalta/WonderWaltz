---
phase: 03-engine
plan: 12
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - apps/api/src/narrative/prompt.ts
  - apps/api/src/narrative/schema.ts
  - apps/api/src/narrative/narrative.service.ts
  - apps/api/tests/narrative/prompt-cache-prefix.test.ts
  - apps/api/tests/narrative/zod-schema.test.ts
  - apps/api/tests/narrative/narrative-service.test.ts
  - apps/api/tests/narrative/ride-id-contract.test.ts
autonomous: true
requirements: [LLM-02, LLM-04]
must_haves:
  truths:
    - "CACHED_PREFIX (catalog + BRAND voice) is byte-stable across repeated builds — critical for Anthropic prompt cache hit rate"
    - "NarrativeService sends static CACHED_PREFIX with `cache_control: { type: 'ephemeral' }` BEFORE the dynamic trip context"
    - "Zod schema validates narrative output: days[].intro, items[].tip, packingDelta[], budgetHacks[]"
    - "Contract test: narrative.items[].planItemId is a subset of solver output plan item IDs — hallucinated references are rejected"
    - "On Zod validation failure: retry once, then persist plan with empty narrative + `narrative_available: false` flag"
  artifacts:
    - path: "apps/api/src/narrative/prompt.ts"
      provides: "buildCachedPrefix() + buildDynamicPrompt(trip, solverOutput) — deterministic"
    - path: "apps/api/src/narrative/schema.ts"
      provides: "NarrativeResponseSchema (Zod) + validateNarrative(response, solverOutput)"
    - path: "apps/api/src/narrative/narrative.service.ts"
      provides: "Full generate() + generateRethinkIntro() with retry + fallback"
  key_links:
    - from: "apps/api/src/narrative/narrative.service.ts"
      to: "apps/api/src/narrative/prompt.ts"
      via: "buildCachedPrefix → Anthropic system prompt with cache_control"
      pattern: "cache_control"
    - from: "apps/api/src/narrative/narrative.service.ts"
      to: "apps/api/src/narrative/schema.ts"
      via: "NarrativeResponseSchema.parse + validateNarrative"
      pattern: "NarrativeResponseSchema"
---

<objective>
Implement the narrative generation pipeline: byte-stable cached prefix, Zod-validated structured output, hallucination-rejection contract test, retry-once + fallback-to-empty-narrative failure mode.

Output:
  - `prompt.ts` building the CACHED_PREFIX and dynamic suffix
  - `schema.ts` with Zod schema + ride-ID-subset contract validator
  - Full `NarrativeService.generate()` with all failure paths
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@apps/api/src/narrative/narrative.module.ts
@apps/api/src/narrative/narrative.service.ts
@apps/api/tests/anthropic-mock.ts
@apps/api/tests/fixtures/narrative-response.json
@apps/api/tests/fixtures/narrative-response.invalid-ride.json
@packages/content/wdw/attractions.yaml
@docs/design/BRAND.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildCachedPrefix — byte-stable across builds</name>
  <files>apps/api/src/narrative/prompt.ts, apps/api/tests/narrative/prompt-cache-prefix.test.ts</files>
  <behavior>
    - `buildCachedPrefix(): string` returns the cached portion: WDW catalog (51 attractions + 38 dining + 30 resorts) + BRAND voice guide + tone rules.
    - Byte-stable: calling twice returns the exact same string (same reads from YAML, deterministic order).
    - Size: 5-8K tokens (roughly 20-32K chars).
    - `buildDynamicPrompt(trip, guests, solverOutput): string` returns the dynamic suffix with guest context + solver plan items.
    - `buildMessagesPayload({ model, cachedPrefix, dynamicPrompt }): { model, system, messages }` shapes the Anthropic call with `cache_control: { type: 'ephemeral' }` on the system block.
  </behavior>
  <action>
    1. Implement `prompt.ts`:
       - `loadCatalog()` reads YAML files, sorts entries by id, produces a stable newline-delimited block.
       - `loadBrandVoice()` reads `docs/design/BRAND.md` relevant sections (voice, tone, examples). If reads are file-system-based, cache in module scope but verify byte-stability.
       - `buildCachedPrefix()` concatenates in fixed order: `<CATALOG>\n...\n</CATALOG>\n<BRAND_VOICE>\n...\n</BRAND_VOICE>\n<TONE_RULES>\n...\n</TONE_RULES>`.
       - `buildMessagesPayload` per behavior with `cache_control: { type: 'ephemeral' }` on system.
    2. `prompt-cache-prefix.test.ts`:
       - Call `buildCachedPrefix()` 100 times; assert all results byte-identical (SHA-256).
       - Assert length is between 15K-40K chars (loose but bounded).
       - Assert includes known attraction name, known dining name, known BRAND tone example.
       - Assert NOT including any dynamic fields (no trip IDs, no guest data).
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/narrative/prompt-cache-prefix.test.ts --run</automated>
  </verify>
  <done>Cached prefix byte-stable across 100 builds; contains catalog + brand + tone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zod schema + ride-ID contract validator</name>
  <files>apps/api/src/narrative/schema.ts, apps/api/tests/narrative/zod-schema.test.ts, apps/api/tests/narrative/ride-id-contract.test.ts</files>
  <behavior>
    - `NarrativeResponseSchema` (Zod):
      ```ts
      z.object({
        days: z.array(z.object({
          dayIndex: z.number().int().nonnegative(),
          intro: z.string().min(50).max(800),
          items: z.array(z.object({
            planItemId: z.string(),
            tip: z.string().min(10).max(400),
          })),
        })),
        packingDelta: z.array(z.object({ item: z.string(), reason: z.string() })),
        budgetHacks: z.array(z.string()),
      })
      ```
    - `validateNarrative(response, solverOutput): { ok: true, data } | { ok: false, error }`
      - Runs `NarrativeResponseSchema.safeParse(response)`.
      - If parse ok: collect all `planItemId` from response, collect all item IDs from solverOutput, assert response IDs ⊆ solver IDs. If not subset, return `ok: false, error: 'hallucinated_ride'` with details.
  </behavior>
  <action>
    1. Implement schema + validator.
    2. `zod-schema.test.ts` — table-driven:
       - Valid fixture → ok.
       - Missing `intro` → error.
       - Empty days → ok (edge case).
       - `intro` too short (<50 chars) → error.
    3. `ride-id-contract.test.ts`:
       - Load `narrative-response.json` fixture + solver fixture → subset passes.
       - Load `narrative-response.invalid-ride.json` (has a planItemId NOT in solver output) → `ok: false, error: 'hallucinated_ride'`.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/narrative/zod-schema.test.ts tests/narrative/ride-id-contract.test.ts --run</automated>
  </verify>
  <done>Schema + contract validator prevent hallucinated rides.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NarrativeService.generate — full pipeline with retry + fallback</name>
  <files>apps/api/src/narrative/narrative.service.ts, apps/api/tests/narrative/narrative-service.test.ts</files>
  <behavior>
    - `generate({ trip, guests, solverOutput, model }): Promise<{ narrative?: NarrativeResponse, narrativeAvailable: boolean, usage: AnthropicUsage }>`
      1. Build payload (CACHED_PREFIX + dynamic).
      2. Call Anthropic SDK (mocked in tests).
      3. Parse `response.content[0].text` as JSON.
      4. Run `validateNarrative` against solverOutput.
      5. On success → return `{ narrative, narrativeAvailable: true, usage }`.
      6. On failure (JSON parse or validation) → retry ONCE with `system: <original>\n\nYour previous response failed validation: <error>. Retry.` appended.
      7. If retry fails → return `{ narrative: undefined, narrativeAvailable: false, usage }`.
    - Usage returned is the sum across all attempts (input_tok, cached_read_tok, output_tok).
    - `generateRethinkIntro({ trip, solverOutput, completedItemIds })` is a thin variant using Haiku + intro-only (no per-item tips).
  </behavior>
  <action>
    1. Implement `generate()` per behavior.
    2. Implement `generateRethinkIntro()` — uses `claude-haiku-4-5` and a different schema that has only `{ intro: string }`.
    3. `narrative-service.test.ts`:
       - Happy path: mock returns valid fixture → `narrativeAvailable: true`, usage reflects 1 call.
       - Zod failure once then success: mock returns invalid first, valid on second → `narrativeAvailable: true`, usage reflects 2 calls.
       - Zod failure twice: mock returns invalid both times → `narrativeAvailable: false`, usage reflects 2 calls.
       - Hallucinated ride fixture: rejected by contract test, retried, still fails → `narrativeAvailable: false`.
       - Model passthrough: `generate({ model: 'claude-haiku-4-5' })` → mock receives `'claude-haiku-4-5'`.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/narrative/narrative-service.test.ts --run</automated>
  </verify>
  <done>Full retry+fallback pipeline green; LLM-04 failure modes exercised.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test -- tests/narrative --run` green
- `pnpm -r typecheck` clean
</verification>

<success_criteria>
- LLM-02 (cache prefix stable) + LLM-04 (Zod + ride-ID subset) fully automated.
- All failure modes exercised (parse, validation, hallucination).
</success_criteria>

<output>
`.planning/phases/03-engine/03-12-SUMMARY.md` — cached prefix structure, Zod schema, retry semantics.
</output>
