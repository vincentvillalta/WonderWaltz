---
phase: 03-engine
plan: 08
type: execute
wave: 5
depends_on: [03-07]
files_modified:
  - packages/solver/src/localSearch.ts
  - packages/solver/src/resources.ts
  - packages/solver/src/lightningLane.ts
  - packages/solver/src/parkHours.ts
  - packages/solver/src/index.ts
  - packages/solver/tests/local-search.test.ts
  - packages/solver/tests/ll-allocation.test.ts
  - packages/solver/tests/das.test.ts
  - packages/solver/tests/park-hours.test.ts
autonomous: true
requirements: [SOLV-04, SOLV-08, SOLV-09]
must_haves:
  truths:
    - "Adjacent-pair swap local search: iterate adjacent pairs, swap if total score improves, single pass, terminates deterministically"
    - "LL allocator: top-N scored rides → assign LL to longest-wait ones within that set, respecting per-day caps"
    - "DAS flag enables a parallel Resource pool with the same per-day math as LL"
    - "On-property hotels get +30 min Early Entry; Deluxe/Deluxe-Villa get Extended Evening Hours on eligible nights"
  artifacts:
    - path: "packages/solver/src/localSearch.ts"
      provides: "adjacentPairSwap(items, scoreFn) → improved items"
    - path: "packages/solver/src/resources.ts"
      provides: "ResourcePool type + allocator (LLMP + LLSP + DAS share the allocator)"
    - path: "packages/solver/src/lightningLane.ts"
      provides: "allocateLL(day, attractions, forecasts, tier, dasEnabled)"
    - path: "packages/solver/src/parkHours.ts"
      provides: "resolveParkHours(date, parkId, lodgingType) → {open, close}"
  key_links:
    - from: "packages/solver/src/lightningLane.ts"
      to: "packages/solver/src/resources.ts"
      via: "ResourcePool.allocate()"
      pattern: "ResourcePool"
    - from: "packages/solver/src/parkHours.ts"
      to: "lodgingType"
      via: "Early Entry / EEH expansion"
      pattern: "lodgingType"
---

<objective>
Ship SOLV-04 (LL allocation), SOLV-08 (DAS as LL-equivalent resource), SOLV-09 (on-property Early Entry + EEH), plus the adjacent-pair swap local search layer on top of Plan 03-07's construction.

Output:
  - Local search (single-pass, deterministic)
  - Shared Resource pool for LLMP / LLSP / DAS
  - Park-hours expansion based on lodging type
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@packages/solver/src/types.ts
@packages/solver/src/construct.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adjacent-pair swap local search</name>
  <files>packages/solver/src/localSearch.ts, packages/solver/tests/local-search.test.ts, packages/solver/src/index.ts</files>
  <behavior>
    - `adjacentPairSwap(items: PlanItem[], totalScoreFn: (items) => number): PlanItem[]`
    - Single linear pass: for i in 0..n-2, swap items[i] and items[i+1] if `totalScoreFn(swapped) > totalScoreFn(original)`, keep; continue.
    - No recursion, no multi-pass (determinism + speed).
    - Hard pins (attractions with `metadata.pinned === true` OR items of type `'dining'` with TS reservation) are NEVER swapped.
    - Must-do attractions whose time was set by forecast are NOT pinned for this purpose — they can shift.
  </behavior>
  <action>
    1. Implement `localSearch.ts` per behavior.
    2. `local-search.test.ts`:
       - 3 items [A(high-wait first), B(low-wait second), C(high-score last)] → after swap, A and B order improves.
       - Pinned item stays in place (TS dining at 13:00 does not move).
       - Same input → byte-identical output (determinism).
       - Zero-improvement case: already-optimal ordering → no changes.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/local-search.test.ts --run</automated>
  </verify>
  <done>Local search deterministic and table-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared ResourcePool + LL allocator + DAS path</name>
  <files>packages/solver/src/resources.ts, packages/solver/src/lightningLane.ts, packages/solver/tests/ll-allocation.test.ts, packages/solver/tests/das.test.ts, packages/solver/src/index.ts</files>
  <behavior>
    - `ResourcePool`: `{ kind: 'LLMP' | 'LLSP' | 'DAS', capacityPerDay: number, allocations: Map<rideId, returnWindow> }`.
    - `allocateLL({ day, filteredAttractions, forecasts, budgetTier, dasEnabled }): { itemsWithLL: PlanItem[], warnings: string[] }`
      - LLMP capacity: 3 (all tiers eligible for LLMP rides).
      - LLSP capacity: 0 (pixie) / 1 (fairy) / 2 (royal).
      - DAS: if `dasEnabled === true`, adds a separate pool with capacity 3, pulling from same eligible-ride set (DAS available for standby waits, typically longer).
    - Selection rule: filter to top-10 scored rides in day → assign LL slots to the longest-wait rides within that filtered set.
    - For must-do rides that don't get an LL slot: emit warning `"Upgrade to {nextTier} for LL access on {ride.name}"` when applicable.
    - 90-minute fixed return-window offset from booking time (pre-park use; rethink-today overrides with `active_ll_bookings`).
  </behavior>
  <action>
    1. `resources.ts`: `ResourcePool` class with `allocate(rideId): returnWindow | null` that decrements capacity.
    2. `lightningLane.ts`:
       - Filter day items to top-N scored.
       - For each pool (LLMP, LLSP, DAS in that order), pick the longest-predicted-wait ride from the top-N set that this pool can handle (LLMP pool only handles rides with `lightning_lane_type === 'multi_pass'`; LLSP only `single_pass`; DAS any).
       - Mark PlanItem with `lightningLaneType` field; compute `returnWindow = bookingTime + 90min`.
       - Return warnings for unmet must-dos.
    3. `ll-allocation.test.ts`:
       - Fairy tier + 5 candidate rides → 3 LLMP + 1 LLSP assigned; 1 without.
       - Pixie tier → 0 LLSP; LLMP only.
       - Royal tier + 2 single-pass eligible → 2 LLSP assigned.
       - Must-do without budget → warning emitted with tier-upgrade text.
    4. `das.test.ts`:
       - `dasEnabled: true` + fairy tier → DAS pool + LLMP pool both used; total resource count ≈ 3+3 = 6 effective.
       - `dasEnabled: false` → no DAS pool.
       - DAS narrative label on allocated items differs from LL label.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/ll-allocation.test.ts tests/das.test.ts --run</automated>
  </verify>
  <done>LL + DAS allocator deterministic; per-tier caps enforced; warnings emitted correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Park hours resolution with on-property Early Entry + EEH</name>
  <files>packages/solver/src/parkHours.ts, packages/solver/tests/park-hours.test.ts, packages/solver/src/index.ts</files>
  <behavior>
    - `resolveParkHours({ date, parkId, lodgingType, baseHours, eehNights }): { open: string, close: string }`
    - `lodgingType: 'off_property' | 'value' | 'moderate' | 'deluxe' | 'deluxe_villa'`
    - Base hours come from `parks.yaml` seasonal schedule OR `themeparks.wiki` ingestion (Phase 2 data; plan 03-11 forecast uses it).
    - On-property (any `value`|`moderate`|`deluxe`|`deluxe_villa`): `open -= 30min` (Early Entry).
    - Deluxe or Deluxe-Villa: if `eehNights` contains `date`, `close += 2h` (Extended Evening Hours, ~2 hours per eligible night).
    - Off-property: no adjustments.
  </behavior>
  <action>
    1. Implement `parkHours.ts`.
    2. `park-hours.test.ts` table-driven:
       - Off-property + MK 9-22 → 9-22.
       - Moderate + MK 9-22 → 8:30-22 (Early Entry).
       - Deluxe + MK 9-22 on EEH night → 8:30-24 (EE + EEH).
       - Deluxe + MK on non-EEH night → 8:30-22 (EE only).
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/park-hours.test.ts --run</automated>
  </verify>
  <done>SOLV-09 automated; EE + EEH applied only to eligible lodging types.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/solver test --run` green (LL, DAS, park hours, local search)
</verification>

<success_criteria>
- SOLV-04, SOLV-08, SOLV-09 each have independent automated signals.
- ResourcePool is the shared allocator for all three (LL flavors + DAS).
</success_criteria>

<output>
`.planning/phases/03-engine/03-08-SUMMARY.md` — ResourcePool shape, LL selection rule, EE/EEH math.
</output>
