---
phase: 03-engine
plan: 17
type: execute
wave: 8
depends_on: [03-16, 03-03]
files_modified:
  - apps/api/src/trips/trips.controller.ts
  - apps/api/src/trips/trips.module.ts
  - apps/api/src/plans/plans.controller.ts
  - apps/api/src/plans/plans.service.ts
  - apps/api/src/plans/plans.module.ts
  - apps/api/src/app.module.ts
  - apps/api/tests/e2e/generate-plan.e2e.test.ts
  - apps/api/tests/e2e/get-plan-projection.e2e.test.ts
  - apps/api/tests/e2e/rethink-today.e2e.test.ts
autonomous: true
requirements: [PLAN-04]
must_haves:
  truths:
    - "POST /v1/trips/:id/generate-plan returns 202 with { plan_job_id } and enqueues a BullMQ job"
    - "POST /v1/trips/:id/rethink-today returns 202 with { plan_job_id }; accepts RethinkRequestDto (current_time, completed_item_ids, active_ll_bookings)"
    - "GET /v1/plans/:id returns PlanDto with discriminated-union days (FullDayPlan | LockedDayPlan) based on trips.entitlement_state"
    - "Free tier: Day 1 full + Days 2+ locked (templated headline from solver, no LLM)"
    - "Unlocked tier: all days full"
    - "Rethink in-progress inference: if current_time falls within an unfinished item's window, it's pinned"
    - "active_ll_bookings become hard pins in the solver"
    - "Budget-exhausted: endpoints return 402 with PlanBudgetExhaustedDto"
    - "End-to-end integration test: POST generate-plan → job completes → GET plans/:id returns full structured plan within 30s"
  artifacts:
    - path: "apps/api/src/trips/trips.controller.ts"
      provides: "generatePlan + rethinkToday endpoint bodies"
    - path: "apps/api/src/plans/plans.controller.ts"
      provides: "GET /v1/plans/:id with entitlement projection"
    - path: "apps/api/src/plans/plans.service.ts"
      provides: "projectForEntitlement(plan, entitlementState) → Full/Locked days"
  key_links:
    - from: "apps/api/src/trips/trips.controller.ts"
      to: "plan-generation queue"
      via: "@InjectQueue('plan-generation') queue.add(...)"
      pattern: "plan-generation"
    - from: "apps/api/src/plans/plans.service.ts"
      to: "trips.entitlement_state"
      via: "SELECT entitlement_state FROM trips"
      pattern: "entitlement_state"
---

<objective>
Wire the three HTTP endpoints — the user-visible surface of Phase 3 — and prove the full generate→poll→view round trip via an e2e integration test.

Output:
  - `POST /v1/trips/:id/generate-plan` (202 enqueue)
  - `POST /v1/trips/:id/rethink-today` (202 enqueue + rethink-specific orchestration)
  - `GET /v1/plans/:id` (projection by entitlement)
  - E2E integration test covering the full flow
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@apps/api/src/trips/trips.controller.ts
@apps/api/src/shared/dto/plan.dto.ts
@apps/api/src/shared/dto/rethink.dto.ts
@apps/api/src/shared/dto/plan-budget-exhausted.dto.ts
@apps/api/src/plan-generation/plan-generation.service.ts
@apps/api/src/plan-generation/rate-limit.guard.ts
@apps/api/src/plan-generation/circuit-breaker.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: trips.controller — generatePlan + rethinkToday</name>
  <files>apps/api/src/trips/trips.controller.ts, apps/api/src/trips/trips.module.ts, apps/api/tests/e2e/generate-plan.e2e.test.ts, apps/api/tests/e2e/rethink-today.e2e.test.ts</files>
  <behavior>
    - `POST /v1/trips/:id/generate-plan`:
      1. `@UseGuards(RateLimitGuard)` with `@RateLimit('free-tier-lifetime')` (if trip.entitlement_state === 'free').
      2. `CircuitBreakerService.checkBudget(tripId, projectedCents = 0)` — if already over budget, return 402 immediately.
      3. Enqueue job: `queue.add('generate', { tripId, kind: 'initial' })` with attempts=5, backoff=fixed 30s.
      4. Return `202 { plan_job_id: job.id }`.
    - `POST /v1/trips/:id/rethink-today`:
      1. `@UseGuards(RateLimitGuard)` with `@RateLimit('rethink')`.
      2. `CircuitBreakerService.checkBudget` same as above.
      3. Validate `RethinkRequestDto` (class-validator).
      4. Compute in-progress item: find solver item where `current_time ∈ [startTime, endTime]` AND `id NOT IN completed_item_ids` → mark pinned.
      5. Convert `active_ll_bookings` to solver hard pins.
      6. Enqueue `{ tripId, kind: 'rethink', rethinkInput }`.
      7. Return `202 { plan_job_id }`.
    - Over-budget: return 402 with `PlanBudgetExhaustedDto`.
    - Rate-limited: 429 (rethink) or 403 (free-tier).
  </behavior>
  <action>
    1. Replace the 501 stub bodies in `trips.controller.ts` with full implementations.
    2. Register `TripsModule` imports: `BullModule.registerQueue('plan-generation')`, `PlanGenerationModule`.
    3. `generate-plan.e2e.test.ts` using `@nestjs/testing` + supertest:
       - POST /v1/trips/{id}/generate-plan with valid trip → 202 + `{ plan_job_id }`.
       - Trip with spent=55 budget=50 → 402 + PlanBudgetExhaustedDto body shape.
       - Free-tier user on 4th call → 403.
       - BullMQ queue receives job with correct data (spy on queue.add).
    4. `rethink-today.e2e.test.ts`:
       - Valid rethink body → 202.
       - In-progress inference: current_time=13:00 with solver item 12:30-13:30 not completed → job data includes pin for that item.
       - active_ll_bookings: 2 bookings → job data includes 2 hard pins.
       - 16th rethink in a day → 429.
       - Invalid body (missing current_time) → 400.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/e2e/generate-plan.e2e.test.ts tests/e2e/rethink-today.e2e.test.ts --run</automated>
  </verify>
  <done>Both POST endpoints return correct status codes; rate limits + budget + rethink semantics all verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: plans.controller + plans.service with entitlement projection</name>
  <files>apps/api/src/plans/plans.controller.ts, apps/api/src/plans/plans.service.ts, apps/api/src/plans/plans.module.ts, apps/api/src/app.module.ts, apps/api/tests/e2e/get-plan-projection.e2e.test.ts</files>
  <behavior>
    - `GET /v1/plans/:id` returns envelope `{ data: PlanDto }` with discriminated-union days.
    - Projection logic (`plans.service.ts`):
      1. Load plan + planDays + planItems from DB.
      2. Check `trips.entitlement_state`:
         - `'unlocked'` or `'paid'`: return all days as FullDayPlan.
         - `'free'` or default: Day 0 = FullDayPlan; Days 1+ = LockedDayPlan with `{ type: 'locked', dayIndex, park, totalItems: planDay.items.length, headline: templatedHeadline(planDay), unlockTeaser: "Upgrade to see your full multi-day plan" }`.
      3. `templatedHeadline(planDay)`: `"Your ${park.name} ${trip.budgetTier} day centers on ${topScoredItem.name}."` — NO LLM call; deterministic from solver output.
      4. Inject `plan.meta.forecast_disclaimer` if any day has low-confidence bucket.
      5. Inject `plan.warnings` from solver output.
    - 404 if plan not found.
  </behavior>
  <action>
    1. Create `plans.controller.ts` + `plans.service.ts` + `plans.module.ts`. Register in `AppModule`.
    2. `plans.service.projectForEntitlement(plan, entitlementState, trip)` — deterministic projection.
    3. `get-plan-projection.e2e.test.ts`:
       - Unlocked trip: all days return `type: 'full'`.
       - Free-tier: Day 0 full; Days 1+ locked with templated headline.
       - Locked day's totalItems counts rides + meals + shows + rest (everything).
       - Plan with low-confidence day → `meta.forecast_disclaimer: 'Beta Forecast'`.
       - 404 on unknown plan id.
       - Response envelope shape matches OpenAPI snapshot.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/e2e/get-plan-projection.e2e.test.ts --run</automated>
  </verify>
  <done>Projection correct for both tiers; 404 works; envelope shape matches snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end integration — POST generate-plan → poll → GET plans/:id</name>
  <files>apps/api/tests/e2e/plan-roundtrip.e2e.test.ts</files>
  <behavior>
    - Full flow test:
      1. POST /v1/trips/{id}/generate-plan → 202 + plan_job_id.
      2. Drive BullMQ queue processor in-test (no external worker) by calling `queue.processJob` or similar helper.
      3. Wait for job completion (with timeout 30s).
      4. GET /v1/plans/{planId} → 200 with full PlanDto; at least 1 FullDayPlan with items, narrative, warnings array.
      5. Assert total round-trip latency < 30s.
    - Use the 3-day fixture from plan 03-10 to keep runtime within CI budget.
    - Run against mocked Anthropic (via `createAnthropicMock`) + mocked DB (use testcontainers or pg-mem per Phase 2 pattern).
  </behavior>
  <action>
    1. Write `plan-roundtrip.e2e.test.ts`:
       - Spin up full NestJS app with `Test.createTestingModule` including AppModule + WorkerModule.
       - Override ANTHROPIC_CLIENT_TOKEN with mock.
       - Seed DB with a 3-day trip + catalog + walking graph + minimal wait history.
       - Execute the POST → process-job → GET sequence.
       - Assert PlanDto structure matches OpenAPI snapshot.
       - Assert `llm_costs` has at least 1 row (per-day call count).
       - Assert runtime < 30s.
    2. If BullMQ can't be driven in-process cleanly, use a direct `plan-generation.service.generate(tripId)` call and assert the PlanDto is retrievable via the HTTP endpoint — the 202 path is covered by the task-1 e2e test already.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/e2e/plan-roundtrip.e2e.test.ts --run</automated>
  </verify>
  <done>Full generate → poll → view flow green within 30s budget.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test -- tests/e2e --run` green
- `pnpm --filter @wonderwaltz/api run build` clean
- OpenAPI snapshot test unchanged
</verification>

<success_criteria>
- PLAN-04 fully automated.
- E2E round trip proves the phase goal: real plan generated within 30s.
</success_criteria>

<output>
`.planning/phases/03-engine/03-17-SUMMARY.md` — endpoint shapes, projection logic, e2e timing data.
</output>
