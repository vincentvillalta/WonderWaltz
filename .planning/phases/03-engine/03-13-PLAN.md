---
phase: 03-engine
plan: 13
type: execute
wave: 4
depends_on: [03-01, 03-12]
files_modified:
  - apps/api/src/narrative/cost.ts
  - apps/api/src/narrative/cost-alert.service.ts
  - apps/api/src/narrative/narrative.service.ts
  - apps/api/src/narrative/narrative.module.ts
  - apps/api/tests/narrative/cost.test.ts
  - apps/api/tests/narrative/cost-hit-rate-alert.test.ts
autonomous: true
requirements: [LLM-05, LLM-06]
must_haves:
  truths:
    - "Every LLM call writes one row to `llm_costs` with `trip_id, plan_id, model, input_tok, cached_read_tok, output_tok, usd_cents, created_at`"
    - "USD cost computed from pinned per-model rate card (Sonnet vs Haiku); cached reads discounted"
    - "Cache hit rate = `cached_read_tok / (cached_read_tok + input_tok)`; computed over a 1-hour rolling window"
    - "When rolling hit rate drops below 70%, Sentry captureException + Slack alert fire"
  artifacts:
    - path: "apps/api/src/narrative/cost.ts"
      provides: "calculateUsdCents(usage, model) + recordLlmCost(db, row) helpers"
    - path: "apps/api/src/narrative/cost-alert.service.ts"
      provides: "Rolling hit-rate check + Sentry/Slack alerting"
  key_links:
    - from: "apps/api/src/narrative/narrative.service.ts"
      to: "apps/api/src/narrative/cost.ts"
      via: "recordLlmCost after each call"
      pattern: "recordLlmCost"
    - from: "apps/api/src/narrative/cost-alert.service.ts"
      to: "@sentry/nestjs + SlackAlerterService"
      via: "captureException + slackAlerter.sendAlert"
      pattern: "Sentry\\.captureException|slackAlerter"
---

<objective>
Ship LLM-05 (cost telemetry) + LLM-06 (cache-hit-rate alert). Every LLM call must write a row; cost math is deterministic; hit-rate alert runs on a 1-hour rolling window.

Output:
  - Cost calculation helper
  - `recordLlmCost` integrated into NarrativeService after every call
  - Background cron (or on-write check) that evaluates rolling hit rate and alerts
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@apps/api/src/narrative/narrative.service.ts
@apps/api/src/alerting/slack-alerter.service.ts
@packages/db/src/schema/entitlements.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cost calculation + recordLlmCost DB write</name>
  <files>apps/api/src/narrative/cost.ts, apps/api/src/narrative/narrative.service.ts, apps/api/src/narrative/narrative.module.ts, apps/api/tests/narrative/cost.test.ts</files>
  <behavior>
    - `calculateUsdCents({ model, input_tokens, cache_creation_input_tokens, cache_read_input_tokens, output_tokens }): number`
    - Rate card (USD per 1M tokens, pinned as of 2026-04-15):
      - Sonnet (`claude-sonnet-4-6`): input $3.00, cache_write $3.75, cache_read $0.30, output $15.00.
      - Haiku (`claude-haiku-4-5`): input $0.80, cache_write $1.00, cache_read $0.08, output $4.00.
    - Returns rounded-up integer cents.
    - `recordLlmCost(db, row): Promise<void>` INSERTs into `llm_costs`. Row shape matches existing Drizzle `llmCosts` schema.
    - `NarrativeService.generate()` calls `recordLlmCost` after every Anthropic call (both the first attempt AND retry — two rows if retry happened).
  </behavior>
  <action>
    1. Implement `cost.ts` with the rate card as a frozen constant.
    2. Modify `NarrativeService.generate` + `generateRethinkIntro` to accept `{ tripId, planId }` in their call context and call `recordLlmCost` after every Anthropic response (including retry).
    3. `cost.test.ts` table-driven:
       - Sonnet: input 5000 (0 cached) + output 800 → usd_cents = ceil((5000 × 3 + 800 × 15) / 1000) = 27 cents (3.0e-6 × 5000 = 0.015, 15e-6 × 800 = 0.012, total $0.027).
       - Sonnet with cache hit: input 200 + cache_read 4800 + output 700 → usd_cents ≈ 0.02 × 200/1000 + 0.3 × 4800 / 1000000 × 100 + ... (verify exact math in test).
       - Haiku: input 3000 + output 400 → 0.5 cents.
    4. Update `narrative-service.test.ts` from plan 03-12 to assert `db.execute` was called for `INSERT INTO llm_costs` after each response. Use mocked DB_TOKEN.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/narrative/cost.test.ts tests/narrative/narrative-service.test.ts --run</automated>
  </verify>
  <done>Cost math exact; every LLM call writes a row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rolling hit-rate alert + Sentry/Slack</name>
  <files>apps/api/src/narrative/cost-alert.service.ts, apps/api/src/narrative/narrative.module.ts, apps/api/tests/narrative/cost-hit-rate-alert.test.ts</files>
  <behavior>
    - `CostAlertService.checkHitRate(): Promise<{ rate, windowRows, alerted }>`
    - Runs on a cron (`@nestjs/schedule` or BullMQ scheduler — pick consistent with Phase 2 pattern); every 15 minutes.
    - Query: `SELECT SUM(cached_read_tok) AS cached, SUM(input_tok) AS input, COUNT(*) AS rows FROM llm_costs WHERE created_at > NOW() - INTERVAL '1 hour'`.
    - `rate = cached / (cached + input)`.
    - If `rows >= 5` (enough signal) AND `rate < 0.70` → fire Sentry exception + Slack alert with structured fields.
    - Rate-limit alerts to once per hour (Redis key `cost-alert:last-fired` with 1h TTL).
  </behavior>
  <action>
    1. Implement `CostAlertService` injecting `DB_TOKEN`, `REDIS_CLIENT_TOKEN`, `SlackAlerterService`.
    2. Use `@Cron('*/15 * * * *')` from `@nestjs/schedule` — check if installed; if not, use a BullMQ scheduler job.
    3. `cost-hit-rate-alert.test.ts`:
       - DB mock returns cached=100, input=900 (10% hit rate), rows=10 → Sentry + Slack called.
       - DB mock returns cached=800, input=200 (80% hit rate) → no alert.
       - Same low rate on immediate second call → alert skipped (dedup).
       - rows < 5 → no alert (insufficient signal).
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/narrative/cost-hit-rate-alert.test.ts --run</automated>
  </verify>
  <done>Hit-rate alert fires correctly; dedup works; Sentry + Slack both called.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test -- tests/narrative --run` green
</verification>

<success_criteria>
- LLM-05 + LLM-06 fully automated.
- Cost rows present from call #1.
</success_criteria>

<output>
`.planning/phases/03-engine/03-13-SUMMARY.md` — rate card, alert thresholds, dedup pattern.
</output>
