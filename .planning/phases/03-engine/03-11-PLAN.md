---
phase: 03-engine
plan: 11
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/api/src/forecast/forecast.module.ts
  - apps/api/src/forecast/forecast.service.ts
  - apps/api/src/forecast/calendar.service.ts
  - packages/content/wdw/calendar-rules.ts
  - apps/api/src/app.module.ts
  - apps/api/tests/forecast/forecast.service.test.ts
  - apps/api/tests/forecast/calendar.service.test.ts
  - apps/api/tests/forecast/calendar-rules.test.ts
  - apps/api/tests/forecast/confidence.test.ts
  - apps/api/tests/forecast/beta-framing.test.ts
autonomous: true
requirements: [FC-01, FC-03, FC-04, FC-05]
must_haves:
  truths:
    - "`ForecastModule.predictWait(rideId, ts): Promise<{ minutes, confidence }>` returns a result from bucketed median of `wait_times_1h` grouped by (ride_id, dow, hour_of_day, crowd_level_bucket)"
    - "When history is insufficient (confidence `low`), returns `attraction.baseline_wait_minutes` instead of noisy bucket median"
    - "Confidence label derived from sample count + weeks-of-history: `high` (8+ weeks AND >50 samples), `medium` (4-8 weeks), `low` (<4 weeks)"
    - "`CalendarService.getBucket(date): 'low'|'medium'|'high'|'peak'` hybrid: rule-engine default + DB override (crowd_calendar table wins)"
    - "Plan response metadata includes `forecast_disclaimer: 'Beta Forecast'` when any day of the plan has any `low`-confidence bucket"
  artifacts:
    - path: "apps/api/src/forecast/forecast.service.ts"
      provides: "predictWait(rideId, ts) — DB query on wait_times_1h + baseline fallback"
    - path: "apps/api/src/forecast/calendar.service.ts"
      provides: "getBucket(date) — DB-first, rule-engine fallback"
    - path: "packages/content/wdw/calendar-rules.ts"
      provides: "Pure rule engine — weekends, federal holidays, school holidays"
  key_links:
    - from: "apps/api/src/forecast/forecast.service.ts"
      to: "wait_times_1h continuous aggregate"
      via: "percentile_cont / median over (ride_id, dow, hour, bucket)"
      pattern: "wait_times_1h"
    - from: "apps/api/src/forecast/calendar.service.ts"
      to: "crowd_calendar table"
      via: "SELECT bucket FROM crowd_calendar WHERE date = $1"
      pattern: "crowd_calendar"
---

<objective>
Ship FC-01, FC-03, FC-04, FC-05. The forecast module has two pieces: (1) `predictWait` bucketed-median over TimescaleDB continuous aggregate with baseline fallback, (2) `CalendarService` hybrid rule-engine + DB override producing crowd buckets.

Output:
  - `ForecastModule` with `predictWait` + confidence labeling
  - `CalendarService` for crowd bucket resolution
  - Pure `calendar-rules.ts` rule engine in `packages/content/wdw`
  - Beta Forecast framing surfaced in plan metadata
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@packages/db/src/schema/timeseries.ts
@apps/api/src/ingestion/queue-times.service.ts
@packages/db/migrations/0002_timescale_continuous_agg.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure calendar-rules engine + CalendarService DB override</name>
  <files>packages/content/wdw/calendar-rules.ts, apps/api/src/forecast/calendar.service.ts, apps/api/tests/forecast/calendar-rules.test.ts, apps/api/tests/forecast/calendar.service.test.ts</files>
  <behavior>
    - `calendar-rules.ts`: pure function `getRuleBucket(date: Date): 'low'|'medium'|'high'|'peak'`
      - Peak: Christmas week (Dec 23-Jan 2), Spring Break 2 weeks, July 4 week, Thanksgiving week.
      - High: weekends, federal holidays, marathon weekends (first weekend of Jan), Food & Wine Festival weeks (mid-Sep to mid-Nov).
      - Medium: all other non-holiday weekdays May-August.
      - Low: weekdays Jan-Feb (excluding holidays), Sep-early-Dec weekdays.
      - Uses `date-holidays` for US federal holidays (install if not present — per research).
    - `CalendarService.getBucket(date)`:
      1. Query `crowd_calendar` by date.
      2. If a row exists → return its bucket (admin override wins).
      3. Else → call `getRuleBucket(date)`.
    - Deterministic; no network calls.
  </behavior>
  <action>
    1. Install `date-holidays` if missing: `pnpm --filter @wonderwaltz/content add date-holidays`.
    2. Implement `calendar-rules.ts` as a pure function with the heuristics above.
    3. Implement `CalendarService` (Nest injectable) using `DB_TOKEN` with the duck-typed executable pattern.
    4. Tests:
       - `calendar-rules.test.ts` table-driven: `2026-12-25` → peak; `2026-07-04` → peak; `2026-09-15 (weekday)` → high (Food & Wine); `2026-02-10 (weekday)` → low; `2026-06-15 (weekday)` → medium.
       - `calendar.service.test.ts`: override in crowd_calendar (mocked DB) wins over rule default; no override → rule default returned.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/forecast/calendar-rules.test.ts tests/forecast/calendar.service.test.ts --run</automated>
  </verify>
  <done>Rule engine table-tested; DB override verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ForecastService.predictWait bucketed median + confidence + baseline fallback</name>
  <files>apps/api/src/forecast/forecast.module.ts, apps/api/src/forecast/forecast.service.ts, apps/api/src/app.module.ts, apps/api/tests/forecast/forecast.service.test.ts, apps/api/tests/forecast/confidence.test.ts</files>
  <behavior>
    - `ForecastService.predictWait(rideId: string, targetTs: Date): Promise<{ minutes: number, confidence: 'high'|'medium'|'low' }>`
    - Query: `SELECT percentile_cont(0.5) WITHIN GROUP (ORDER BY minutes) AS median, COUNT(*) AS samples FROM wait_times_1h WHERE ride_id = $1 AND EXTRACT(DOW FROM bucket) = $2 AND EXTRACT(HOUR FROM bucket) = $3 AND crowd_bucket = $4`
      - `crowd_bucket` joined via `CalendarService.getBucket(bucket::date)` in a subquery OR by materializing bucket-per-day.
    - Confidence: `high` if samples > 50 AND weeks-of-history >= 8; `medium` if samples > 20 AND weeks >= 4; else `low`.
    - If confidence === `low` OR samples < 5: look up `attractions.baseline_wait_minutes` for rideId and return `{ minutes: baseline, confidence: 'low' }`.
    - `ForecastModule` exports `ForecastService`; registered in `AppModule`.
  </behavior>
  <action>
    1. Implement `ForecastService` using `DB_TOKEN`. Compute weeks-of-history via `MIN(bucket) FROM wait_times_1h WHERE ride_id = $1`.
    2. Implement baseline fallback — read `baseline_wait_minutes` from `attractions` table.
    3. `forecast.module.ts`: `@Module({ imports: [], providers: [ForecastService, CalendarService], exports: [ForecastService] })`. Register in AppModule.
    4. `forecast.service.test.ts` (integration, mocked DB):
       - DB returns 100 samples × 10 weeks → confidence `high`, median returned.
       - DB returns 30 samples × 5 weeks → confidence `medium`, median returned.
       - DB returns 3 samples → confidence `low`, baseline used.
       - DB returns 0 rows → baseline used, confidence `low`.
    5. `confidence.test.ts` — table-driven pure-function test of the confidence classifier extracted as a helper.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/forecast/forecast.service.test.ts tests/forecast/confidence.test.ts --run</automated>
  </verify>
  <done>predictWait returns confidence + median or baseline; table-driven tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Beta Forecast framing contract test</name>
  <files>apps/api/src/forecast/forecast.service.ts, apps/api/tests/forecast/beta-framing.test.ts</files>
  <behavior>
    - `ForecastService.computePlanForecastFraming(daysWithForecasts): { disclaimer?: string }` returns `{ disclaimer: 'Beta Forecast' }` if ANY forecasted wait has confidence `low`.
    - Plan orchestrator (plan 03-16) calls this and sets `plan.meta.forecast_disclaimer` accordingly.
  </behavior>
  <action>
    1. Add `computePlanForecastFraming` helper to `forecast.service.ts`.
    2. `beta-framing.test.ts`:
       - All-medium + all-high days → no disclaimer.
       - Any-low day → `disclaimer: 'Beta Forecast'`.
    3. FC-04 is covered by the forecast fixture tests already established — reference the solver fixtures from plan 03-10 as they exercise the full forecast round-trip. Add a brief note in test file header.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/forecast/beta-framing.test.ts --run</automated>
  </verify>
  <done>Beta Forecast framing deterministic; contract test green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test -- tests/forecast --run` green
- `pnpm -r typecheck` clean
</verification>

<success_criteria>
- FC-01, FC-03, FC-04, FC-05 each have automated signals.
- Baseline fallback triggered in low-confidence scenarios (expected operating mode for Phase 3).
</success_criteria>

<output>
`.planning/phases/03-engine/03-11-SUMMARY.md` — bucketing query shape, confidence thresholds, baseline fallback flow.
</output>
