---
phase: 03-engine
plan: 09
type: execute
wave: 5
depends_on: [03-07]
files_modified:
  - packages/solver/src/fatigue.ts
  - packages/solver/src/rules.ts
  - packages/solver/src/index.ts
  - packages/solver/tests/fatigue.test.ts
  - packages/solver/tests/budget-tier.test.ts
autonomous: true
requirements: [SOLV-07, SOLV-10]
must_haves:
  truths:
    - "Child fatigue rest blocks inserted based on age distribution × budget tier"
    - "Budget tier constants (Pixie/Fairy/Royal) live in `packages/solver/src/rules.ts` — NOT in `packages/content` — because they are algorithm, not catalog"
    - "Tier rules: LL allocation, rest frequency, dining tier — all encoded as a single constant table"
  artifacts:
    - path: "packages/solver/src/fatigue.ts"
      provides: "insertRestBlocks(items, guests, tier, date) → PlanItem[]"
    - path: "packages/solver/src/rules.ts"
      provides: "BUDGET_TIER_RULES: Record<Tier, TierRules>"
  key_links:
    - from: "packages/solver/src/fatigue.ts"
      to: "packages/solver/src/rules.ts"
      via: "BUDGET_TIER_RULES.restFrequency"
      pattern: "BUDGET_TIER_RULES"
---

<objective>
Ship SOLV-07 (child fatigue model) + SOLV-10 (budget tier rules). Both are driven by a single `BUDGET_TIER_RULES` constant table that also informs 03-08's allocator.

Output:
  - Age-bracket-weighted rest block insertion
  - Budget tier rules table + all three tier snapshot tests
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@packages/solver/src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Budget tier rules constant table</name>
  <files>packages/solver/src/rules.ts, packages/solver/tests/budget-tier.test.ts, packages/solver/src/index.ts</files>
  <behavior>
    - `BUDGET_TIER_RULES: Record<'pixie'|'fairy'|'royal', TierRules>`
    - `TierRules`: `{ llmpCap: number, llspCap: number, restFrequencyHours: number, diningTier: 'value'|'quick_service'|'table_service'|'signature', restBlockDurationMinutes: number }`
    - Pixie: `{ llmpCap: 0, llspCap: 0, restFrequencyHours: 3, diningTier: 'value', restBlockDurationMinutes: 60 }`
    - Fairy: `{ llmpCap: 3, llspCap: 1, restFrequencyHours: 2, diningTier: 'table_service', restBlockDurationMinutes: 60 }`
    - Royal: `{ llmpCap: 3, llspCap: 2, restFrequencyHours: 2, diningTier: 'signature', restBlockDurationMinutes: 120 }`
  </behavior>
  <action>
    1. Write `rules.ts` with the table + type.
    2. `budget-tier.test.ts`:
       - Each tier returns correct cap values.
       - Pixie forbids LLMP (`llmpCap === 0`).
       - Royal allows signature dining (`diningTier === 'signature'`).
       - Immutability: `BUDGET_TIER_RULES` is `as const`; TypeScript rejects mutation.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/budget-tier.test.ts --run</automated>
  </verify>
  <done>Tier constants defined and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Age-weighted fatigue rest block insertion</name>
  <files>packages/solver/src/fatigue.ts, packages/solver/tests/fatigue.test.ts, packages/solver/src/index.ts</files>
  <behavior>
    - `insertRestBlocks(items, guests, tier): PlanItem[]`
    - If any guest `ageBracket === '0-2'`: insert rest block 12:30-13:30 (peak fatigue).
    - If any guest `ageBracket === '3-6'`: insert rest block 13:00-14:00.
    - If BOTH: insert 12:30-14:00 (merged).
    - Further rest blocks scale by `tier.restFrequencyHours`: e.g., Royal (every 2h) + Deluxe lodging → afternoon resort break 14:00-16:00.
    - Displaced attractions shift; if a must-do conflicts with fatigue rest, must-do keeps (fatigue is a soft constraint).
    - Returns items sorted by `startTime`.
  </behavior>
  <action>
    1. Implement per behavior. Deterministic — guests iterated by id-lex.
    2. `fatigue.test.ts`:
       - Party [toddler 0-2, adult 18+] → rest block at 12:30-13:30.
       - Party [young kid 3-6] → rest block at 13:00-14:00.
       - Party [both toddler + young kid] → merged 12:30-14:00.
       - Adults only → no rest block inserted for fatigue (still may have mid-day break per tier).
       - Royal tier + deluxe lodging → afternoon resort break 14:00-16:00.
       - Must-do at 13:00 conflicts → must-do kept; rest split around it or skipped that day.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/fatigue.test.ts --run</automated>
  </verify>
  <done>Fatigue rest insertion deterministic and table-tested.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/solver test --run` green
</verification>

<success_criteria>
- SOLV-07 + SOLV-10 have table-driven unit tests covering all three tiers × age distribution matrix.
</success_criteria>

<output>
`.planning/phases/03-engine/03-09-SUMMARY.md` — tier table, fatigue windows, displacement rules.
</output>
