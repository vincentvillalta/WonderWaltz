---
phase: 03-engine
plan: 10
type: execute
wave: 6
depends_on: [03-08, 03-09]
files_modified:
  - packages/solver/src/index.ts
  - packages/solver/src/__fixtures__/single-day-mk-toddler.ts
  - packages/solver/src/__fixtures__/three-day-all-parks.ts
  - packages/solver/src/__fixtures__/adult-thrill-day.ts
  - packages/solver/src/__fixtures__/mobility-constrained.ts
  - packages/solver/src/__fixtures__/ecv-das.ts
  - packages/solver/src/__fixtures__/five-day-royal.ts
  - packages/solver/tests/deterministic.test.ts
  - packages/solver/tests/snapshot.test.ts
  - packages/solver/tests/__snapshots__/snapshot.test.ts.snap
autonomous: true
requirements: [SOLV-11, SOLV-12]
must_haves:
  truths:
    - "`solve(SolverInput): DayPlan[]` wires construct → local-search → LL alloc → fatigue → meals → shows"
    - "`solve()` is deterministic: same SolverInput → byte-identical output on 100 runs"
    - "Six canonical fixture trips each produce a committed Vitest snapshot; snapshots byte-identical across runs and across Node minor versions (within project pin)"
    - "Determinism test runs `solve()` 100× on the same input and asserts identical JSON hashes"
  artifacts:
    - path: "packages/solver/src/index.ts"
      provides: "Fully-wired solve(SolverInput): DayPlan[]"
    - path: "packages/solver/src/__fixtures__/*.ts"
      provides: "Six canonical fixture inputs with pinned dates + pinned forecast fn"
    - path: "packages/solver/tests/__snapshots__/snapshot.test.ts.snap"
      provides: "Committed Vitest snapshots (6 canonical trips)"
  key_links:
    - from: "packages/solver/src/index.ts"
      to: "packages/solver/src/construct.ts + localSearch.ts + fatigue.ts + lightningLane.ts + meals.ts + shows.ts"
      via: "orchestration inside solve()"
      pattern: "constructDay|adjacentPairSwap|allocateLL|insertRestBlocks|insertMeals|insertShows"
---

<objective>
Wire the full solver pipeline and prove determinism with six canonical fixture snapshot tests. This is the Phase 3 gate for solver work.

Output:
  - Fully-implemented `solve()` function orchestrating every solver subsystem
  - Six fixture files with pinned inputs
  - Six committed snapshots
  - One 100-run determinism test
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@packages/solver/src/types.ts
@packages/solver/src/construct.ts
@packages/solver/src/localSearch.ts
@packages/solver/src/lightningLane.ts
@packages/solver/src/fatigue.ts
@packages/solver/src/meals.ts
@packages/solver/src/shows.ts
@packages/solver/src/parkHours.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire solve() orchestration</name>
  <files>packages/solver/src/index.ts, packages/solver/tests/deterministic.test.ts</files>
  <behavior>
    - `solve(input: SolverInput): DayPlan[]`:
      1. For each date in `[dateStart..dateEnd]`:
         a. Resolve park hours (`resolveParkHours`)
         b. Filter attractions (`filterAttractionsForParty`)
         c. Construct day (greedy + pinning)
         d. Insert meals
         e. Insert shows
         f. Local search (adjacent swap)
         g. Allocate LL + DAS
         h. Insert fatigue rest blocks
      2. Return `DayPlan[]` sorted by `dayIndex`.
    - Deterministic: 100 runs on same input → byte-identical output (JSON.stringify match).
    - Pinned forecast fn (passed in via input) — solver itself calls no stubs.
    - `solve()` must complete in < 2 seconds for a 5-day fixture (leaves budget for LLM).
  </behavior>
  <action>
    1. Replace the stub `solve()` in `packages/solver/src/index.ts` with the full orchestration.
    2. Orchestrator is pure — no Math.random, no Date.now; uses `input.date` for all time math.
    3. `deterministic.test.ts`:
       - Loads one fixture input.
       - Runs `solve()` 100 times.
       - Computes SHA-256 of JSON.stringify(result) for each.
       - Asserts all 100 hashes equal.
       - Asserts runtime < 2s for a 3-day fixture (perf budget).
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/deterministic.test.ts --run</automated>
  </verify>
  <done>solve() orchestrates all subsystems; 100-run determinism proven; perf budget met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Six canonical fixtures + Vitest snapshot suite</name>
  <files>packages/solver/src/__fixtures__/single-day-mk-toddler.ts, packages/solver/src/__fixtures__/three-day-all-parks.ts, packages/solver/src/__fixtures__/adult-thrill-day.ts, packages/solver/src/__fixtures__/mobility-constrained.ts, packages/solver/src/__fixtures__/ecv-das.ts, packages/solver/src/__fixtures__/five-day-royal.ts, packages/solver/tests/snapshot.test.ts, packages/solver/tests/__snapshots__/snapshot.test.ts.snap</files>
  <behavior>
    - Each fixture exports `{ input: SolverInput, name: string }` with pinned dates (e.g., `2026-06-15` for the first, `2026-06-16..18` for 3-day, etc.) and a pinned deterministic forecastFn.
    - `snapshot.test.ts` runs `solve()` on each, calls `expect(result).toMatchSnapshot()`, resulting in 6 committed snapshots.
    - Snapshots byte-identical across runs; CI gate requires no snapshot changes.
    - Running in CI mode (`--run`) FAILS if any snapshot diff exists.
  </behavior>
  <action>
    1. Author six fixture files with realistic solver inputs:
       - `single-day-mk-toddler.ts`: 1 day, Magic Kingdom, party = 2 adults + 1 toddler (0-2), Pixie tier, 3 must-do low-intensity attractions.
       - `three-day-all-parks.ts`: 3 days (MK, EPCOT, DHS), party = 2 adults + 2 kids (3-6, 7-9), Fairy tier, mixed must-dos.
       - `adult-thrill-day.ts`: 1 day, DHS, party = 2 adults, Royal tier, headliner must-dos (Rise of the Resistance, TRON, Rock 'n' Roller Coaster).
       - `mobility-constrained.ts`: 2 days, party includes reduced-mobility guest, walking budget hard cap, Fairy tier.
       - `ecv-das.ts`: 2 days, party with ECV + DAS flag, Fairy tier.
       - `five-day-royal.ts`: 5 days all 4 parks + resort day, 2 adults + 2 kids, Royal tier, Deluxe Villa lodging.
    2. Each fixture includes a deterministic forecastFn: `(rideId, ts) => { minutes: hash(rideId + ts) % 120, confidence: 'medium' }` so snapshots don't need real history.
    3. `snapshot.test.ts`:
       ```ts
       import { solve } from '../src/index.js';
       import { fixtures } from '../src/__fixtures__/index.js';
       for (const f of fixtures) {
         it(`${f.name} → byte-identical DayPlan[]`, () => {
           const result = solve(f.input);
           expect(result).toMatchSnapshot();
         });
       }
       ```
    4. Run the test in update mode ONCE (`vitest -u`) to generate `snapshot.test.ts.snap`. Then run in `--run` mode to confirm no diff. Commit both.
    5. Add a second assertion per fixture: compute SHA-256 of the result, log it — future regressions show as diff here.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/solver test -- tests/snapshot.test.ts --run</automated>
  </verify>
  <done>6/6 snapshot tests green; committed snapshot files present; no diff.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/solver test --run` green (all tests including determinism + snapshots)
- `pnpm --filter @wonderwaltz/solver typecheck` clean
- 100-run determinism proof passes
</verification>

<success_criteria>
- SOLV-11 + SOLV-12 fully automated. This is the phase gate for solver work.
- Any solver change that breaks a snapshot is immediately visible in CI.
</success_criteria>

<output>
`.planning/phases/03-engine/03-10-SUMMARY.md` — orchestration order, fixture rationale, snapshot-file locations, perf numbers.
</output>
