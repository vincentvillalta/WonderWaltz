---
phase: 03-engine
plan: 14
type: execute
wave: 5
depends_on: [03-13]
files_modified:
  - apps/api/src/plan-generation/circuit-breaker.service.ts
  - apps/api/src/plan-generation/plan-generation.module.ts
  - apps/api/src/narrative/narrative.service.ts
  - apps/api/tests/plan-generation/circuit-breaker.test.ts
  - apps/api/tests/narrative/sonnet-haiku-fallback.test.ts
  - apps/api/tests/narrative/model-id-contract.test.ts
autonomous: true
requirements: [LLM-03, LLM-07]
must_haves:
  truths:
    - "Sonnet model ID pinned: `claude-sonnet-4-6` for initial generation"
    - "Haiku model ID pinned: `claude-haiku-4-5` for rethink + budget-exhausted fallback"
    - "Per-trip lifetime budget: $0.50 (50 cents), stored on `trips.llm_budget_cents`"
    - "Mid-generation: if `trip_spent + projected_call_cost > budget` AND current model is Sonnet → swap to Haiku"
    - "If Haiku also fails Zod validation → persist plan with empty narrative (chain-fall-through)"
    - "Over-budget NEW request: returns 402 with PlanBudgetExhausted body; does NOT call Anthropic"
    - "Every circuit-breaker trip writes to: (1) Sentry exception, (2) Slack alert (aggregated hourly), (3) `llm_cost_incidents` table"
  artifacts:
    - path: "apps/api/src/plan-generation/circuit-breaker.service.ts"
      provides: "checkBudget(tripId): BudgetCheck + recordTrip + three-sink alerting"
    - path: "apps/api/src/narrative/narrative.service.ts"
      provides: "(updated) generate() with Sonnet→Haiku fallback integrated"
  key_links:
    - from: "apps/api/src/plan-generation/circuit-breaker.service.ts"
      to: "llm_cost_incidents table"
      via: "INSERT on every trip"
      pattern: "llm_cost_incidents"
    - from: "apps/api/src/narrative/narrative.service.ts"
      to: "CircuitBreakerService"
      via: "checkBudget before each Anthropic call"
      pattern: "checkBudget"
---

<objective>
Ship LLM-03 (model ID pinning) + LLM-07 (per-trip circuit breaker with 3-sink telemetry + Sonnet→Haiku fallback + 402 contract).

Output:
  - CircuitBreakerService with per-trip budget check
  - NarrativeService integration for mid-generation Sonnet→Haiku swap
  - 402 contract enforced before Anthropic call in over-budget path
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@apps/api/src/narrative/narrative.service.ts
@apps/api/src/alerting/slack-alerter.service.ts
@apps/api/src/shared/dto/plan-budget-exhausted.dto.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin model IDs + contract test</name>
  <files>apps/api/src/narrative/narrative.service.ts, apps/api/tests/narrative/model-id-contract.test.ts</files>
  <behavior>
    - Constants exported from `narrative.service.ts`:
      - `SONNET_MODEL_ID = 'claude-sonnet-4-6'`
      - `HAIKU_MODEL_ID = 'claude-haiku-4-5'`
    - `generate()` defaults to Sonnet; `generateRethinkIntro()` defaults to Haiku.
    - Any call supplying a different model ID fails a contract check (throws `Error('unpinned model')`).
  </behavior>
  <action>
    1. Add pinned constants + contract check in `generate` / `generateRethinkIntro`.
    2. `model-id-contract.test.ts`:
       - `generate()` → Anthropic mock receives `claude-sonnet-4-6`.
       - `generateRethinkIntro()` → mock receives `claude-haiku-4-5`.
       - Passing a different model → throws.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/narrative/model-id-contract.test.ts --run</automated>
  </verify>
  <done>Model IDs pinned; contract check enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CircuitBreakerService + 3-sink alerting</name>
  <files>apps/api/src/plan-generation/circuit-breaker.service.ts, apps/api/src/plan-generation/plan-generation.module.ts, apps/api/tests/plan-generation/circuit-breaker.test.ts</files>
  <behavior>
    - `CircuitBreakerService.checkBudget(tripId, projectedCents): { allowed: boolean, swapTo?: 'haiku', reason?: string, spentCents, budgetCents }`
      - Reads `trips.llm_budget_cents` (default 50) and `SUM(usd_cents) FROM llm_costs WHERE trip_id = $1`.
      - If `spent + projected <= budget` → `{ allowed: true }`.
      - If `spent + projected > budget` AND projected was for Sonnet → `{ allowed: true, swapTo: 'haiku' }` (still allowed but downgraded).
      - If `spent >= budget` → `{ allowed: false, reason: 'trip_budget_exhausted' }`.
    - `recordIncident({ tripId, event, model, spentCents }): Promise<void>` writes to 3 sinks:
      1. `INSERT INTO llm_cost_incidents`
      2. `Sentry.captureException(new BudgetEvent(...), { tags: { tripId, event, model } })`
      3. `slackAlerter.sendAlert(...)` — aggregated hourly via Redis key dedup.
    - `buildBudgetExhaustedResponse(spent, budget)`: returns a `PlanBudgetExhaustedDto` with `resetOptions: [{ type: 'top_up', sku: 'trip_topup_050', usd_cents: 50 }]` (Phase 4 wires the real SKU).
  </behavior>
  <action>
    1. Implement `CircuitBreakerService` with injected `DB_TOKEN`, `REDIS_CLIENT_TOKEN`, `SlackAlerterService`.
    2. Register in `PlanGenerationModule`.
    3. `circuit-breaker.test.ts` table-driven:
       - Spent 20, projected 15, budget 50 → allowed.
       - Spent 40, projected 15 (sonnet call), budget 50 → allowed+swapTo haiku.
       - Spent 55, budget 50 → disallowed, 402 response built.
       - recordIncident writes to all 3 sinks (verify DB insert, Sentry captureException called, slackAlerter called).
       - Second incident within same hour → Slack NOT called again (dedup).
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/plan-generation/circuit-breaker.test.ts --run</automated>
  </verify>
  <done>Budget math correct; 3 sinks fire; dedup works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NarrativeService integration — mid-gen Sonnet→Haiku swap</name>
  <files>apps/api/src/narrative/narrative.service.ts, apps/api/tests/narrative/sonnet-haiku-fallback.test.ts</files>
  <behavior>
    - `generate()` before each Anthropic call invokes `CircuitBreakerService.checkBudget(tripId, estimatedCents)`.
    - If `swapTo === 'haiku'`: override model = HAIKU_MODEL_ID for this call; record incident with `event = 'sonnet_to_haiku_swap'`.
    - If `allowed === false`: throw `BudgetExhaustedError` (caught by plan-generation orchestrator → 402 response, no Anthropic call made).
    - If Haiku call fails Zod: fall through to retry (per 03-12); if retry also fails → narrative_available: false.
    - Test matrix covers: Sonnet-happy, Sonnet-swap-to-Haiku-happy, Sonnet-swap-Haiku-fails, over-budget-rejects-without-call.
  </behavior>
  <action>
    1. Wire `CircuitBreakerService` DI into NarrativeService.
    2. Implement swap logic per behavior.
    3. `sonnet-haiku-fallback.test.ts`:
       - Mock circuit breaker: `allowed: true, swapTo: 'haiku'` → Anthropic mock called with Haiku model; incident recorded.
       - Mock: `allowed: false` → NarrativeService throws `BudgetExhaustedError`; Anthropic mock NOT called.
       - Happy path: allowed + no swap → Sonnet used.
       - Swap + Haiku Zod failure twice → `narrativeAvailable: false`, incident recorded.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/narrative/sonnet-haiku-fallback.test.ts --run</automated>
  </verify>
  <done>Mid-gen swap works; over-budget rejects without network call; fall-through to empty-narrative works.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test -- tests/plan-generation tests/narrative --run` green
</verification>

<success_criteria>
- LLM-03 + LLM-07 fully automated.
- 402 contract published; Phase 4 will wire the real SKU.
</success_criteria>

<output>
`.planning/phases/03-engine/03-14-SUMMARY.md` — model IDs, budget math, 3-sink telemetry pattern.
</output>
