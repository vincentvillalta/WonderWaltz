---
phase: 03-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - apps/api/tests/anthropic-mock.ts
  - apps/api/tests/fixtures/narrative-response.json
  - apps/api/tests/fixtures/narrative-response.invalid-ride.json
  - apps/api/tests/anthropic-mock.test.ts
  - apps/api/src/narrative/narrative.module.ts
  - apps/api/src/narrative/narrative.service.ts
  - apps/api/src/narrative/anthropic.client.ts
  - docs/ops/PROVISIONING_STATE.md
autonomous: true
requirements: [LLM-01]
user_setup:
  - service: anthropic
    why: "Claude API for plan narrative"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → Settings → API Keys"
    dashboard_config: []
must_haves:
  truths:
    - "`@anthropic-ai/sdk` is installed in apps/api and typecheck clean"
    - "NarrativeModule can be imported from apps/api without Anthropic network access"
    - "Anthropic mock harness replays a real-shape Messages response deterministically"
    - "Mock reports prompt-caching usage fields (cache_read_input_tokens, cache_creation_input_tokens) that downstream cost-tracking tests can assert on"
  artifacts:
    - path: "apps/api/tests/anthropic-mock.ts"
      provides: "Deterministic mock for Anthropic SDK respecting cache_control semantics and usage reporting"
    - path: "apps/api/tests/fixtures/narrative-response.json"
      provides: "Captured real-shape Anthropic Messages response used for replay"
    - path: "apps/api/src/narrative/narrative.service.ts"
      provides: "NarrativeModule entry point; callClaude(input): Promise<NarrativeResult>"
  key_links:
    - from: "apps/api/src/narrative/narrative.service.ts"
      to: "@anthropic-ai/sdk"
      via: "new Anthropic({ apiKey }).messages.create"
      pattern: "anthropic\\.messages\\.create"
---

<objective>
Install `@anthropic-ai/sdk`, establish a deterministic mock harness for every downstream narrative/cost/circuit-breaker test, and stand up the `NarrativeModule` skeleton (wired but unimplemented). Prompt/schema/validation land in plan 03-12.

Purpose: Every LLM-related plan after this one assumes (a) the SDK is present, (b) tests can run without network, (c) mocked usage fields are realistic enough to exercise cost math and cache-hit alerting. Building this once prevents each feature plan from reinventing the mock.

Output:
  - `@anthropic-ai/sdk` installed (pinned `^0.65.0`)
  - Anthropic mock with cache-aware usage reporting
  - Narrative fixture JSON captured from a real call shape
  - `NarrativeModule` scaffolded in NestJS with DI-injectable `AnthropicClient`
  - `ANTHROPIC_API_KEY` documented as a Phase 3 prerequisite
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@apps/api/package.json
@apps/api/src/shared-infra.module.ts
@apps/api/src/alerting/slack-alerter.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install @anthropic-ai/sdk + document env var</name>
  <files>apps/api/package.json, pnpm-lock.yaml, docs/ops/PROVISIONING_STATE.md, apps/api/tests/anthropic-sdk.import.test.ts</files>
  <behavior>
    - `pnpm --filter @wonderwaltz/api add @anthropic-ai/sdk` lands at `^0.65.0` or later minor.
    - `import Anthropic from '@anthropic-ai/sdk'` compiles cleanly.
    - `docs/ops/PROVISIONING_STATE.md` lists `ANTHROPIC_API_KEY` as a Phase 3 prerequisite with where-to-obtain notes.
  </behavior>
  <action>
    1. Run `pnpm --filter @wonderwaltz/api add @anthropic-ai/sdk@^0.65.0`. Verify `apps/api/package.json` dependencies include it; verify `pnpm-lock.yaml` updated.
    2. Add `apps/api/tests/anthropic-sdk.import.test.ts`:
       ```ts
       import Anthropic from '@anthropic-ai/sdk';
       it('instantiates with API key', () => {
         const c = new Anthropic({ apiKey: 'test' });
         expect(c.messages).toBeDefined();
       });
       ```
    3. Append a "Phase 3 Prerequisites" row to `docs/ops/PROVISIONING_STATE.md`: `ANTHROPIC_API_KEY` from console.anthropic.com, required in Railway worker env + `.env.local`.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/anthropic-sdk.import.test.ts --run && pnpm --filter @wonderwaltz/api typecheck</automated>
  </verify>
  <done>SDK resolves; import test green; provisioning doc updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anthropic mock harness + replay fixtures</name>
  <files>apps/api/tests/anthropic-mock.ts, apps/api/tests/fixtures/narrative-response.json, apps/api/tests/fixtures/narrative-response.invalid-ride.json, apps/api/tests/anthropic-mock.test.ts</files>
  <behavior>
    - `createAnthropicMock({ fixtures })` returns an object shape-compatible with `Anthropic.prototype.messages.create` — specifically `messages.create(params): Promise<Message>`.
    - Default fixture path (`narrative-response.json`) replays a realistic Messages API response with:
      - `content: [{ type: 'text', text: '<json-encoded narrative>' }]`
      - `model: 'claude-sonnet-4-6'` (pinned ID per research)
      - `usage: { input_tokens: 5200, output_tokens: 650, cache_creation_input_tokens: 5000, cache_read_input_tokens: 0 }` (first call — cache miss)
    - Second call in the same session (same system prompt) returns `cache_read_input_tokens: 4800` (hit) and `cache_creation_input_tokens: 0`.
    - Supports fixture switching: `mock.setFixture('invalid-ride')` for the Zod-rejection test case.
    - Mock tracks call count and exposes `mock.calls[]` for assertion.
    - Mock respects `params.model` — if test passes Haiku, `response.model === 'claude-haiku-4-5'`.
  </behavior>
  <action>
    1. Create `apps/api/tests/fixtures/narrative-response.json` — a real-shape Anthropic Messages response. Inside its `content[0].text` put a JSON string containing a narrative payload that matches the Zod schema we'll finalize in 03-12 (per-day intro + per-item tips keyed by solver plan_item ID). For now, hand-author a valid 2-day shape:
       ```json
       {
         "days": [
           { "dayIndex": 0, "intro": "...", "items": [{ "planItemId": "...", "tip": "..." }] },
           { "dayIndex": 1, "intro": "...", "items": [...] }
         ],
         "packingDelta": [{ "item": "Sunscreen", "reason": "Orlando sun" }],
         "budgetHacks": ["..."]
       }
       ```
       Use placeholder plan_item UUIDs that can be parameterized by the test via string substitution.
    2. Create `narrative-response.invalid-ride.json` that references a ride ID NOT in any solver output — used for Zod rejection test in 03-12.
    3. Implement `apps/api/tests/anthropic-mock.ts`:
       - Exports `createAnthropicMock(opts?: { fixture?: string })` returning `{ messages: { create }, calls, setFixture, reset }`.
       - Tracks whether the system prompt prefix is byte-identical across calls (checks the `cache_control` block specifically); if same → reports cache hit usage, if different → reports cache creation.
       - Respects `params.model` in the response (enables LLM-03 contract test later).
    4. Write `apps/api/tests/anthropic-mock.test.ts` asserting:
       - First call returns cache-miss usage
       - Second call with same prefix returns cache-hit usage
       - Different prefix → fresh cache creation
       - `model: 'claude-haiku-4-5'` passthrough works
       - `calls[]` length increments
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/anthropic-mock.test.ts --run</automated>
  </verify>
  <done>Mock harness tests green; fixtures committed; cache-aware usage reporting verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NarrativeModule scaffold + injectable AnthropicClient</name>
  <files>apps/api/src/narrative/narrative.module.ts, apps/api/src/narrative/narrative.service.ts, apps/api/src/narrative/anthropic.client.ts, apps/api/src/app.module.ts, apps/api/tests/narrative/narrative.module.test.ts</files>
  <behavior>
    - `NarrativeModule` exports `NarrativeService`.
    - `ANTHROPIC_CLIENT_TOKEN` provider factory reads `process.env.ANTHROPIC_API_KEY` and returns `new Anthropic({ apiKey })`. In tests, overridden with the mock from Task 2.
    - `NarrativeService.generate(input): Promise<NarrativeResult>` exists with a stub that THROWS `new Error('NarrativeService.generate — implemented in 03-12')` — wiring only; prompt/validation land in 03-12.
    - `NarrativeModule` imported into `AppModule` (HTTP side) and `WorkerModule` (the latter actually uses it in 03-16).
    - Typecheck green; NestJS DI resolves the module in a `@nestjs/testing` Test.
  </behavior>
  <action>
    1. `apps/api/src/narrative/anthropic.client.ts`:
       ```ts
       export const ANTHROPIC_CLIENT_TOKEN = Symbol('ANTHROPIC_CLIENT');
       export const anthropicClientProvider = {
         provide: ANTHROPIC_CLIENT_TOKEN,
         useFactory: () => {
           const apiKey = process.env['ANTHROPIC_API_KEY'];
           if (!apiKey && process.env['NODE_ENV'] !== 'test') {
             throw new Error('ANTHROPIC_API_KEY is required');
           }
           return new Anthropic({ apiKey: apiKey ?? 'test-key' });
         },
       };
       ```
    2. `narrative.service.ts`: class with `@Inject(ANTHROPIC_CLIENT_TOKEN) private client: Anthropic`; methods `generate()` / `generateRethinkIntro()` throw Not-Implemented. Export `NarrativeResult` type stub.
    3. `narrative.module.ts` registers the provider and exports `NarrativeService`.
    4. Register `NarrativeModule` in `AppModule`.
    5. `narrative.module.test.ts` uses `Test.createTestingModule({ imports: [NarrativeModule] }).overrideProvider(ANTHROPIC_CLIENT_TOKEN).useValue(createAnthropicMock())` and asserts the service resolves.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/narrative/narrative.module.test.ts --run && pnpm --filter @wonderwaltz/api typecheck</automated>
  </verify>
  <done>Module compiles; DI resolves with mock; AppModule registers it; LLM-01 contract surface (service exists) satisfied at the scaffold level.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test --run` green for narrative + mock tests
- `pnpm -r typecheck` clean
</verification>

<success_criteria>
- Anthropic SDK installed and imported.
- Mock harness supports cache-aware usage reporting.
- NarrativeModule wired (implementation deferred to 03-12).
</success_criteria>

<output>
After completion, create `.planning/phases/03-engine/03-02-SUMMARY.md` documenting: SDK version pinned, mock harness API, fixture paths, ANTHROPIC_API_KEY provisioning status.
</output>
