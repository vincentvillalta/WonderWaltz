---
phase: 03-engine
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/migrations/0005_plan_schema_alignment.sql
  - packages/db/src/schema/plans.ts
  - apps/api/src/plan-generation/persist-plan.service.ts
  - apps/api/src/plans/plans.service.ts
autonomous: true
gap_closure: true
requirements:
  - SC-1
  - SC-2
  - SC-5
  - PLAN-02
  - PLAN-03
  - FC-05

must_haves:
  truths:
    - "GET /plans/:id returns fully structured DayPlan[] with narrative text, wait_minutes, lightning_lane_type, and name on each item"
    - "persist-plan writes name, wait_minutes, lightning_lane_type, notes, forecast_confidence as direct columns (not JSONB metadata)"
    - "plans.warnings column exists and stores JSON array of solver warnings"
    - "plan_days.forecast_confidence column exists for Beta Forecast framing"
  artifacts:
    - path: "packages/db/migrations/0005_plan_schema_alignment.sql"
      provides: "DDL adding missing columns to plan_days, plan_items, plans"
      contains: "ALTER TABLE"
    - path: "packages/db/src/schema/plans.ts"
      provides: "Drizzle schema updated with new columns"
      contains: "forecastConfidence"
    - path: "apps/api/src/plan-generation/persist-plan.service.ts"
      provides: "Write path uses direct columns instead of JSONB metadata"
      contains: "wait_minutes"
    - path: "apps/api/src/plans/plans.service.ts"
      provides: "Read path queries columns that exist in migration"
      contains: "narrative_intro"
  key_links:
    - from: "apps/api/src/plan-generation/persist-plan.service.ts"
      to: "plan_items direct columns"
      via: "INSERT INTO plan_items with name, wait_minutes, lightning_lane_type, notes, narrative_tip"
      pattern: "INSERT INTO plan_items.*name.*wait_minutes"
    - from: "apps/api/src/plans/plans.service.ts"
      to: "plan_items direct columns"
      via: "SELECT pi.name, pi.wait_minutes, pi.lightning_lane_type, pi.notes, pi.narrative_tip"
      pattern: "pi\\.name.*pi\\.wait_minutes"
---

<objective>
Close Gap 1 (CRITICAL): Align the DB schema, persist path, and read path so that
GET /plans/:id returns real data instead of NULL/error columns.

Purpose: plans.service.ts queries 8 columns that do not exist in any migration.
persist-plan.service.ts writes some of these values into JSONB metadata, but
plans.service.ts reads them as direct columns. This makes the entire plan read
path non-functional on a real Postgres connection.

Output: A new migration adding the missing columns, updated Drizzle schema,
and aligned write/read paths.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-engine/03-VERIFICATION.md
@packages/db/migrations/0000_lying_nicolaos.sql (plan_days/plan_items CREATE TABLE at lines 202-224)
@packages/db/migrations/0004_phase3_engine.sql (existing Phase 3 migration)
@packages/db/src/schema/plans.ts (current Drizzle schema)
@apps/api/src/plans/plans.service.ts (read path with nonexistent columns)
@apps/api/src/plan-generation/persist-plan.service.ts (write path using JSONB metadata)

<interfaces>
<!-- Current DB columns (from 0000 migration): -->
<!-- plan_days: id, plan_id, day_index, park_id, date, narrative, created_at -->
<!-- plan_items: id, plan_day_id, item_type, ref_id, start_time, end_time, sort_index, narrative, metadata, is_completed, created_at -->
<!-- plans: id, trip_id, version, solver_input_hash, status, generated_at, created_at, updated_at -->

<!-- plans.service.ts READS these columns that DO NOT EXIST: -->
<!-- plan_days.narrative_intro (exists as 'narrative') -->
<!-- plan_days.forecast_confidence (absent) -->
<!-- plan_items.name (absent) -->
<!-- plan_items.wait_minutes (absent) -->
<!-- plan_items.lightning_lane_type (absent) -->
<!-- plan_items.notes (absent) -->
<!-- plan_items.narrative_tip (exists as 'narrative') -->
<!-- plans.warnings (absent) -->

<!-- persist-plan.service.ts WRITES: -->
<!-- plan_days: plan_id, day_index, park_id, date, narrative (day intro) -->
<!-- plan_items: plan_day_id, item_type, ref_id, start_time, end_time, sort_index, narrative (item tip), metadata (JSON with lightning_lane_type + wait_minutes) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + Drizzle schema for missing columns</name>
  <files>packages/db/migrations/0005_plan_schema_alignment.sql, packages/db/src/schema/plans.ts</files>
  <action>
Create migration `packages/db/migrations/0005_plan_schema_alignment.sql` that adds:

**plan_days table:**
- `narrative_intro TEXT` (the existing `narrative` column stores the day intro; add `narrative_intro` as an ALIAS approach is not viable in Postgres, so RENAME `narrative` to `narrative_intro`)
- `forecast_confidence TEXT` (nullable, values: 'high'|'medium'|'low')

**plan_items table:**
- `name TEXT NOT NULL DEFAULT ''` (attraction/dining/show name for display)
- `wait_minutes INTEGER` (nullable, predicted wait)
- `lightning_lane_type TEXT` (nullable, 'multi_pass'|'single_pass'|'none')
- `notes TEXT` (nullable, solver notes like "Use LL here")
- Rename `narrative` to `narrative_tip` (the existing column stores per-item tips from LLM)

**plans table:**
- `warnings TEXT DEFAULT '[]'` (JSON array of solver warning strings)

Implementation details:
- Use `ALTER TABLE ... RENAME COLUMN` for the renames (plan_days.narrative -> narrative_intro, plan_items.narrative -> narrative_tip). This is safe because persist-plan.service.ts is the ONLY writer, and plans.service.ts is the ONLY reader -- both will be updated in Task 2.
- Use `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` for new columns.
- Add CHECK constraint on lightning_lane_type: `('multi_pass', 'single_pass', 'none')` matching the attractions table pattern.
- Guard everything with IF NOT EXISTS / DO $$ blocks for idempotency.

Update `packages/db/src/schema/plans.ts`:
- In `planDays`: rename `narrative` field to `narrativeIntro` mapped to `narrative_intro`, add `forecastConfidence` mapped to `forecast_confidence`
- In `planItems`: add `name`, `waitMinutes` mapped to `wait_minutes`, `lightningLaneType` mapped to `lightning_lane_type`, `notes`, rename `narrative` to `narrativeTip` mapped to `narrative_tip`
- In `plans`: add `warnings` field mapped to `warnings` (text, default '[]')

IMPORTANT: The rename of `narrative` -> `narrative_intro` / `narrative_tip` is a breaking change for any code that references the old column name. Task 2 updates the two services that use these columns.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz && grep -c "narrative_intro\|forecast_confidence\|wait_minutes\|lightning_lane_type\|narrative_tip" packages/db/migrations/0005_plan_schema_alignment.sql && grep -c "narrativeIntro\|forecastConfidence\|waitMinutes\|lightningLaneType\|narrativeTip" packages/db/src/schema/plans.ts</automated>
  </verify>
  <done>Migration file exists with all column additions/renames. Drizzle schema reflects all new columns. Both files reference narrative_intro (not narrative) for plan_days and narrative_tip (not narrative) for plan_items.</done>
</task>

<task type="auto">
  <name>Task 2: Align persist-plan write path and plans.service read path</name>
  <files>apps/api/src/plan-generation/persist-plan.service.ts, apps/api/src/plans/plans.service.ts</files>
  <action>
**persist-plan.service.ts changes:**

1. In the plan_days INSERT: change column name from `narrative` to `narrative_intro` to match the renamed column.

2. In the plan_items INSERT: change from writing to `narrative` + `metadata` JSONB to writing direct columns:
   - Add `name` column: value is `item.name` (from solver output)
   - Add `wait_minutes` column: value is `item.waitMinutes ?? null`
   - Add `lightning_lane_type` column: value is `item.lightningLaneType ?? null`
   - Add `notes` column: value is `item.notes ?? null`
   - Change column name from `narrative` to `narrative_tip` for the item narrative
   - REMOVE the `metadata` JSONB construction that currently packs lightning_lane_type and wait_minutes into JSON. The metadata column can remain for future extensible data, but write it as `'{}'::jsonb` (empty) or omit it.

3. In the plans INSERT: add `warnings` column, serialize `input.solverOutput` warnings. Collect all day-level warnings into a single JSON array:
   ```
   const allWarnings = input.solverOutput.flatMap(d => d.warnings);
   const warningsJson = JSON.stringify(allWarnings);
   ```
   Pass `warningsJson` as the `warnings` column value in the INSERT.

**plans.service.ts changes:**

1. plan_days query: The column `pd.narrative_intro` now matches the renamed DB column. Keep as-is (it was correct for the intended schema, just the migration was missing).

2. plan_items query: The columns `pi.name`, `pi.wait_minutes`, `pi.lightning_lane_type`, `pi.notes`, `pi.narrative_tip` now match the new direct columns. Keep as-is.

3. plans query: The `COALESCE(warnings, '[]')` now matches the new `warnings` column. Keep as-is.

4. plan_days query also reads `pd.forecast_confidence` -- this column now exists (added in migration). Keep as-is.

So plans.service.ts needs NO code changes -- it was already written for the target schema. Only persist-plan.service.ts needs updating.

Also update the rethink query in trips.controller.ts: the `pi.name` column reference in the rethink-today handler (line 191) now resolves correctly against the new schema. No code change needed there either.
  </action>
  <verify>
    <automated>cd /Users/vincentvillalta/Documents/Projects/WonderWaltz && grep -c "narrative_intro" apps/api/src/plan-generation/persist-plan.service.ts && grep -c "narrative_tip" apps/api/src/plan-generation/persist-plan.service.ts && grep "metadata" apps/api/src/plan-generation/persist-plan.service.ts | grep -cv "lightning_lane_type\|wait_minutes" && pnpm --filter @wonderwaltz/api exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>persist-plan.service.ts writes name, wait_minutes, lightning_lane_type, notes as direct columns. narrative renamed to narrative_intro in plan_days INSERT and narrative_tip in plan_items INSERT. Metadata JSONB no longer packs lightning_lane_type/wait_minutes. plans.service.ts read path matches the persisted columns exactly. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. Migration 0005 adds all 8 missing columns (2 renames + 6 new)
2. Drizzle schema matches migration column names exactly
3. persist-plan INSERT columns match plans.service SELECT columns
4. No JSONB metadata used for lightning_lane_type or wait_minutes
5. TypeScript compiles cleanly
</verification>

<success_criteria>
- Migration file `0005_plan_schema_alignment.sql` exists with all DDL
- Drizzle schema `plans.ts` defines all columns plans.service.ts queries
- persist-plan.service.ts writes `name`, `wait_minutes`, `lightning_lane_type`, `notes`, `narrative_tip` as direct columns
- plans.service.ts queries match persisted column names exactly
- `pnpm --filter @wonderwaltz/api exec tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-engine/03-19-SUMMARY.md`
</output>
