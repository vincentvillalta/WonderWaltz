---
phase: 03-engine
plan: 18
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - apps/api/src/packing-list/packing-list.service.ts
  - apps/api/src/packing-list/packing-list.module.ts
  - apps/api/src/packing-list/affiliate.service.ts
  - apps/api/src/plans/plans.service.ts
  - apps/api/src/app.module.ts
  - apps/api/tests/packing-list/packing-list.service.test.ts
  - apps/api/tests/packing-list/affiliate.test.ts
autonomous: true
requirements: [PLAN-06]
must_haves:
  truths:
    - "`generatePackingList({ planItems, weather, guests }): PackingItem[]` derives items from solver plan + weather + guest ages"
    - "Items like sunscreen, rain jacket, stroller, ECV backup, snacks are inserted per rules"
    - "Affiliate links rewritten at GET /plans/:id serialization time — response contains the affiliate URL, never the raw Amazon SKU without tag"
    - "Amazon Associates tag `wonderwaltz-20` (placeholder) injected into every amazon.com link"
    - "No client response contains the raw tag string"
  artifacts:
    - path: "apps/api/src/packing-list/packing-list.service.ts"
      provides: "generatePackingList rules engine"
    - path: "apps/api/src/packing-list/affiliate.service.ts"
      provides: "rewriteAffiliateUrl(url) — server-side tag injection"
  key_links:
    - from: "apps/api/src/plans/plans.service.ts"
      to: "apps/api/src/packing-list/affiliate.service.ts"
      via: "rewriteAffiliateUrl on every packing_list_items row before serialization"
      pattern: "rewriteAffiliateUrl"
---

<objective>
Ship PLAN-06: packing list generator + server-side affiliate tag injection. Deterministic rules engine driven by plan + weather + guest ages.

Output:
  - `PackingListService.generate(...)`
  - `AffiliateService.rewriteUrl(...)` — single-responsibility, hardened against tag leakage
  - Integration with GET /plans/:id
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@packages/db/src/schema/affiliate.ts
@packages/solver/src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PackingListService rules engine</name>
  <files>apps/api/src/packing-list/packing-list.service.ts, apps/api/src/packing-list/packing-list.module.ts, apps/api/src/app.module.ts, apps/api/tests/packing-list/packing-list.service.test.ts</files>
  <behavior>
    - `generate({ tripId, planItems, weatherByDate, guests }): Promise<PackingItem[]>`
    - Rules (deterministic):
      - Every trip: sunscreen SPF 50, refillable water bottle, portable phone charger, park map (free), rain poncho.
      - If ANY day temp ≥ 85°F: cooling towel.
      - If ANY day probability-of-precipitation ≥ 50%: rain jacket.
      - If ANY guest ageBracket ∈ ['0-2', '3-6']: stroller/rental guide link, baby snacks, extra hydration.
      - If ANY guest mobility === 'ecv': ECV-backup note (rental contact + charger).
      - If any day has a water ride (check solver plan for known-water rides like Splash Mountain, Kali River Rapids): quick-dry clothes + ziploc.
    - Items stored in `packing_list_items` table with `{ trip_id, name, category, recommended_amazon_url?, created_at }`.
    - Deterministic: same input → same ordered list.
  </behavior>
  <action>
    1. Implement `PackingListService`. Known water rides hardcoded as a Set (or tagged in `attractions.yaml` as `gets_wet: boolean` — check and use if already present).
    2. `packing-list.service.test.ts` table-driven:
       - Baseline party + 85°F day → includes cooling towel + sunscreen + water bottle.
       - Rainy day → includes rain jacket.
       - Toddler in party → stroller + baby snacks.
       - ECV guest → ECV backup.
       - Plan includes Splash Mountain → quick-dry + ziploc.
       - All together → superset.
       - Determinism: 10 runs → identical ordered output.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/packing-list/packing-list.service.test.ts --run</automated>
  </verify>
  <done>Rules engine deterministic; all conditions tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AffiliateService + integration at GET /plans/:id</name>
  <files>apps/api/src/packing-list/affiliate.service.ts, apps/api/src/plans/plans.service.ts, apps/api/tests/packing-list/affiliate.test.ts</files>
  <behavior>
    - `rewriteAffiliateUrl(url: string): string`
      - If URL matches `amazon.com` host: append/replace `tag=wonderwaltz-20` query param.
      - Non-Amazon URLs: passthrough.
      - Uses `URL` native API — robust to existing query strings.
    - `plans.service.ts` — at serialization time for GET /plans/:id, load `packing_list_items` for the plan's trip and map each `recommended_amazon_url` through `rewriteAffiliateUrl`.
    - Response always contains the rewritten URL; a response-serialization contract test scans all string fields for the raw untagged SKU pattern and fails if found.
  </behavior>
  <action>
    1. Implement `AffiliateService`. Use the hardcoded tag `'wonderwaltz-20'` (document it's a placeholder — Phase 8 may swap).
    2. Integrate into `plans.service.ts` in the projection step (extends plan 03-17 service).
    3. `affiliate.test.ts`:
       - `amazon.com/dp/B0XYZ` → appends `?tag=wonderwaltz-20`.
       - `amazon.com/dp/B0XYZ?ref=foo` → preserves `ref=foo`, adds tag.
       - `amazon.com/dp/B0XYZ?tag=other` → REPLACES with `tag=wonderwaltz-20`.
       - `rei.com/product/x` → passthrough (no Amazon).
       - Response scan: mock plan with packing list; GET → response body scanned for `amazon.com` without `tag=` pattern → asserts none found.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/packing-list/affiliate.test.ts --run</automated>
  </verify>
  <done>Tag rewrite correct; no raw tag leakage path; response-serialization contract enforced.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test -- tests/packing-list --run` green
</verification>

<success_criteria>
- PLAN-06 fully automated.
- Affiliate tag invariant proven by contract test.
</success_criteria>

<output>
`.planning/phases/03-engine/03-18-SUMMARY.md` — rules table, tag injection invariant.
</output>
