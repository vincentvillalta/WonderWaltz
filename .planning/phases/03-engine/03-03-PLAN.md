---
phase: 03-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/shared/dto/plan.dto.ts
  - apps/api/src/shared/dto/trip.dto.ts
  - apps/api/src/shared/dto/rethink.dto.ts
  - apps/api/src/shared/dto/plan-budget-exhausted.dto.ts
  - apps/api/src/shared/dto/locked-day.dto.ts
  - apps/api/src/shared/dto/plan-warning.dto.ts
  - packages/shared-openapi/openapi.v1.snapshot.json
  - apps/api/scripts/generate-openapi-snapshot.ts
  - apps/api/tests/openapi-snapshot.test.ts
autonomous: true
requirements: [PLAN-02]
must_haves:
  truths:
    - "`Plan.days` is a discriminated union of `FullDayPlan | LockedDayPlan` tagged by `type`"
    - "Plan response includes `warnings: string[]` and `meta.forecast_disclaimer` hooks"
    - "`RethinkRequestDto` accepts `current_time`, `completed_item_ids`, `active_ll_bookings[]`"
    - "`PlanBudgetExhaustedDto` exists and is referenced by the 402 response schema on generate/rethink endpoints"
    - "OpenAPI snapshot regenerates cleanly; CI gate passes"
    - "This is the ONE deliberate v1 amendment for Phase 3 — no further snapshot churn expected mid-phase"
  artifacts:
    - path: "apps/api/src/shared/dto/plan.dto.ts"
      provides: "FullDayPlanDto + LockedDayPlanDto + PlanDto with discriminated union"
      contains: "type: 'full'|'locked'"
    - path: "apps/api/src/shared/dto/rethink.dto.ts"
      provides: "RethinkRequestDto + LLBookingDto"
    - path: "apps/api/src/shared/dto/plan-budget-exhausted.dto.ts"
      provides: "PlanBudgetExhaustedDto for 402 responses"
    - path: "packages/shared-openapi/openapi.v1.snapshot.json"
      provides: "Updated frozen v1 OpenAPI contract"
      contains: "FullDayPlan, LockedDayPlan, RethinkRequest, PlanBudgetExhausted"
  key_links:
    - from: "apps/api/src/trips/trips.controller.ts"
      to: "apps/api/src/shared/dto/rethink.dto.ts"
      via: "@ApiBody({ type: RethinkRequestDto }) on rethinkToday"
      pattern: "RethinkRequestDto"
    - from: "apps/api/scripts/generate-openapi-snapshot.ts"
      to: "packages/shared-openapi/openapi.v1.snapshot.json"
      via: "regenerated artifact"
      pattern: "openapi\\.v1\\.snapshot\\.json"
---

<objective>
Execute the ONE deliberate v1 OpenAPI snapshot amendment for Phase 3: replace the stub `DayPlan` with a `FullDayPlan | LockedDayPlan` discriminated union, add `warnings`, add `RethinkRequestDto`, add `PlanBudgetExhaustedDto` (402). After this plan, the snapshot is frozen again and every downstream plan must not break it.

Purpose: Mobile clients generate their networking code from this snapshot. The free-tier blur semantics, rethink contract, and circuit-breaker 402 contract are load-bearing. Amending them once — now, before implementation — keeps the contract stable through the rest of Phase 3.

Output:
  - Updated DTO files with `@ApiExtraModels` and `@ApiProperty` decorators for discriminated union
  - Regenerated `openapi.v1.snapshot.json`
  - CI-gate snapshot test green
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@apps/api/src/shared/dto/plan.dto.ts
@apps/api/src/shared/dto/trip.dto.ts
@packages/shared-openapi/openapi.v1.snapshot.json
@apps/api/scripts/generate-openapi-snapshot.ts
@apps/api/tests/openapi-snapshot.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Amend DTOs for discriminated union + warnings + rethink + 402</name>
  <files>apps/api/src/shared/dto/plan.dto.ts, apps/api/src/shared/dto/locked-day.dto.ts, apps/api/src/shared/dto/plan-warning.dto.ts, apps/api/src/shared/dto/rethink.dto.ts, apps/api/src/shared/dto/plan-budget-exhausted.dto.ts, apps/api/tests/dto/plan-dto-discriminator.test.ts</files>
  <behavior>
    - `FullDayPlanDto` carries `type: 'full'` literal + all existing `DayPlanDto` fields.
    - `LockedDayPlanDto` carries `{ type: 'locked', dayIndex: int, park: string, totalItems: int, headline: string, unlockTeaser: string }`.
    - `PlanDto.days: (FullDayPlanDto | LockedDayPlanDto)[]` with `@ApiProperty({ oneOf: [...], discriminator: { propertyName: 'type' } })`.
    - `PlanDto.warnings: string[]` exists with default `[]`.
    - `PlanDto.meta.forecast_disclaimer?: string` exists (optional; set to "Beta Forecast" when any day has a `low`-confidence bucket).
    - `RethinkRequestDto`: `{ current_time: string (ISO8601), completed_item_ids: string[] (UUIDs), active_ll_bookings: LLBookingDto[] }`. `LLBookingDto`: `{ attraction_id: string, return_window_start: string, return_window_end: string }`.
    - `PlanBudgetExhaustedDto`: `{ error: 'trip_budget_exhausted', spent_cents: number, budget_cents: number, resetOptions: ResetOptionDto[] }`. `ResetOptionDto`: `{ type: 'top_up', sku: string, usd_cents: number }`.
  </behavior>
  <action>
    1. Rename existing `DayPlanDto` → `FullDayPlanDto`, add `@ApiProperty({ enum: ['full'], type: String }) type: 'full'`.
    2. Create `LockedDayPlanDto` in `apps/api/src/shared/dto/locked-day.dto.ts` with the six fields.
    3. In `PlanDto`, replace `day_plans!: DayPlanDto[]` with `days!: (FullDayPlanDto | LockedDayPlanDto)[]` using `@ApiProperty({ type: 'array', items: { oneOf: [{ $ref: getSchemaPath(FullDayPlanDto) }, { $ref: getSchemaPath(LockedDayPlanDto) }], discriminator: { propertyName: 'type', mapping: { full: ..., locked: ... } } } })`. Register both with `@ApiExtraModels`.
       (Keep `day_plans` as a deprecated alias for one phase if downstream code assumes it — OR fully migrate and rename. Given trips.controller is still a stub, full rename is safe.)
    4. Add `warnings!: string[]` on `PlanDto`.
    5. Create `rethink.dto.ts` with `RethinkRequestDto` + `LLBookingDto`, using `class-validator` decorators (`@IsISO8601`, `@IsUUID('4', { each: true })`, `@ValidateNested({ each: true })`, `@Type(() => LLBookingDto)`).
    6. Create `plan-budget-exhausted.dto.ts` with `PlanBudgetExhaustedDto` + `ResetOptionDto`.
    7. Update `trips.controller.ts` `@ApiResponse` annotations for generate-plan + rethink-today to include `status: 402, type: PlanBudgetExhaustedDto` and `@ApiBody({ type: RethinkRequestDto })` (endpoints still return 501 — annotation-only change).
    8. Write `apps/api/tests/dto/plan-dto-discriminator.test.ts` that uses `class-transformer.plainToInstance(PlanDto, fixture)` to parse both a `type: 'full'` and `type: 'locked'` day, and asserts discriminated parsing works.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/dto/plan-dto-discriminator.test.ts --run && pnpm --filter @wonderwaltz/api typecheck</automated>
  </verify>
  <done>DTOs compile; discriminator test green; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate OpenAPI v1 snapshot + assert CI gate passes cleanly</name>
  <files>apps/api/scripts/generate-openapi-snapshot.ts, packages/shared-openapi/openapi.v1.snapshot.json, apps/api/tests/openapi-snapshot.test.ts</files>
  <behavior>
    - Running the snapshot generator produces a JSON file containing `FullDayPlan`, `LockedDayPlan`, `RethinkRequest`, `LLBooking`, `PlanBudgetExhausted`, `ResetOption` schemas.
    - `PlanDto.days` in the snapshot has `oneOf` with discriminator `type`.
    - Generate-plan and rethink-today responses include a `402` entry referencing `#/components/schemas/PlanBudgetExhausted`.
    - The snapshot test (`openapi-snapshot.test.ts`) that compares regenerated output to committed file: PASSES (zero diff after commit).
    - No stray debug fields; formatting matches prettier-ignore list (snapshot is prettier-ignored per Phase 2 lesson).
  </behavior>
  <action>
    1. Ensure `generate-openapi-snapshot.ts` builds the Nest app with the updated DTOs. If it needs `@ApiExtraModels(LockedDayPlanDto, FullDayPlanDto, RethinkRequestDto, PlanBudgetExhaustedDto)` added on the controller or at the SwaggerModule setup level, add them.
    2. Run `pnpm --filter @wonderwaltz/api run build && pnpm --filter @wonderwaltz/api run openapi:generate` (or whatever the existing script names are — inspect package.json).
    3. Review the diff against the current snapshot:
       - Confirm added schemas: FullDayPlan, LockedDayPlan, RethinkRequest, LLBooking, PlanBudgetExhausted, ResetOption
       - Confirm `DayPlan` (the old stub) is replaced, not just additive
       - Confirm `Plan.days` has `oneOf` + discriminator
    4. Commit the regenerated snapshot.
    5. Verify CI gate by running the snapshot test `apps/api/tests/openapi-snapshot.test.ts` — it should PASS (regenerated matches committed).
    6. If the existing snapshot test does not exist or doesn't cover all new schemas, extend it with explicit assertions:
       - `snapshot.components.schemas.FullDayPlan` exists
       - `snapshot.components.schemas.LockedDayPlan.properties.type.enum` contains `'locked'`
       - `snapshot.components.schemas.Plan.properties.days.items.oneOf` length === 2
       - `snapshot.components.schemas.PlanBudgetExhausted.properties.resetOptions.items.$ref` references `ResetOption`

    Do NOT regenerate the snapshot again in later Phase 3 plans. Downstream plans must add endpoint *bodies* without changing DTO shapes. If a later plan needs a new field, it's a follow-up plan that explicitly re-opens the snapshot — not drive-by.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api run build && pnpm --filter @wonderwaltz/api test -- tests/openapi-snapshot.test.ts --run</automated>
  </verify>
  <done>Snapshot regenerates identically on repeated runs; CI gate passes; all 6 new schemas present.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test -- tests/dto tests/openapi-snapshot.test.ts --run` green
- `pnpm -r typecheck` clean
- `git diff packages/shared-openapi/openapi.v1.snapshot.json` shows deliberate additions only — no formatting churn
</verification>

<success_criteria>
- v1 snapshot contains FullDayPlan/LockedDayPlan discriminator, RethinkRequest, PlanBudgetExhausted.
- CI snapshot gate green.
- No additional snapshot updates required for the remainder of Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/03-engine/03-03-SUMMARY.md` documenting: DTOs added, discriminator pattern used, snapshot gate status, explicit note that this was the deliberate one-time amendment.
</output>
