---
phase: 03-engine
plan: 15
type: execute
wave: 6
depends_on: [03-14]
files_modified:
  - apps/api/src/plan-generation/rate-limit.service.ts
  - apps/api/src/plan-generation/rate-limit.guard.ts
  - apps/api/src/plan-generation/plan-generation.module.ts
  - apps/api/tests/plan-generation/rethink-rate-limit.test.ts
  - apps/api/tests/plan-generation/free-tier-lifetime.test.ts
autonomous: true
requirements: [LLM-08, PLAN-05]
must_haves:
  truths:
    - "Rethink cap per-user-per-day: 15 unlocked / 5 free-tier — 16th/6th returns 429"
    - "Free-tier plan generation cap: 3 per anonymous user lifetime — 4th returns 403 (not 429 — different UX)"
    - "Redis counters: `rethink:{user_id}:{utc_date}` with EXPIRE at UTC midnight; `plans_generated:{user_id}` with no expiry"
    - "Rate limits enforced via NestJS Guard on the controller endpoints"
  artifacts:
    - path: "apps/api/src/plan-generation/rate-limit.service.ts"
      provides: "checkRethinkLimit + checkFreeTierLifetime + incrementers"
    - path: "apps/api/src/plan-generation/rate-limit.guard.ts"
      provides: "RateLimitGuard for @UseGuards on generatePlan + rethinkToday"
  key_links:
    - from: "apps/api/src/plan-generation/rate-limit.guard.ts"
      to: "Redis INCR + EXPIRE"
      via: "atomic Redis calls"
      pattern: "INCR|EXPIRE"
---

<objective>
Ship LLM-08 (rethink daily cap) + PLAN-05 (free-tier lifetime cap). Both enforced via Redis counters + NestJS Guard.

Output:
  - RateLimitService with two counter types
  - RateLimitGuard ready to be applied in plan 03-17 (endpoints)
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine/03-CONTEXT.md
@.planning/phases/03-engine/03-RESEARCH.md
@apps/api/src/shared-infra.module.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RateLimitService (rethink + free-tier lifetime counters)</name>
  <files>apps/api/src/plan-generation/rate-limit.service.ts, apps/api/src/plan-generation/plan-generation.module.ts, apps/api/tests/plan-generation/rethink-rate-limit.test.ts, apps/api/tests/plan-generation/free-tier-lifetime.test.ts</files>
  <behavior>
    - `checkRethinkLimit(userId, isUnlocked): { allowed: boolean, remaining: number, resetAt: Date }`
      - Key: `rethink:${userId}:${utcDateString}`
      - Cap: 15 if unlocked, 5 if free-tier.
      - Uses `INCR` then checks value; if over cap, DECR and return disallowed.
      - Sets `EXPIRE` on key to 86400 seconds on first INCR.
    - `checkFreeTierLifetime(userId): { allowed: boolean, remaining: number }`
      - Key: `plans_generated:${userId}` (no expiry).
      - Cap: 3.
      - Same INCR/DECR pattern.
    - Both return structured results. HTTP-layer wrapping in guard (Task 2).
    - UTC day boundary: `utcDateString = new Date().toISOString().slice(0, 10)` — deterministic across timezones.
  </behavior>
  <action>
    1. Implement `RateLimitService` injecting `REDIS_CLIENT_TOKEN`.
    2. Register in `PlanGenerationModule`.
    3. `rethink-rate-limit.test.ts`:
       - Unlocked user × 15 calls → all allowed; 16th disallowed.
       - Free-tier user × 5 calls → all allowed; 6th disallowed.
       - `remaining` field correct across the sequence.
       - Key TTL set to 86400 on first call (Redis mock assertion).
       - Cross-day: simulate date change in mock → counter resets.
    4. `free-tier-lifetime.test.ts`:
       - 3 calls → allowed; 4th disallowed.
       - No expiry on key.
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/plan-generation/rethink-rate-limit.test.ts tests/plan-generation/free-tier-lifetime.test.ts --run</automated>
  </verify>
  <done>Both counters enforce correctly; table-driven tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RateLimitGuard</name>
  <files>apps/api/src/plan-generation/rate-limit.guard.ts, apps/api/src/plan-generation/plan-generation.module.ts, apps/api/tests/plan-generation/rate-limit-guard.test.ts</files>
  <behavior>
    - `@Injectable() RateLimitGuard implements CanActivate`
    - Reads `Reflector` metadata for `@RateLimit('rethink' | 'free-tier-lifetime')` decorator.
    - On `rethink`: calls `checkRethinkLimit`; if disallowed → throw `HttpException('rate_limited', 429)`.
    - On `free-tier-lifetime`: calls `checkFreeTierLifetime`; if disallowed → throw `HttpException('free_tier_exhausted', 403)`.
    - Resolves `userId` from `request.user?.id` (Phase 4 attaches via auth middleware; Phase 3 can use `request.headers['x-anon-user-id']` as a stub).
    - Guard bypassed when `isUnlocked` cannot be determined — defaults to free-tier for safety.
  </behavior>
  <action>
    1. Implement guard + `@RateLimit(type)` decorator metadata key.
    2. `rate-limit-guard.test.ts` using `@nestjs/testing` controller harness:
       - Hit mock endpoint 16 times with rethink guard → 16th throws 429.
       - Hit mock endpoint 4 times with free-tier guard → 4th throws 403.
       - Missing userId → throws 401 (unauthorized — defensive default).
  </action>
  <verify>
    <automated>pnpm --filter @wonderwaltz/api test -- tests/plan-generation/rate-limit-guard.test.ts --run</automated>
  </verify>
  <done>Guard returns correct HTTP statuses; ready to attach to controller in 03-17.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @wonderwaltz/api test -- tests/plan-generation/rate-limit* tests/plan-generation/free-tier* --run` green
</verification>

<success_criteria>
- LLM-08 + PLAN-05 automated.
- Guard ready for wiring in endpoint plan (03-17).
</success_criteria>

<output>
`.planning/phases/03-engine/03-15-SUMMARY.md` — Redis key shapes, counter semantics, guard usage.
</output>
